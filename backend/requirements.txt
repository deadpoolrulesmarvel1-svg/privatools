fastapi==0.137.2
uvicorn[standard]==0.27.1
python-multipart==0.0.32
pikepdf==8.12.0
pypdf==6.13.3
pdf2image==1.17.0
Pillow==12.2.0
reportlab==4.1.0
aiofiles==25.1.0
pytesseract==0.3.13
weasyprint
slowapi==0.1.9
PyMuPDF==1.27.2.3
python-docx
openpyxl
python-pptx
# Prod-only deps — these mirror the root /requirements.txt and cover the
# tools that the slim dev image doesn't need (background removal, HEIC
# decode, barcode/QR generators, markdown rendering, defused XML parsing).
# The backend imports each of these directly somewhere under app/services.
pillow-heif
onnxruntime
rembg
cairosvg
python-barcode
qrcode
pyzbar
defusedxml
mistune
