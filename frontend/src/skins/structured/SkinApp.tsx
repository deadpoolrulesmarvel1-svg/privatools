/* AUTO-GENERATED by scripts/build-skin-app.mjs — do not edit by hand.
 *
 * Structured Privacy OS, ported from its Claude Design project.
 *
 * The logic below is the design's own component class, unchanged except for
 * its base (DCLogic -> React.Component, which have the same setState and
 * lifecycle contract). The markup is its own markup with its own inline
 * styles, mechanically converted to JSX. Both are kept verbatim so the result
 * matches the source design rather than approximating it.
 *
 * Regenerate:  node scripts/build-skin-app.mjs structured
 */
/* eslint-disable */
// @ts-nocheck
import React from "react";
import { css } from "@/lib/skin/css";
import { skinIcon } from "@/lib/skin/icons";
import { AURORA_CATALOGUE, CARBON_REGISTRY } from "@/skins/catalogue";

/** Icon lookup by Material Symbols name, resolved to this skin's font. */
const ICON = new Proxy({}, { get: (_t, name) => skinIcon("structured", String(name)) });

const FAMS = {
  'PDF': ['picture_as_pdf', 'var(--coral)', 'var(--coralSoft)'],
  'Images': ['image', 'var(--blue)', 'var(--blueSoft)'],
  'Video': ['movie', 'var(--violet)', 'rgba(158,130,247,.12)'],
  'Audio': ['graphic_eq', 'var(--teal)', 'rgba(25,183,164,.12)'],
  'Archives': ['folder_zip', 'var(--amber)', 'var(--amberSoft)'],
  'Documents & Data': ['table_chart', 'var(--em)', 'var(--emSoft)']
};
const RAW = [
  ['compress-pdf','Compress PDF','Reduce PDF file size','PDF','Optimise','Compress','fallback','PDF','PDF',1],
  ['merge-pdf','Merge PDF','Combine multiple PDF files','PDF','Organise','Organise','local','PDF','PDF',1],
  ['split-pdf','Split PDF','Split one PDF into separate files','PDF','Organise','Organise','local','PDF','PDF',0],
  ['organise-pages','Organise pages','Reorder, delete and insert pages','PDF','Organise','Organise','local','PDF','PDF',0],
  ['extract-pages','Extract pages','Save chosen pages as a new PDF','PDF','Organise','Organise','local','PDF','PDF',0],
  ['rotate-pdf','Rotate PDF','Turn pages to the right angle','PDF','Organise','Edit','local','PDF','PDF',0],
  ['pdf-to-jpg','PDF to JPG','Extract images from PDF','PDF','Convert from PDF','Convert','local','PDF','JPG',1],
  ['pdf-to-png','PDF to PNG','Render pages as PNG images','PDF','Convert from PDF','Convert','local','PDF','PNG',0],
  ['pdf-to-text','PDF to text','Pull plain text out of a PDF','PDF','Convert from PDF','Extract','local','PDF','TXT',0],
  ['pdf-to-word','PDF to Word','Convert PDF into an editable document','PDF','Convert from PDF','Convert','server','PDF','DOCX',1],
  ['pdf-to-excel','PDF to Excel','Turn PDF tables into a spreadsheet','PDF','Convert from PDF','Convert','server','PDF','XLSX',0],
  ['jpg-to-pdf','JPG to PDF','Combine images into one PDF','PDF','Convert to PDF','Convert','local','JPG','PDF',1],
  ['word-to-pdf','Word to PDF','Convert documents to PDF','PDF','Convert to PDF','Convert','fallback','DOCX','PDF',0],
  ['excel-to-pdf','Excel to PDF','Convert spreadsheets to PDF','PDF','Convert to PDF','Convert','fallback','XLSX','PDF',0],
  ['ppt-to-pdf','PowerPoint to PDF','Convert slides to PDF','PDF','Convert to PDF','Convert','fallback','PPTX','PDF',0],
  ['markdown-to-pdf','Markdown to PDF','Render Markdown as a PDF','PDF','Convert to PDF','Convert','local','MD','PDF',0],
  ['html-to-pdf','HTML to PDF','Save a page or file as PDF','PDF','Convert to PDF','Convert','server','HTML','PDF',0],
  ['protect-pdf','Protect PDF','Add a password to a PDF','PDF','Security','Security','local','PDF','PDF',1],
  ['unlock-pdf','Unlock PDF','Remove a password you own','PDF','Security','Security','local','PDF','PDF',0],
  ['redact-pdf','Redact PDF','Permanently remove content','PDF','Security','Security','local','PDF','PDF',1],
  ['sign-pdf','Sign PDF','Place a signature on a page','PDF','Security','Security','local','PDF','PDF',0],
  ['remove-pdf-metadata','Remove PDF metadata','Strip author, tool and history data','PDF','Security','Security','local','PDF','PDF',0],
  ['watermark-pdf','Watermark PDF','Stamp text or an image on pages','PDF','Edit','Edit','local','PDF','PDF',0],
  ['add-page-numbers','Add page numbers','Number pages your way','PDF','Edit','Edit','local','PDF','PDF',0],
  ['crop-pdf','Crop PDF','Trim page margins','PDF','Edit','Edit','local','PDF','PDF',0],
  ['edit-pdf-metadata','Edit PDF metadata','Set title, author and subject','PDF','Edit','Edit','local','PDF','PDF',0],
  ['bates-numbering','Bates numbering','Sequential legal numbering','PDF','Advanced','Automate','local','PDF','PDF',0],
  ['ocr-pdf','OCR PDF','Make a scan searchable','PDF','Advanced','Extract','fallback','PDF','PDF',1],
  ['flatten-pdf','Flatten PDF','Merge layers and form fields','PDF','Advanced','Advanced','local','PDF','PDF',0],
  ['compare-pdf','Compare PDF','See what changed between versions','PDF','Advanced','Advanced','local','PDF','PDF',0],
  ['pdf-to-pdfa','PDF to PDF/A','Convert for long-term archiving','PDF','Advanced','Advanced','server','PDF','PDF',0],
  ['heic-to-jpg','HEIC to JPG','Convert HEIC photos','Images','Convert','Convert','local','HEIC','JPG',1],
  ['png-to-jpg','PNG to JPG','Convert PNG images','Images','Convert','Convert','local','PNG','JPG',0],
  ['jpg-to-png','JPG to PNG','Convert JPG images','Images','Convert','Convert','local','JPG','PNG',0],
  ['webp-to-jpg','WebP to JPG','Convert WebP images','Images','Convert','Convert','local','WEBP','JPG',0],
  ['image-to-webp','Image to WebP','Make smaller web images','Images','Convert','Convert','local','JPG','WEBP',0],
  ['svg-to-png','SVG to PNG','Rasterise vector art','Images','Convert','Convert','fallback','SVG','PNG',0],
  ['compress-image','Compress image','Reduce image file size','Images','Optimise','Compress','local','JPG','JPG',1],
  ['resize-image','Resize image','Set exact width and height','Images','Edit','Edit','local','JPG','JPG',0],
  ['crop-image','Crop image','Trim to the part you need','Images','Edit','Edit','local','JPG','JPG',0],
  ['rotate-image','Rotate image','Straighten or turn a photo','Images','Edit','Edit','local','JPG','JPG',0],
  ['remove-exif','Remove EXIF data','Strip location and camera data','Images','Security','Security','local','JPG','JPG',1],
  ['image-ocr','Image to text','Read text out of a picture','Images','Extract','Extract','fallback','JPG','TXT',0],
  ['convert-to-mp4','Convert to MP4','Convert video files','Video','Convert','Convert','fallback','MOV','MP4',1],
  ['mov-to-mp4','MOV to MP4','Convert Apple video files','Video','Convert','Convert','fallback','MOV','MP4',0],
  ['video-to-gif','Video to GIF','Make a short looping clip','Video','Convert','Convert','fallback','MP4','GIF',0],
  ['compress-video','Compress video','Reduce video file size','Video','Optimise','Compress','server','MP4','MP4',0],
  ['trim-video','Trim video','Cut the start and end','Video','Edit','Edit','local','MP4','MP4',0],
  ['extract-audio','Extract audio','Save the sound track only','Video','Extract','Extract','local','MP4','MP3',0],
  ['mute-video','Mute video','Remove the audio track','Video','Edit','Edit','local','MP4','MP4',0],
  ['mp3-converter','MP3 converter','Convert audio files','Audio','Convert','Convert','local','WAV','MP3',1],
  ['wav-to-mp3','WAV to MP3','Compress lossless audio','Audio','Convert','Convert','local','WAV','MP3',0],
  ['compress-audio','Compress audio','Reduce audio file size','Audio','Optimise','Compress','fallback','MP3','MP3',0],
  ['trim-audio','Trim audio','Cut a section of audio','Audio','Edit','Edit','local','MP3','MP3',0],
  ['audio-metadata','Edit audio tags','Set title, artist and album','Audio','Edit','Edit','local','MP3','MP3',0],
  ['zip-extractor','ZIP extractor','Extract files from ZIP archives','Archives','Extract','Extract','local','ZIP','FILES',1],
  ['create-zip','Create ZIP','Package files into one archive','Archives','Convert','Convert','local','FILES','ZIP',0],
  ['rar-extractor','RAR extractor','Extract files from RAR archives','Archives','Extract','Extract','local','RAR','FILES',0],
  ['sevenz-extractor','7z extractor','Extract files from 7z archives','Archives','Extract','Extract','local','7Z','FILES',0],
  ['targz-extractor','TAR.GZ extractor','Extract Unix archives','Archives','Extract','Extract','local','TGZ','FILES',0],
  ['csv-to-json','CSV to JSON','Restructure tabular data','Documents & Data','Convert','Convert','local','CSV','JSON',0],
  ['json-to-csv','JSON to CSV','Flatten data into a table','Documents & Data','Convert','Convert','local','JSON','CSV',0],
  ['xlsx-to-csv','XLSX to CSV','Export a sheet as plain CSV','Documents & Data','Convert','Convert','local','XLSX','CSV',0],
  ['docx-to-txt','DOCX to text','Strip formatting from a document','Documents & Data','Convert','Extract','local','DOCX','TXT',0],
  ['epub-to-pdf','EPUB to PDF','Convert an ebook to PDF','Documents & Data','Convert','Convert','fallback','EPUB','PDF',0]
];
const SYN = {shrink:'compress',smaller:'compress',reduce:'compress',size:'compress',photo:'image',picture:'image',pic:'image',zip:'archive',password:'protect',lock:'protect',secure:'protect',unlock:'unlock',hide:'redact',blackout:'redact',scan:'ocr',searchable:'ocr',text:'extract',combine:'merge',join:'merge',split:'split',turn:'rotate',exif:'metadata',location:'exif',gif:'gif',sound:'audio',music:'audio'};
const TASKS = ['Convert','Compress','Edit','Organise','Extract','Security','Automate'];
var LIVE = null;

class Component extends React.Component {
  state = {
    theme: 'dark', route: 'home', slug: 'compress-pdf', w: 1440,
    drawer: false, palette: false, pq: '', pIdx: 0,
    returning: true, drop: 'idle', file: null,
    fam: 'All', task: 'All', view: 'list', q: '',
    inp: 'All', out: 'All', pmode: 'All', onlyFav: false, preview: 'compress-pdf', sheet: null,
    stage: 'input', opts: {}, prog: 0, stageText: '', alert: null, serverRun: false, zip: false, faq: null,
    pipe: [
      { slug: 'compress-pdf', on: true, fixed: true, label: 'Add files', sub: 'From device', icon: 'note_add', mode: 'local' },
      { slug: 'compress-pdf', on: true },
      { slug: 'redact-pdf', on: true, fail: true },
      { slug: 'pdf-to-jpg', on: true },
      { slug: 'compress-pdf', on: true, fixed: true, label: 'Save output', sub: 'To device', icon: 'download', mode: 'local' }
    ],
    pipeSel: 2, pipeState: 'configured', pipeAt: 0, pipeProg: 0, pipeFailed: -1, pipeFile: true, preserve: true, libQ: '',
    saved: [{ name: 'Daily Redact', steps: ['redact-pdf', 'compress-pdf'] }, { name: 'Compress Batch', steps: ['compress-pdf'] }, { name: 'Photo Prepare', steps: ['resize-image', 'compress-image', 'remove-exif'] }],
    activity: [
      { slug: 'compress-pdf', date: '20 Aug 2026', settings: 'Medium · Remove metadata on' },
      { slug: 'heic-to-jpg', date: '18 Aug 2026', settings: 'Quality 90 · Keep dates off' },
      { slug: 'merge-pdf', date: '15 Aug 2026', settings: '3 inputs · Bookmarks kept' },
      { slug: 'redact-pdf', date: '14 Aug 2026', settings: 'Text & images · All pages' },
      { slug: 'zip-extractor', date: '11 Aug 2026', settings: 'Flatten folders off' }
    ],
    assets: [
      { name: 'Signature — dark ink', kind: 'Signature', icon: 'draw' },
      { name: 'Company mark', kind: 'Logo', icon: 'workspaces' },
      { name: 'Confidential stamp', kind: 'Stamp', icon: 'approval' }
    ],
    bates: [{ name: 'Case A — discovery', next: 1042 }, { name: 'Case B — exhibits', next: 318 }],
    defaults: [{ slug: 'compress-pdf', value: 'Medium · metadata removed' }, { slug: 'redact-pdf', value: 'Text & images · flatten on' }, { slug: 'resize-image', value: '1600 × 900 · keep ratio' }],
    conns: [{ name: 'Google Drive', icon: 'add_to_drive', on: true }, { name: 'Dropbox', icon: 'inventory_2', on: false }, { name: 'OneDrive', icon: 'cloud', on: true }, { name: 'Box', icon: 'inbox', on: false }],
    vault: [
      { name: 'Email (work)', user: 'you@example.com', pw: 'k7-Fern-Quay-2026', icon: 'mail' },
      { name: 'Bank account', user: 'personal', pw: 'Tide*Marsh*4419', icon: 'account_balance' },
      { name: 'Wi-Fi home', user: 'router admin', pw: 'copper-lane-88', icon: 'wifi' },
      { name: 'Archive passphrase', user: 'backups', pw: 'slate-harbour-quiet', icon: 'folder_zip' }
    ],
    vaultOpen: false, reveal: null, protect: 'pin',
    vaultStage: 'locked', pin: '', pinErr: '', pinTries: 0, pass: '', passShow: false, passErr: '', vSecret: '', draftSecret: '', storeReset: null,
    entryEdit: null, entryDraft: { name: '', user: '', pw: '' },
    dialog: null, bk: { pass: '', confirm: '', show: false, state: 'idle' },
    competitor: 'reference-profile-a', failedPath: '/not-found', live: '', restored: false, blogQ: '',
    pendingDelete: null, hideIncidents: false,
    priority: 'all', drawer2: null, blogCat: 'All', postSlug: 'how-local-file-tools-protect-your-privacy',
    logWin: '24h', form: { subject: '', desc: '', cat: 'Report an issue', file: null }, formState: 'idle', formTouched: false,
    batchTool: 'compress-pdf', batchState: 'idle',
    batch: [
      { name: 'report-q1.pdf', size: '4.2 MB', tool: 'Compress PDF', status: 'queued', prog: 0, step: 'Reading locally', failStep: 'Redact' },
      { name: 'invoice-042.pdf', size: '2.1 MB', tool: 'Compress PDF', status: 'queued', prog: 0, step: 'Reading locally', failStep: 'Redact' },
      { name: 'contract-draft.pdf', size: '9.8 MB', tool: 'Compress PDF', status: 'queued', prog: 0, step: 'Optimising', failStep: 'Redact', willFail: true },
      { name: 'slides-deck.pdf', size: '22.4 MB', tool: 'Compress PDF', status: 'queued', prog: 0, step: 'Optimising', failStep: 'Convert' },
      { name: 'notes.pdf', size: '1.1 MB', tool: 'Compress PDF', status: 'queued', prog: 0, step: 'Writing output', failStep: 'Convert to JPG', willFail: true },
      { name: 'photo-set.zip', size: '31.2 MB', tool: 'Compress PDF', status: 'queued', prog: 0, step: 'Extracting', failStep: 'Extract' }
    ],
    favs: ['compress-pdf', 'heic-to-jpg'], recents: ['merge-pdf', 'zip-extractor', 'convert-to-mp4'],
    toast: null, history: ['home']
  };

  catalogue() {
    var ext = (typeof window !== 'undefined' && window.PRIVATOOLS_CATALOGUE) || null;
    if (ext && ext.records && ext.records.length) return ext;
    return {
      meta: { declaredTotal: 221, declaredPdf: 107, declaredNonPdf: 114, publicCopy: '200+ free file tools', dataset: 'representative', note: 'Built-in fallback records.' },
      records: RAW.map(function (r) {
        return { slug: r[0], name: r[1], purpose: r[2], family: r[3], subfamily: r[4], task: r[5], mode: r[6], input: r[7], output: r[8], popular: !!r[9] };
      })
    };
  }
  tools() {
    if (this._tc) return this._tc;
    var cat = this.catalogue();
    var rep = cat.meta && cat.meta.dataset === 'representative';
    this._tc = cat.records.map(function (r) {
      var f = FAMS[r.family] || FAMS['Documents & Data'];
      var verified = r.modeVerified === true;
      var mode = (rep && !verified) ? 'unverified' : r.mode;
      return { slug: r.slug, name: r.name, purpose: r.purpose, fam: r.family, sub: r.subfamily, task: r.task, mode: mode, declaredMode: r.mode, modeVerified: verified, inp: r.input, out: r.output, popular: !!r.popular, icon: f[0], color: f[1], tint: f[2], offline: mode === 'local', options: r.options || null };
    });
    return this._tc;
  }
  byslug(s) { var t = this.tools().filter(function (x) { return x.slug === s; }); return t[0] || null; }
  toolOr404(s) { return this.byslug(s); }
  modeLabel(m) { return m === 'local' ? 'Local' : m === 'fallback' ? 'Local + server fallback' : m === 'server' ? 'Server required' : 'Processing mode unverified'; }
  modeShort(m) { return m === 'local' ? 'Local' : m === 'fallback' ? 'Fallback' : m === 'server' ? 'Server' : 'Unverified'; }
  modeColor(m) { return m === 'local' ? 'var(--em)' : m === 'fallback' ? 'var(--teal)' : m === 'server' ? 'var(--amber)' : 'var(--ink3)'; }

  postData() {
    return [
      ['how-local-file-tools-protect-your-privacy', 'How local file tools protect your privacy', 'Privacy', '20 May 2026', '6 min read', 'Local-first tools keep your files on your device. No uploads, no tracking, no retention. Here is how it works and why it matters.'],
      ['choosing-the-right-file-tool', 'Choosing the right file tool: what to look for', 'How-to', '10 May 2026', '5 min read', 'Size limits, offline support, and where processing happens matter more than the length of a feature list.'],
      ['pwa-vs-native', 'PWA vs native: why it matters for privacy', 'Product', '28 Apr 2026', '4 min read', 'An installable web app can work offline without asking for access to the rest of your device.'],
      ['pdf-compression-explained', 'PDF compression, explained without the jargon', 'Formats', '12 Apr 2026', '7 min read', 'What actually shrinks when you compress a PDF, and which settings change quality.'],
      ['when-a-server-is-needed', 'When a server is genuinely needed', 'Privacy', '2 Apr 2026', '5 min read', 'Some conversions cannot finish in a browser. Here is how we disclose it and what happens to the file.'],
      ['metadata-you-did-not-know', 'The metadata you did not know you were sharing', 'Privacy', '18 Mar 2026', '6 min read', 'Documents and photos carry authorship, device and location traces. Removing them is one step.']
    ].map(function (p, i) { return { slug: p[0], title: p[1], cat: p[2], date: p[3], read: p[4], lead: p[5], complete: i === 0 }; });
  }
  postBySlug(s) { var a = this.postData().filter(function (p) { return p.slug === s; }); return a[0] || null; }
  competitorData() {
    var ill = [
      ['reference-profile-a', 'Reference profile A', 'Cloud-first, no local cache', 'Larger template library for office formats.'],
      ['reference-profile-b', 'Reference profile B', 'Cloud-first, limited free tier', 'Team accounts and shared workspaces.'],
      ['reference-profile-c', 'Reference profile C', 'Local-first, small size limits', 'Native desktop integration on one platform.']
    ].map(function (c, i) { return { slug: c[0], name: c[1], sub: c[2], advantage: c[3], illustrative: true, verified: false, col: i + 1 }; });
    var pending = [];
    for (var i = 4; i <= 13; i++) {
      pending.push({ slug: 'competitor-slot-' + i, name: 'Competitor slot ' + i, sub: 'Verified comparison data pending', advantage: '', illustrative: false, verified: false, col: i });
    }
    return ill.concat(pending);
  }
  competitorBySlug(s) { var a = this.competitorData().filter(function (c) { return c.slug === s; }); return a[0] || null; }
  routeTable() {
    return [
      ['/', 'home'], ['/tools', 'tools'], ['/tool/', 'tool'], ['/tools/', 'tool'],
      ['/pipeline', 'pipeline'], ['/batch', 'batch'], ['/my-stuff/vault', 'vault'], ['/my-stuff', 'mystuff'],
      ['/compare/', 'competitor'], ['/compare', 'compare'], ['/blog/', 'post'], ['/blog', 'blog'],
      ['/about', 'about'], ['/privacy', 'privacy'], ['/security', 'security'], ['/terms', 'terms'],
      ['/acceptable-use', 'acceptable-use'], ['/processors', 'processors'], ['/accessibility', 'accessibility'],
      ['/support', 'support'], ['/status', 'status'], ['/pwa-errors', 'atlas'], ['/_showcase', 'showcase']
    ];
  }
  pathFor(route, slug) {
    if (route === 'tool') return '/tool/' + (slug || this.state.slug);
    if (route === 'post') return '/blog/' + (slug || this.state.postSlug);
    if (route === 'competitor') return '/compare/' + (slug || this.state.competitor);
    if (route === 'vault') return '/my-stuff/vault';
    if (route === 'mystuff') return '/my-stuff';
    if (route === 'atlas') return '/pwa-errors';
    if (route === 'showcase') return '/_showcase';
    if (route === 'home') return '/';
    if (route === '404') { var p = slug || (LIVE && LIVE.failedPath) || this.state.failedPath || '/not-found'; if (!p || p === '/') p = '/not-found'; return p.charAt(0) === '/' ? p : '/' + p; }
    return '/' + route;
  }
  parsePath(raw) {
    var p = (raw || '/').split('?')[0];
    if (p.length > 1 && p.charAt(p.length - 1) === '/') p = p.slice(0, -1);
    if (!p) p = '/';
    var tab = this.routeTable();
    for (var i = 0; i < tab.length; i++) {
      var pat = tab[i][0], route = tab[i][1];
      if (pat.charAt(pat.length - 1) === '/') {
        if (p.indexOf(pat) === 0 && p.length > pat.length) return { route: route, param: p.slice(pat.length) };
      } else if (p === pat) return { route: route, param: null };
    }
    return { route: '404', param: p };
  }
  titleFor(route, param) {
    var base = ' — PrivaTools';
    var map = {
      home: 'Work with files. Keep them private.', tools: 'All Tools — 200+ free file tools', pipeline: 'Pipeline',
      batch: 'Batch', mystuff: 'My Stuff', vault: 'Vault', compare: 'Compare file tools', blog: 'Blog',
      about: 'About', privacy: 'Privacy Policy', security: 'Security Policy', terms: 'Terms of Service',
      'acceptable-use': 'Acceptable Use Policy', processors: 'Processors', accessibility: 'Accessibility',
      support: 'Support', status: 'Status', atlas: 'PWA, offline and error states', showcase: 'Component showcase',
      '404': 'Page not found'
    };
    if (route === 'tool') { var tt = this.byslug(param || this.state.slug); return (tt ? tt.name : 'Tool not found') + base; }
    if (route === 'post') return (this.postBySlug(param || this.state.postSlug) || { title: 'Article' }).title + base;
    if (route === 'competitor') return (this.competitorBySlug(param || this.state.competitor) || { name: 'Comparison' }).name + ' vs PrivaTools' + base;
    return (map[route] || 'PrivaTools') + (route === 'home' ? '' : base);
  }
  describeFor(route) {
    var d = {
      home: 'Free, local-first file tools. 200+ tools, 500 MB per file, no account, no analytics, no document retention.',
      tools: 'Browse 200+ free file tools by family, task, input, output and processing mode.',
      compare: 'Compare file tools on the priorities that matter to you. No universal winner.',
      status: 'Live status for the website, local tools and best-effort server processing in Mumbai, India.'
    };
    return d[route] || 'Free, local-first file tools. No account, no analytics, no document retention.';
  }
  applyUrl(route, param, replace) {
    var path = this.pathFor(route, param);
    var href = '#' + path;
    try {
      if (replace) history.replaceState({ r: route, p: param }, '', href);
      else if (location.hash !== href) history.pushState({ r: route, p: param }, '', href);
    } catch (e) {
      location.hash = path;
    }
    document.title = this.titleFor(route, param);
    var m = document.querySelector('meta[name="description"]');
    if (!m) { m = document.createElement('meta'); m.setAttribute('name', 'description'); document.head.appendChild(m); }
    m.setAttribute('content', this.describeFor(route));
  }
  readUrl() {
    var h = (location.hash || '').replace(/^#/, '');
    if (h && h.charAt(0) === '/') return this.parsePath(h);
    if (h) return null;
    var p = location.pathname || '/';
    if (p !== '/' && p.indexOf('.htm') < 0 && p.indexOf('/serve/') < 0) return this.parsePath(p);
    return { route: 'home', param: null };
  }
  applyRouteState(hit, push) {
    var s = { route: hit.route, drawer: false, palette: false, dialog: null };
    if (hit.route === 'tool' && hit.param) {
      var found = this.byslug(hit.param);
      if (!found) { s.route = '404'; s.failedPath = '/tool/' + hit.param; s.badSlug = hit.param; hit = { route: '404', param: s.failedPath }; }
      else { s.slug = found.slug; s.badSlug = null; s.stage = 'input'; s.file = null; s.prog = 0; }
    }
    if (hit.route === 'post' && hit.param) s.postSlug = hit.param;
    if (hit.route === 'competitor' && hit.param) s.competitor = hit.param;
    if (hit.route === '404') s.failedPath = hit.param || this.state.failedPath || '/not-found';
    this.setState(s);
    this.applyUrl(hit.route, hit.param, !push);
    this.announce(this.titleFor(hit.route, hit.param));
    return s;
  }
  announce(msg) { var self = this; this.setState({ live: msg }); }

  load() {
    var raw = null;
    try { raw = localStorage.getItem('privatools.local.v1'); } catch (e) { return; }
    if (!raw) return;
    var d = null;
    try { d = JSON.parse(raw); } catch (e) { this.resetStore('unreadable'); return; }
    if (!d || typeof d !== 'object') { this.resetStore('unreadable'); return; }
    if (d.schema !== 2) { this.resetStore(d.schema ? 'migrated from schema ' + d.schema : 'migrated from an unversioned store'); return; }
    var s = {};
    var shape = { theme: 'string', saved: 'array', defaults: 'array', activity: 'array', assets: 'array', bates: 'array', conns: 'array', vault: 'array', protect: 'string', vSecret: 'string' };
    Object.keys(shape).forEach(function (k) {
      var v = d[k];
      if (v === undefined || v === null) return;
      var ok = shape[k] === 'array' ? Array.isArray(v) : typeof v === shape[k];
      if (ok) s[k] = v;
    });
    if (s.vSecret) s.vaultStage = 'locked';
    s.restored = true;
    this.setState(s);
    if (s.theme) this.applyTheme(s.theme);
  }
  resetStore(reason) {
    try { localStorage.removeItem('privatools.local.v1'); } catch (e) { }
    this.setState({ storeReset: reason || 'reset' });
  }
  save() {
    var st = LIVE || this.state;
    var d = { schema: 2, theme: st.theme, saved: st.saved, defaults: st.defaults, activity: st.activity, assets: st.assets, bates: st.bates, conns: st.conns, vault: st.vault, protect: st.protect, vSecret: st.vSecret || '' };
    try { localStorage.setItem('privatools.local.v1', JSON.stringify(d)); } catch (e) { }
  }

  componentDidMount() {
    var self = this;
    this.onResize = function () { self.setState({ w: window.innerWidth }); };
    this.onResize();
    window.addEventListener('resize', this.onResize);
    this.onKey = function (e) {
      var k = e.key;
      if ((e.metaKey || e.ctrlKey) && k.toLowerCase() === 'k') { e.preventDefault(); self.openPalette(); return; }
      if (k === 'Escape') { e.preventDefault(); self.closeTop(); return; }
      if (k === 'Tab' && self.overlayOpen()) { self.trapKey(e); return; }
      if (k === '/' && !/input|textarea|select/i.test((e.target.tagName || '')) && !self.overlayOpen()) { e.preventDefault(); self.openPalette(); }
    };
    window.addEventListener('keydown', this.onKey);
    this.onNav = function (e) {
      var st = e && e.state;
      if (st && st.r) { self.applyRouteState({ route: st.r, param: st.p || null }, false); return; }
      var hit = self.readUrl();
      if (!hit) return;
      self.applyRouteState(hit, false);
    };
    window.addEventListener('popstate', this.onNav);
    window.addEventListener('hashchange', this.onNav);

    this.load();
    if (!document.documentElement.getAttribute('data-theme')) this.applyTheme(this.state.theme || this.props.initialTheme || 'dark');
    var hit = this.readUrl() || { route: 'home', param: null };
    this.setState({ route: hit.route });
    this.applyRouteState(hit, false);

    if (this.props.accent) document.documentElement.style.setProperty('--em', this.props.accent);
    if (this.props.density === 'compact') { document.documentElement.style.setProperty('--rowH', '36px'); document.documentElement.style.setProperty('--pad', '12px'); }

    this.mq = window.matchMedia ? window.matchMedia('(prefers-reduced-motion: reduce)') : null;
    this.syncMotion = function () {
      var reduced = self.props.reducedMotion || (self.mq && self.mq.matches);
      if (reduced) document.documentElement.setAttribute('data-motion', 'reduced');
      else document.documentElement.removeAttribute('data-motion');
      self._reduced = !!reduced;
    };
    this.syncMotion();
    if (this.mq && this.mq.addEventListener) this.mq.addEventListener('change', this.syncMotion);

    this.onVis = function () {
      if (document.hidden) document.documentElement.setAttribute('data-paused', '1');
      else document.documentElement.removeAttribute('data-paused');
    };
    document.addEventListener('visibilitychange', this.onVis);

    this._pt = null;
    this.tilt = function (e) {
      if (self._reduced) return;
      self._pt = { x: e.clientX, y: e.clientY };
      if (self._raf) return;
      self._raf = requestAnimationFrame(function () {
        self._raf = 0;
        var el = document.getElementById('cubeMark');
        if (!el || !self._pt) return;
        var r = el.getBoundingClientRect();
        if (r.bottom < 0 || r.top > window.innerHeight) return;
        var dx = (self._pt.x - (r.left + r.width / 2)) / Math.max(1, r.width);
        var dy = (self._pt.y - (r.top + r.height / 2)) / Math.max(1, r.height);
        el.style.transform = 'perspective(600px) rotateY(' + Math.max(-3, Math.min(3, dx * 3)).toFixed(2) + 'deg) rotateX(' + Math.max(-2.5, Math.min(2.5, -dy * 2.5)).toFixed(2) + 'deg)';
      });
    };
    window.addEventListener('pointermove', this.tilt, { passive: true });
    this.onFocusIn = function (e) {
      var t = e.target;
      if (!t || !t.closest) return;
      if (!t.closest('[data-overlay="1"]')) self._lastFocus = t;
    };
    document.addEventListener('focusin', this.onFocusIn);
    this._tk = setInterval(function () { self.tick(); }, 140);
    this.onLeave = function () { self.save(); };
    window.addEventListener('pagehide', this.onLeave);
    setTimeout(function () { self.save(); }, 60);
  }
  componentWillUnmount() {
    window.removeEventListener('resize', this.onResize);
    window.removeEventListener('keydown', this.onKey);
    window.removeEventListener('popstate', this.onNav);
    window.removeEventListener('hashchange', this.onNav);
    window.removeEventListener('pointermove', this.tilt);
    document.removeEventListener('visibilitychange', this.onVis);
    document.removeEventListener('focusin', this.onFocusIn);
    window.removeEventListener('pagehide', this.onLeave);
    clearInterval(this._tk);
    if (this.mq && this.mq.removeEventListener) this.mq.removeEventListener('change', this.syncMotion);
    clearInterval(this._iv); clearInterval(this._pv); clearInterval(this._bv); clearTimeout(this._t);
  }
  tick() {
    var st = LIVE || this.state;
    var sig = [st.theme, (st.saved || []).length, (st.defaults || []).length, (st.activity || []).length, (st.assets || []).length, (st.bates || []).length, (st.vault || []).length, st.protect, st.vSecret || '', JSON.stringify(st.conns || [])].join('|');
    if (sig !== this._sig) { this._sig = sig; this.save(); }
    if (st.palette && !this._pf) { this._pf = true; var pe = document.getElementById('paletteInput'); if (pe) { try { pe.focus(); } catch (e) { } } }
    if (!st.palette) this._pf = false;
    var host = document.querySelector('[data-overlay="1"]');
    if (host && !this._trapOn) {
      this._trapOn = true;
      this.focusFirst();
    } else if (!host && this._trapOn) {
      this._trapOn = false;
      var r = this._lastFocus;
      if (r && r.focus && document.contains(r)) { try { r.focus(); } catch (e) { } }
    }
  }
  syncProps() {
    var p = this.props;
    if (p.accent && this._pa !== p.accent) { this._pa = p.accent; document.documentElement.style.setProperty('--em', p.accent); }
    if (this._pm !== p.reducedMotion) { this._pm = p.reducedMotion; if (this.syncMotion) this.syncMotion(); }
    if (this._pd !== p.density) {
      this._pd = p.density;
      document.documentElement.style.setProperty('--rowH', p.density === 'compact' ? '36px' : '44px');
      document.documentElement.style.setProperty('--pad', p.density === 'compact' ? '12px' : '16px');
    }
  }
  overlayOpen() { return !!document.querySelector('[data-overlay="1"]'); }
  focusFirst() {
    var self = this;
    setTimeout(function () {
      var host = document.querySelector('[data-overlay="1"]');
      if (!host) return;
      var pi = host.querySelector('input,textarea,select');
      var el = pi || host.querySelector('button');
      if (el) { try { el.focus(); } catch (e) { } }
    }, 30);
  }
  trapKey(e) {
    if (e.key !== 'Tab') return;
    var host = document.querySelector('[data-overlay="1"]');
    if (!host) return;
    var f = [].slice.call(host.querySelectorAll('button,input,textarea,select,a[href],[tabindex]:not([tabindex="-1"])')).filter(function (x) { return !x.disabled && x.offsetParent !== null; });
    if (!f.length) return;
    var first = f[0], last = f[f.length - 1];
    if (e.shiftKey && document.activeElement === first) { e.preventDefault(); last.focus(); }
    else if (!e.shiftKey && document.activeElement === last) { e.preventDefault(); first.focus(); }
  }
  openPalette() { this.setState({ palette: true, pq: '', pIdx: 0, drawer: false, drawer2: null, dialog: null }); }
  closeTop() {
    var s = this.state;
    if (s.dialog) { this.setState({ dialog: null }); return; }
    if (s.palette) { this.setState({ palette: false }); return; }
    if (s.drawer2) { this.setState({ drawer2: null }); return; }
    if (s.drawer) { this.setState({ drawer: false }); return; }
  }
  applyTheme(t) {
    var eff = t;
    if (t === 'system') eff = (window.matchMedia && window.matchMedia('(prefers-color-scheme: light)').matches) ? 'light' : 'dark';
    document.documentElement.setAttribute('data-theme', eff);
    var self = this;
    this.setState({ theme: t }, function () { self.save(); });
  }
  go(route, slug) {
    this.applyRouteState({ route: route, param: slug || null }, true);
    window.scrollTo(0, 0);
  }
  toast(msg) { var self = this; this.setState({ toast: msg }); clearTimeout(this._t); this._t = setTimeout(function () { self.setState({ toast: null }); }, 2600); }

  pickFile(name, size, kind) {
    var self = this;
    this.setState({ drop: 'scanning', file: null });
    setTimeout(function () { self.setState({ drop: 'ready', file: { name: name || 'Report.pdf', size: size || '8.4 MB', kind: kind || 'PDF' } }); }, 900);
  }

  searchTools(q) {
    var all = this.tools();
    if (!q) return all;
    var k = q.toLowerCase().trim();
    var words = k.split(/\s+/).map(function (w) { return SYN[w] || w; });
    return all.filter(function (t) {
      var hay = (t.name + ' ' + t.purpose + ' ' + t.fam + ' ' + t.task + ' ' + t.inp + ' ' + t.out + ' ' + t.slug).toLowerCase();
      return words.every(function (w) { return hay.indexOf(w) >= 0; });
    });
  }

  renderVals() {
    var self = this;
    var st = this.state;
    LIVE = st;
    this.syncProps();
    var vw = st.forceW || st.w;
    var isMobile = vw < 768;
    var isTablet = vw >= 768 && vw < 1280;
    var tools = this.tools();

    var navDef = [
      ['home', 'Home', 'home'], ['tools', 'All Tools', 'apps'], ['pipeline', 'Pipeline', 'account_tree'],
      ['batch', 'Batch', 'grid_view'], ['mystuff', 'My Stuff', 'folder_special'], ['compare', 'Compare', 'compare_arrows'],
      ['blog', 'Blog', 'article'], ['privacy', 'Trust', 'verified_user'], ['support', 'Support', 'support_agent'], ['status', 'Status', 'monitor_heart']
    ];
    var TRUSTROUTES = ['privacy', 'security', 'terms', 'acceptable-use', 'processors', 'accessibility'];
    var activeNav = st.route === 'tool' ? 'tools' : (TRUSTROUTES.indexOf(st.route) >= 0 ? 'privacy' : st.route);
    var navMain = navDef.map(function (n) {
      var on = activeNav === n[0];
      return {
        label: n[1], icon: n[2], cur: on ? 'page' : 'false', fw: on ? '500' : '400',
        fg: on ? 'var(--ink)' : 'var(--ink2)', bg: on ? 'var(--emSoft)' : 'transparent',
        bd: on ? 'var(--emLine)' : 'transparent', ic: on ? 'var(--em)' : 'var(--ink3)',
        go: function () { self.go(n[0]); }, goClose: function () { self.go(n[0]); }
      };
    });
    var catDef = [['PDF', 'picture_as_pdf', 'var(--coral)', '107 tools'], ['Images', 'image', 'var(--blue)', '38 tools'], ['Video', 'movie', 'var(--violet)', '24 tools'], ['Audio', 'graphic_eq', 'var(--teal)', '18 tools'], ['Archives', 'folder_zip', 'var(--amber)', '12 tools'], ['Documents & Data', 'table_chart', 'var(--em)', '22 tools'], ['Converters', 'swap_horiz', 'var(--blue)', 'Task family'], ['Editors', 'edit_document', 'var(--violet)', 'Task family'], ['Security', 'shield_lock', 'var(--em)', 'Task family']];
    var navCats = catDef.map(function (c) {
      var on = st.route === 'tools' && st.fam === c[0];
      return { label: c[0], icon: c[1], ic: on ? 'var(--em)' : c[2], fg: on ? 'var(--ink)' : 'var(--ink2)', bg: on ? 'var(--emSoft)' : 'transparent', count: c[3], color: c[2], go: function () { self.setState({ fam: ['Converters', 'Editors', 'Security'].indexOf(c[0]) >= 0 ? 'All' : c[0], task: c[0] === 'Converters' ? 'Convert' : c[0] === 'Editors' ? 'Edit' : c[0] === 'Security' ? 'Security' : 'All' }); self.go('tools'); }, goClose: function () { self.go('tools'); } };
    });
    var cats = catDef.slice(0, 6).concat([catDef[6], catDef[7], catDef[8]]).map(function (c) {
      return { label: c[0], icon: c[1], color: c[2], count: c[3], go: function () { self.setState({ fam: ['Converters', 'Editors', 'Security'].indexOf(c[0]) >= 0 ? 'All' : c[0] }); self.go('tools'); } };
    });

    var health = [
      { label: 'Local', state: 'Ready', icon: 'my_location', color: 'var(--em)', title: 'Local processing ready', pulse: 'none', bg: 'transparent', go: function () { self.go('status'); } },
      { label: 'Server', state: this.props.serverState === 'unavailable' ? 'Temporary unavailable' : 'Best effort · Mumbai, IN', icon: 'dns', color: this.props.serverState === 'unavailable' ? 'var(--coral)' : 'var(--amber)', title: 'Server fallback status', pulse: 'onepulse 2.4s ease-out 1', bg: 'transparent', go: function () { self.go('status'); } },
      { label: 'Offline', state: 'PWA ready', icon: 'wifi', color: 'var(--teal)', title: 'Offline readiness', pulse: 'none', bg: 'transparent', go: function () { self.go('status'); } }
    ];

    var trust = [
      { icon: 'toll', label: 'Free', sub: 'No account, ever', color: 'var(--em)' },
      { icon: 'developer_board', label: 'Local first', sub: 'Runs on your device', color: 'var(--em)' },
      { icon: 'database', label: '500 MB', sub: 'Per file, every tool', color: 'var(--teal)' },
      { icon: 'delete_forever', label: 'No document retention', sub: 'Not retained, deleted after use', color: 'var(--teal)' },
      { icon: 'dns', label: 'Server fallback', sub: 'Disclosed, Mumbai IN', color: 'var(--amber)' },
      { icon: 'bar_chart_off', label: 'No analytics', sub: 'No tracking, no telemetry', color: 'var(--blue)' },
      { icon: 'install_mobile', label: 'PWA', sub: 'Installable, works offline', color: 'var(--violet)' }
    ];

    var srcDef = [['Device', 'Browse files', 'devices', 'var(--ink2)'], ['Folder', 'Choose folder', 'folder', 'var(--amber)'], ['Clipboard', 'Paste content', 'content_paste', 'var(--ink2)'], ['Camera', 'Take a photo', 'photo_camera', 'var(--ink2)'], ['Public URL', 'From the web', 'link', 'var(--blue)'], ['Google Drive', 'My Drive', 'add_to_drive', 'var(--em)'], ['Dropbox', 'My files', 'inventory_2', 'var(--blue)'], ['OneDrive', 'My files', 'cloud', 'var(--blue)'], ['Box', 'My files', 'inbox', 'var(--violet)']];
    var sources = srcDef.map(function (s) { return { label: s[0], sub: s[1], icon: s[2], color: s[3], go: function () { self.pickFile('Report.pdf', '8.4 MB', 'PDF'); } }; });

    var popular = tools.filter(function (t) { return t.popular; }).slice(0, 6).map(function (t) {
      return { name: t.name, purpose: t.purpose, icon: t.icon, color: t.color, tint: t.tint, modeLabel: self.modeShort(t.mode), modeColor: self.modeColor(t.mode), go: function () { self.go('tool', t.slug); } };
    });

    var sugg = ['compress-pdf', 'pdf-to-jpg', 'merge-pdf', 'protect-pdf', 'ocr-pdf', 'redact-pdf', 'split-pdf', 'extract-pages'].map(function (s) {
      var t = self.byslug(s);
      return { name: t.name, purpose: t.purpose, icon: t.icon, color: t.color, go: function () { self.go('tool', t.slug); } };
    });

    var recentRows = [
      ['compress-pdf', '20 Aug 2026', 'Medium · Remove metadata on'],
      ['heic-to-jpg', '18 Aug 2026', 'Quality 90 · Keep dates off'],
      ['merge-pdf', '15 Aug 2026', '3 inputs · Bookmarks kept'],
      ['zip-extractor', '11 Aug 2026', 'Flatten folders off']
    ].map(function (r) {
      var t = self.byslug(r[0]);
      return { name: t.name, icon: t.icon, color: t.color, date: r[1], settings: r[2], go: function () { self.go('tool', t.slug); } };
    });

    var pRes = [];
    var q = st.pq;
    var cmds = [
      { name: 'Make a PDF smaller', sub: 'Compress PDF', kind: 'Task', icon: 'compress', color: 'var(--em)', slug: 'compress-pdf' },
      { name: 'Remove a password I own', sub: 'Unlock PDF', kind: 'Task', icon: 'lock_open', color: 'var(--em)', slug: 'unlock-pdf' },
      { name: 'Turn photos into one PDF', sub: 'JPG to PDF', kind: 'Task', icon: 'picture_as_pdf', color: 'var(--em)', slug: 'jpg-to-pdf' },
      { name: 'Make a scan searchable', sub: 'OCR PDF', kind: 'Task', icon: 'document_scanner', color: 'var(--em)', slug: 'ocr-pdf' },
      { name: 'Strip photo location data', sub: 'Remove EXIF data', kind: 'Task', icon: 'location_off', color: 'var(--em)', slug: 'remove-exif' }
    ];
    var navCmds = [
      { name: 'Open Pipeline', sub: 'Chain tools in order', kind: 'Go', icon: 'account_tree', color: 'var(--blue)', route: 'pipeline' },
      { name: 'Open Batch', sub: 'One tool, many files', kind: 'Go', icon: 'grid_view', color: 'var(--blue)', route: 'batch' },
      { name: 'Open My Stuff', sub: 'Local activity and assets', kind: 'Go', icon: 'folder_special', color: 'var(--blue)', route: 'mystuff' },
      { name: 'Open Status', sub: 'Service health', kind: 'Go', icon: 'monitor_heart', color: 'var(--blue)', route: 'status' }
    ];
    var kq = (q || '').toLowerCase();
    var matchedTasks = cmds.filter(function (c) { return !kq || (c.name + ' ' + c.sub).toLowerCase().indexOf(kq) >= 0; });
    var matchedNav = navCmds.filter(function (c) { return !kq || c.name.toLowerCase().indexOf(kq) >= 0; });
    var matchedTools = this.searchTools(q).slice(0, 8).map(function (t) { return { name: t.name, sub: t.purpose + ' · ' + self.modeShort(t.mode), kind: t.fam, icon: t.icon, color: t.color, slug: t.slug }; });
    var merged = matchedTasks.slice(0, 3).concat(matchedTools).concat(matchedNav.slice(0, 2));
    var pIdx = Math.min(st.pIdx, Math.max(0, merged.length - 1));
    pRes = merged.map(function (r, i) {
      return {
        name: r.name, sub: r.sub, kind: r.kind, icon: r.icon, color: r.color,
        sel: i === pIdx ? 'true' : 'false', bg: i === pIdx ? 'var(--emSoft)' : 'transparent',
        delay: (140 + i * 26) + 'ms',
        hover: function () { self.setState({ pIdx: i }); },
        go: function () { if (r.slug) self.go('tool', r.slug); else self.go(r.route); }
      };
    });

    var dz = st.drop;
    var t0 = st.file;

    var V = {
      isMobile: isMobile, notMobile: !isMobile, isTablet: isTablet,
      showRail: !isMobile && !isTablet, showIconRail: isTablet, isHome: st.route === 'home',
      live: st.live, failedUrlPath: st.failedPath,
      railExpand: function () { self.setState({ drawer: true }); },
      searchInBar: !(isMobile && st.route === 'tool'), crumbShown: st.route === 'tool',
      crumbParent: 'All Tools', crumbCurrent: (this.byslug(st.slug) || { name: 'Tool' }).name,
      navMain: navMain, navCats: navCats, cats: cats, health: health, trust: trust, sources: sources,
      popular: popular, suggested: sugg, recentRows: recentRows, returning: st.returning,
      navMobile: [['home', 'Home', 'home'], ['tools', 'Tools', 'apps'], ['search', 'Search', 'search'], ['mystuff', 'My Stuff', 'folder_special'], ['more', 'More', 'menu']].map(function (m) {
        var on = activeNav === m[0];
        return { label: m[1], icon: m[2], cur: on ? 'page' : 'false', fg: on ? 'var(--em)' : 'var(--ink3)', bg: on ? 'var(--emSoft)' : 'transparent', go: function () { if (m[0] === 'search') self.setState({ palette: true, pq: '', pIdx: 0 }); else if (m[0] === 'more') self.setState({ drawer: true }); else self.go(m[0]); } };
      }),
      themes: [['system', 'System', 'contrast'], ['dark', 'Dark', 'dark_mode'], ['light', 'Light', 'light_mode']].map(function (t) {
        var on = st.theme === t[0];
        return { title: t[1], icon: t[2], on: on ? 'true' : 'false', bg: on ? 'var(--emSoft)' : 'transparent', fg: on ? 'var(--em)' : 'var(--ink3)', go: function () { self.applyTheme(t[0]); } };
      }),
      drawer: st.drawer, palette: st.palette, pq: st.pq, pResults: pRes, pEmpty: merged.length === 0,
      toast: st.toast,
      dzTitle: dz === 'scanning' ? 'Inspecting on your device…' : t0 ? t0.name + ' · ' + t0.size : 'Drop a file or choose a tool',
      dzSub: dz === 'scanning' ? 'Reading file type and properties locally' : t0 ? 'Choose an action below. Nothing runs automatically.' : 'Your file stays on your device. Nothing is uploaded.',
      dzBorder: dz === 'hover' ? 'var(--em)' : dz === 'scanning' ? 'var(--emLine)' : 'var(--line2)',
      dzBg: dz === 'hover' || dz === 'scanning' ? 'var(--emSoft)' : 'transparent',
      dzScanning: dz === 'scanning',
      hasFile: !!t0,
      inspect: t0 ? [
        { label: 'File type and size', value: t0.kind + ' · ' + t0.size, icon: 'check_circle', color: 'var(--em)', fg: 'var(--ink2)' },
        { label: 'Basic properties', value: '12 pages · no forms', icon: 'check_circle', color: 'var(--em)', fg: 'var(--ink2)' },
        { label: 'Compatibility check', value: 'Under 500 MB', icon: 'check_circle', color: 'var(--em)', fg: 'var(--ink2)' }
      ] : [],
      onDragOver: function (e) { e.preventDefault(); if (st.drop !== 'hover') self.setState({ drop: 'hover' }); },
      onDragLeave: function () { self.setState({ drop: st.file ? 'ready' : 'idle' }); },
      onDrop: function (e) { e.preventDefault(); self.pickFile('Report.pdf', '8.4 MB', 'PDF'); },
      firstVisit: function () { self.setState({ returning: false }); },
      openDrawer: function () { self.setState({ drawer: true }); },
      closeDrawer: function () { self.setState({ drawer: false }); },
      openPalette: function () { self.setState({ palette: true, pq: '', pIdx: 0 }); },
      closePalette: function () { self.setState({ palette: false }); },
      onPq: function (e) { self.setState({ pq: e.target.value, pIdx: 0 }); },
      onPaletteKey: function (e) {
        if (e.key === 'ArrowDown') { e.preventDefault(); self.setState({ pIdx: Math.min(pIdx + 1, merged.length - 1) }); }
        else if (e.key === 'ArrowUp') { e.preventDefault(); self.setState({ pIdx: Math.max(pIdx - 1, 0) }); }
        else if (e.key === 'Enter') { var r = merged[pIdx]; if (r) { if (r.slug) self.go('tool', r.slug); else self.go(r.route); } }
      },
      openInstall: function () { self.toast('Install prompt shown — this demo does not install.'); },
      back: function () { self.go('tools'); },
      goHome: function () { self.go('home'); }, goTools: function () { self.go('tools'); },
      goPipeline: function () { self.go('pipeline'); }, goBatch: function () { self.go('batch'); }, goStatus: function () { self.go('status'); }
    };
    return Object.assign(V, this.vTools(isMobile), this.vTool(isMobile), this.vFlow(isMobile), this.vData(isMobile), this.vTrust(isMobile), this.vShow());
  }

  vTools(isMobile) {
    var self = this, st = this.state;
    var list = this.searchTools(st.q).filter(function (t) {
      if (st.fam !== 'All' && t.fam !== st.fam) return false;
      if (st.task !== 'All' && t.task !== st.task) return false;
      if (st.inp !== 'All' && t.inp !== st.inp) return false;
      if (st.out !== 'All' && t.out !== st.out) return false;
      if (st.pmode !== 'All' && t.mode !== st.pmode) return false;
      if (st.onlyFav && st.favs.indexOf(t.slug) < 0) return false;
      return true;
    });
    var prev = this.byslug(st.preview || 'compress-pdf');
    var row = function (t) {
      var fav = st.favs.indexOf(t.slug) >= 0;
      return {
        slug: t.slug, name: t.name, purpose: t.purpose, icon: t.icon, color: t.color, tint: t.tint,
        fam: t.fam, io: t.inp + ' → ' + t.out, modeLabel: self.modeShort(t.mode), modeColor: self.modeColor(t.mode),
        dotLocal: t.mode === 'unverified' ? 'var(--line2)' : t.mode === 'server' ? 'var(--line2)' : 'var(--em)',
        dotServer: t.mode === 'unverified' ? 'var(--line2)' : t.mode === 'local' ? 'var(--line2)' : 'var(--amber)',
        dotOffline: t.offline ? 'var(--blue)' : 'var(--line2)',
        localTitle: t.mode === 'unverified' ? 'Processing mode not verified' : t.mode === 'server' ? 'Local: not available' : 'Local: yes',
        serverTitle: t.mode === 'unverified' ? 'Processing mode not verified' : t.mode === 'local' ? 'Server: not used' : (t.mode === 'server' ? 'Server: required' : 'Server: fallback only'),
        offlineTitle: t.mode === 'unverified' ? 'Processing mode not verified' : t.offline ? 'Offline: works' : 'Offline: needs connection',
        favIcon: fav ? 'star' : 'star_border', favColor: fav ? 'var(--amber)' : 'var(--ink3)',
        favLabel: (fav ? 'Remove ' : 'Add ') + t.name + ' to favourites',
        bg: st.preview === t.slug ? 'var(--emSoft)' : 'transparent',
        go: function () { self.go('tool', t.slug); },
        hover: function () { if (st.preview !== t.slug) self.setState({ preview: t.slug }); },
        toggleFav: function (e) {
          e.stopPropagation();
          var f = st.favs.slice(); var i = f.indexOf(t.slug);
          if (i >= 0) f.splice(i, 1); else f.push(t.slug);
          self.setState({ favs: f });
        }
      };
    };
    var chip = function (label, active, fn) {
      return { label: label, bg: active ? 'var(--emSoft)' : 'var(--panel)', fg: active ? 'var(--em)' : 'var(--ink2)', bd: active ? 'var(--emLine)' : 'var(--line)', go: fn };
    };
    var famChips = [chip('All', st.fam === 'All' && st.task === 'All', function () { self.setState({ fam: 'All', task: 'All' }); })];
    ['PDF', 'Images', 'Video', 'Audio', 'Archives', 'Documents & Data'].forEach(function (f) {
      famChips.push(chip(f, st.fam === f, function () { self.setState({ fam: st.fam === f ? 'All' : f }); }));
    });
    TASKS.forEach(function (t) {
      famChips.push(chip(t, st.task === t, function () { self.setState({ task: st.task === t ? 'All' : t }); }));
    });
    var opts = function (key, label, values) {
      return {
        label: label, value: st[key],
        options: ['All'].concat(values).map(function (v) { return { v: v, label: v === 'All' ? 'All ' + label.toLowerCase() : v }; }),
        onChange: function (e) { var s = {}; s[key] = e.target.value; self.setState(s); }
      };
    };
    var ins = []; var outs = [];
    this.tools().forEach(function (t) { if (ins.indexOf(t.inp) < 0) ins.push(t.inp); if (outs.indexOf(t.out) < 0) outs.push(t.out); });
    ins.sort(); outs.sort();
    var quick = ['compress pdf', 'convert pdf to jpg', 'merge pdf', 'heic to jpg', 'zip extractor', 'compress images'];
    return {
      isTools: st.route === 'tools',
      toolCount: list.length,
      catalogueNote: 'Showing ' + list.length + ' of ' + this.tools().length + ' records from the PrivaTools registry. Processing modes in this dataset are unverified. The real catalogue declares 221 entries (107 PDF, 114 non-PDF) and has not been supplied.',
      q: st.q, onQ: function (e) { self.setState({ q: e.target.value, preview: null }); },
      clearQ: function () { self.setState({ q: '' }); },
      hasQ: !!st.q, noMatch: list.length === 0,
      rows: list.slice(0, 60).map(row),
      isList: st.view === 'list', isCards: st.view === 'cards',
      setList: function () { self.setState({ view: 'list' }); }, setCards: function () { self.setState({ view: 'cards' }); },
      listBg: st.view === 'list' ? 'var(--emSoft)' : 'transparent', listFg: st.view === 'list' ? 'var(--em)' : 'var(--ink3)',
      cardsBg: st.view === 'cards' ? 'var(--emSoft)' : 'transparent', cardsFg: st.view === 'cards' ? 'var(--em)' : 'var(--ink3)',
      famChips: famChips,
      showTable: st.view === 'list' && list.length > 0, showCards: st.view === 'cards' && list.length > 0,
      selectFilters: [opts('inp', 'Input types', ins), opts('out', 'Output types', outs), { label: 'Processing', value: st.pmode, options: [{ v: 'All', label: 'All processing' }, { v: 'unverified', label: 'Mode unverified' }, { v: 'local', label: 'Local only' }, { v: 'fallback', label: 'Local + server fallback' }, { v: 'server', label: 'Server required' }], onChange: function (e) { self.setState({ pmode: e.target.value }); } }],
      fInput: opts('inp', 'Input types', ins), fOutput: opts('out', 'Output types', outs),
      fMode: { label: 'Processing', value: st.pmode, options: [{ v: 'All', label: 'All processing' }, { v: 'unverified', label: 'Mode unverified' }, { v: 'local', label: 'Local only' }, { v: 'fallback', label: 'Local + server fallback' }, { v: 'server', label: 'Server required' }], onChange: function (e) { self.setState({ pmode: e.target.value }); } },
      favOn: st.onlyFav, favToggleBg: st.onlyFav ? 'var(--em)' : 'var(--line2)', favKnob: st.onlyFav ? '18px' : '2px',
      toggleFavFilter: function () { self.setState({ onlyFav: !st.onlyFav }); },
      clearFilters: function () { self.setState({ fam: 'All', task: 'All', inp: 'All', out: 'All', pmode: 'All', onlyFav: false, q: '' }); },
      filtersActive: st.fam !== 'All' || st.task !== 'All' || st.inp !== 'All' || st.out !== 'All' || st.pmode !== 'All' || st.onlyFav,
      quickSearches: quick.map(function (k) { return { label: k, go: function () { self.setState({ q: k }); } }; }),
      favRows: st.favs.map(function (s) { return row(self.byslug(s)); }),
      hasFavs: st.favs.length > 0,
      recentTools: st.recents.map(function (s) { return row(self.byslug(s)); }),
      pv: { name: prev.name, purpose: prev.purpose, icon: prev.icon, color: prev.color, tint: prev.tint, io: prev.inp + ' → ' + prev.out, mode: self.modeLabel(prev.mode), modeColor: self.modeColor(prev.mode), go: function () { self.go('tool', prev.slug); } },
      openFilters: function () { self.setState({ sheet: 'filters' }); },
      closeSheet: function () { self.setState({ sheet: null }); },
      filterSheet: st.sheet === 'filters'
    };
  }

  optionsFor(slug) {
    var S = {
      'compress-pdf': [
        { t: 'radio', k: 'level', label: 'Compression level', help: 'Balance quality and size', choices: [['low', 'Low', 'Smaller change'], ['medium', 'Medium', 'Recommended'], ['high', 'High', 'Smallest file'], ['custom', 'Custom', 'Set values below']] },
        { t: 'slider', k: 'quality', label: 'Image quality', min: 30, max: 100, step: 5, unit: '%', help: 'Applies to images inside the PDF' },
        { t: 'number', k: 'dpi', label: 'Downsample images above', min: 72, max: 600, step: 12, unit: 'dpi' },
        { t: 'select', k: 'optimise', label: 'Optimise for', choices: [['size', 'Smallest size'], ['quality', 'Best quality'], ['web', 'Web viewing'], ['print', 'Print'], ['archive', 'Archive']] },
        { t: 'toggle', k: 'meta', label: 'Remove metadata', help: 'Strips author, producer and history' }
      ],
      'protect-pdf': [
        { t: 'password', k: 'pw', label: 'Open password', help: 'Needed to open the document' },
        { t: 'password', k: 'pw2', label: 'Confirm password' },
        { t: 'select', k: 'enc', label: 'Encryption', choices: [['aes256', 'AES-256'], ['aes128', 'AES-128']] },
        { t: 'multi', k: 'perm', label: 'Allow', choices: [['print', 'Printing'], ['copy', 'Copying text'], ['annot', 'Annotations'], ['forms', 'Form filling'], ['assemble', 'Page assembly']] }
      ],
      'watermark-pdf': [
        { t: 'text', k: 'wtext', label: 'Watermark text', placeholder: 'Confidential' },
        { t: 'color', k: 'wcolor', label: 'Colour', choices: ['#20D497', '#5A96F7', '#F2685C', '#8A9A98'] },
        { t: 'slider', k: 'opacity', label: 'Opacity', min: 5, max: 100, step: 5, unit: '%' },
        { t: 'select', k: 'pos', label: 'Position', choices: [['center', 'Centre'], ['diagonal', 'Diagonal'], ['header', 'Header'], ['footer', 'Footer']] },
        { t: 'pages', k: 'pages', label: 'Pages', help: 'e.g. 1-3, 7, 10-12' }
      ],
      'organise-pages': [
        { t: 'reorder', k: 'order', label: 'Page order', help: 'Drag a page, or use the arrow buttons' },
        { t: 'toggle', k: 'bookmarks', label: 'Keep bookmarks' }
      ],
      'redact-pdf': [
        { t: 'select', k: 'mode', label: 'Mode', choices: [['text', 'Redact text'], ['both', 'Redact text & images'], ['area', 'Redact drawn areas']] },
        { t: 'crop', k: 'region', label: 'Redaction area', help: 'Drawn on page 1. Values in millimetres.' },
        { t: 'pages', k: 'pages', label: 'Scope', help: 'All pages, or a range like 2-6' },
        { t: 'toggle', k: 'flatten', label: 'Flatten after redaction', help: 'Removes the original content permanently' }
      ],
      'extract-pages': [
        { t: 'pages', k: 'pages', label: 'Pages to extract', help: 'e.g. 1-3, 7, 10-12' },
        { t: 'radio', k: 'split', label: 'Output', choices: [['one', 'One PDF', 'All chosen pages together'], ['each', 'One PDF per page', 'Delivered as a ZIP']] }
      ],
      'resize-image': [
        { t: 'number', k: 'w', label: 'Width', min: 16, max: 12000, step: 10, unit: 'px' },
        { t: 'number', k: 'h', label: 'Height', min: 16, max: 12000, step: 10, unit: 'px' },
        { t: 'radio', k: 'fit', label: 'Fit', choices: [['contain', 'Contain', 'Fit inside'], ['cover', 'Cover', 'Fill and crop'], ['exact', 'Exact', 'Ignore ratio']] },
        { t: 'toggle', k: 'ratio', label: 'Keep aspect ratio' }
      ],
      'bates-numbering': [
        { t: 'text', k: 'prefix', label: 'Prefix', placeholder: 'ABC-' },
        { t: 'number', k: 'start', label: 'Start at', min: 1, max: 999999, step: 1, unit: '' },
        { t: 'number', k: 'pad', label: 'Digits', min: 3, max: 10, step: 1, unit: '' },
        { t: 'select', k: 'pos', label: 'Position', choices: [['br', 'Bottom right'], ['bl', 'Bottom left'], ['tr', 'Top right'], ['tl', 'Top left']] },
        { t: 'toggle', k: 'counter', label: 'Save counter to My Stuff', help: 'Stores the next number only — never filenames' }
      ]
    };
    return S[slug] || [
      { t: 'radio', k: 'level', label: 'Quality', choices: [['low', 'Faster', 'Lower quality'], ['medium', 'Balanced', 'Recommended'], ['high', 'Best', 'Slower']] },
      { t: 'select', k: 'optimise', label: 'Output', choices: [['default', 'Default settings'], ['size', 'Smallest size'], ['quality', 'Best quality']] },
      { t: 'toggle', k: 'meta', label: 'Remove metadata', help: 'Strips embedded author and device data' }
    ];
  }
  optVal(k, d) { var v = this.state.opts[k]; return v === undefined ? d : v; }
  setOpt(k, v) { var o = Object.assign({}, this.state.opts); o[k] = v; this.setState({ opts: o }); }

  runTool(server) {
    var self = this;
    clearInterval(this._iv);
    var localStages = ['Reading file on your device', 'Rebuilding page content', 'Optimising embedded images', 'Writing output file'];
    var serverStages = ['Sending to server · Mumbai, India', 'Processing in isolated request', 'Returning result', 'Deleting server copy'];
    var stages = server ? serverStages : localStages;
    this.setState({ stage: 'queued', prog: 0, stageText: 'Waiting to start', serverRun: !!server, err: null });
    setTimeout(function () {
      if (self.state.stage !== 'queued') return;
      self.setState({ stage: 'processing', prog: 2, stageText: stages[0] });
      self._iv = setInterval(function () {
        var p = self.state.prog + 3 + Math.round(Math.random() * 4);
        if (p >= 100) {
          clearInterval(self._iv);
          self.setState({ prog: 100, stage: 'result', stageText: 'Complete' });
          return;
        }
        var si = Math.min(stages.length - 1, Math.floor(p / 26));
        self.setState({ prog: p, stageText: stages[si] });
      }, 260);
    }, 900);
  }

  vTool(isMobile) {
    var self = this, st = this.state;
    var t = this.byslug(st.slug) || this.tools()[0];
    var stage = st.stage || 'input';
    var order = ['input', 'configure', 'queued', 'processing', 'result'];
    var idx = order.indexOf(stage); if (idx < 0) idx = st.file ? 1 : 0;
    var steps = [['input', 'Add files', 'From any source'], ['configure', 'Configure', 'Set options'], ['queued', 'Queued', 'Waiting to start'], ['processing', t.mode === 'server' ? 'Processing on server' : 'Processing locally', t.mode === 'server' ? 'Mumbai, India' : 'Your device'], ['result', 'Ready to download', 'Result is private']].map(function (s, i) {
      var state = i < idx ? 'done' : i === idx ? 'current' : 'todo';
      return {
        label: s[1], sub: s[2], n: String(i + 1),
        color: state === 'todo' ? 'var(--ink3)' : 'var(--em)',
        fg: state === 'current' ? 'var(--ink)' : state === 'done' ? 'var(--ink2)' : 'var(--ink3)',
        ring: state === 'todo' ? 'var(--line2)' : 'var(--emLine)',
        bg: state === 'current' ? 'var(--emSoft)' : 'transparent',
        fw: state === 'current' ? '500' : '400',
        icon: state === 'done' ? 'check' : ''
      };
    });

    var groups = this.optionsFor(t.slug).map(function (o) {
      var val = self.optVal(o.k, o.t === 'toggle' ? (o.k === 'meta' || o.k === 'ratio' || o.k === 'bookmarks') : o.t === 'radio' ? (o.choices[1] ? o.choices[1][0] : o.choices[0][0]) : o.t === 'select' ? o.choices[0][0] : o.t === 'slider' ? Math.round((o.min + o.max) / 2) : o.t === 'number' ? (o.k === 'w' ? 1600 : o.k === 'h' ? 900 : o.k === 'start' ? 1 : o.k === 'pad' ? 6 : 150) : o.t === 'multi' ? ['print'] : o.t === 'pages' ? '' : o.t === 'color' ? o.choices[0] : '');
      var g = {
        key: o.k, label: o.label, help: o.help || '', hasHelp: !!o.help,
        isRadio: o.t === 'radio', isSelect: o.t === 'select', isToggle: o.t === 'toggle', isSlider: o.t === 'slider',
        isNumber: o.t === 'number', isText: o.t === 'text', isPassword: o.t === 'password', isMulti: o.t === 'multi',
        isPages: o.t === 'pages', isColor: o.t === 'color', isReorder: o.t === 'reorder', isCrop: o.t === 'crop',
        value: val, placeholder: o.placeholder || '', unit: o.unit || '', min: o.min, max: o.max, step: o.step,
        onChange: function (e) { self.setOpt(o.k, e.target.value); },
        onNum: function (e) { self.setOpt(o.k, e.target.value); },
        onToggle: function () { self.setOpt(o.k, !val); },
        toggleBg: val ? 'var(--em)' : 'var(--line2)', knob: val ? '18px' : '2px',
        ariaChecked: val ? 'true' : 'false',
        display: o.t === 'slider' ? val + (o.unit || '') : String(val)
      };
      if (o.t === 'radio') g.choices = o.choices.map(function (c) {
        var on = val === c[0];
        return { v: c[0], label: c[1], sub: c[2] || '', bd: on ? 'var(--em)' : 'var(--line)', bg: on ? 'var(--emSoft)' : 'var(--panel2)', dot: on ? 'var(--em)' : 'transparent', ring: on ? 'var(--em)' : 'var(--line2)', go: function () { self.setOpt(o.k, c[0]); }, checked: on ? 'true' : 'false' };
      });
      if (o.t === 'select') g.choices = o.choices.map(function (c) { return { v: c[0], label: c[1] }; });
      if (o.t === 'multi') g.choices = o.choices.map(function (c) {
        var on = (val || []).indexOf(c[0]) >= 0;
        return { v: c[0], label: c[1], bg: on ? 'var(--emSoft)' : 'var(--panel2)', bd: on ? 'var(--emLine)' : 'var(--line)', fg: on ? 'var(--em)' : 'var(--ink2)', icon: on ? 'check' : 'add', pressed: on ? 'true' : 'false', go: function () { var a = (val || []).slice(); var i = a.indexOf(c[0]); if (i >= 0) a.splice(i, 1); else a.push(c[0]); self.setOpt(o.k, a); } };
      });
      if (o.t === 'color') g.choices = o.choices.map(function (c) { return { v: c, on: val === c ? '2px solid var(--ink)' : '1px solid var(--line)', go: function () { self.setOpt(o.k, c); } }; });
      if (o.t === 'reorder') {
        var pages = self.optVal('order', [1, 2, 3, 4, 5, 6]);
        g.pages = pages.map(function (p, i) {
          return {
            n: 'Page ' + p, i: i,
            up: function () { var a = pages.slice(); if (i > 0) { var x = a[i - 1]; a[i - 1] = a[i]; a[i] = x; self.setOpt('order', a); } },
            down: function () { var a = pages.slice(); if (i < a.length - 1) { var x = a[i + 1]; a[i + 1] = a[i]; a[i] = x; self.setOpt('order', a); } },
            del: function () { var a = pages.slice(); a.splice(i, 1); self.setOpt('order', a); },
            upLabel: 'Move page ' + p + ' earlier', downLabel: 'Move page ' + p + ' later', delLabel: 'Remove page ' + p
          };
        });
      }
      return g;
    });

    var alerts = {
      'wrong-type': { icon: 'block', color: 'var(--coral)', bg: 'var(--coralSoft)', title: 'Unsupported file', body: 'This file type is not supported by ' + t.name + '. Accepted input: ' + t.inp + '.', action: 'Choose another file' },
      'too-large': { icon: 'scale', color: 'var(--coral)', bg: 'var(--coralSoft)', title: 'File exceeds 500 MB', body: 'Every tool supports files up to 500 MB. Please choose a file of 500 MB or smaller.', action: 'Choose another file' },
      'password': { icon: 'password', color: 'var(--amber)', bg: 'var(--amberSoft)', title: 'Password needed', body: 'This PDF is protected. Enter the open password to continue. It is used on your device only.', action: 'Unlock on this device' }
    };
    var al = alerts[st.alert] || null;

    var related = this.tools().filter(function (x) { return x.fam === t.fam && x.slug !== t.slug; }).slice(0, 6).map(function (x) {
      return { name: x.name, purpose: x.purpose, icon: x.icon, color: x.color, go: function () { self.go('tool', x.slug); } };
    });

    var stateOpts = [['input', 'Empty input'], ['wrong-type', 'Wrong file type'], ['too-large', 'File over 500 MB'], ['password', 'Password needed'], ['configure', 'Configuring'], ['queued', 'Queued'], ['processing', 'Processing (local)'], ['processing-server', 'Processing (server)'], ['cancelled', 'Cancelled'], ['result', 'Result'], ['result-zip', 'Result (ZIP)'], ['error', 'Error'], ['server-down', 'Server unavailable']];

    return {
      isTool: st.route === 'tool',
      tName: t.name, tPurpose: t.purpose, tFam: t.fam, tIcon: t.icon, tColor: t.color, tTint: t.tint,
      tMode: this.modeLabel(t.mode), tModeColor: this.modeColor(t.mode), tIO: t.inp + ' → ' + t.out,
      tServerNote: t.mode === 'unverified' ? 'Processing mode for this tool is not verified in this prototype. Nothing is read, processed or sent anywhere — the run below is a labelled simulation.' : t.mode === 'local' ? 'Runs entirely on your device. Nothing is uploaded.' : t.mode === 'fallback' ? 'Runs on your device when possible. If your device cannot finish it, processing falls back to our server in Mumbai, India — always disclosed. Documents are not retained and the temporary processing copy is deleted immediately after the response.' : 'This tool needs our server in Mumbai, India. Your file is processed in an isolated request; documents are not retained and the temporary processing copy is deleted immediately after the response.',
      steps: steps,
      stageInput: stage === 'input', stageConfigure: stage === 'configure', stageQueued: stage === 'queued',
      stageProcessing: stage === 'processing', stageResult: stage === 'result', stageError: stage === 'error',
      stageServerDown: stage === 'server-down', stageCancelled: stage === 'cancelled',
      hasAlert: !!al, alert: al || { icon: '', color: '', bg: '', title: '', body: '', action: '' },
      clearAlert: function () { self.setState({ alert: null, stage: 'input' }); },
      fileName: st.file ? st.file.name : 'Report.pdf', fileSize: st.file ? st.file.size : '8.4 MB',
      optGroups: groups,
      prog: st.prog || 0, progW: (st.prog || 0) + '%', stageText: st.stageText || '',
      serverRun: st.serverRun, processingNote: t.mode === 'unverified' ? 'Simulated run. No file is read, processed or sent anywhere, and no processing location is claimed.' : st.serverRun ? 'Processed in an isolated request in Mumbai, India. Input and output documents are not retained. The temporary processing copy is deleted immediately after the response.' : 'Your file never leaves your device.',
      resultName: (t.out === 'ZIP' || st.zip) ? 'privatools-results.zip' : 'Report-compressed.' + (t.out === 'FILES' ? 'zip' : t.out.toLowerCase()),
      resultSize: st.zip ? '12.6 MB · 6 files' : '3.1 MB',
      resultSaving: st.zip ? 'Delivered as one ZIP' : '63% smaller than the input',
      chooseFile: function () { self.setState({ file: { name: 'Report.pdf', size: '8.4 MB', kind: t.inp }, stage: 'configure', alert: null, opts: {} }); },
      runLocal: function () { self.runTool(false); },
      runServer: function () { self.runTool(true); },
      canRunServer: t.mode === 'fallback' || t.mode === 'server',
      runLabel: t.mode === 'unverified' ? 'Run simulation' : t.mode === 'server' ? 'Process on server' : 'Run on this device',
      modeUnverified: t.mode === 'unverified',
      modeNote: t.mode === 'unverified' ? 'Processing mode unverified — simulation only' : this.modeLabel(t.mode),
      cancel: function () { clearInterval(self._iv); self.setState({ stage: 'cancelled', prog: 0 }); },
      resume: function () { self.setState({ stage: 'configure' }); },
      startOver: function () { clearInterval(self._iv); self.setState({ stage: 'input', file: null, prog: 0, opts: {}, zip: false, alert: null }); },
      download: function () { self.toast('Download started. The result is not stored anywhere.'); },
      sendOn: function () { self.setState({ q: '', preview: null }); self.go('tools'); },
      addToPipeline: function () { self.go('pipeline'); },
      retry: function () { self.runTool(st.serverRun); },
      tryLocal: function () { self.runTool(false); },
      setStateDemo: function (e) {
        var v = e.target.value;
        var s = { alert: null, zip: false, prog: 0, serverRun: false };
        clearInterval(self._iv);
        if (v === 'wrong-type' || v === 'too-large' || v === 'password') { s.stage = 'input'; s.alert = v; }
        else if (v === 'processing') { s.stage = 'processing'; s.prog = 65; s.stageText = 'Optimising embedded images'; s.file = { name: 'Report.pdf', size: '8.4 MB' }; }
        else if (v === 'processing-server') { s.stage = 'processing'; s.prog = 45; s.serverRun = true; s.stageText = 'Processing in isolated request'; s.file = { name: 'Report.pdf', size: '8.4 MB' }; }
        else if (v === 'result-zip') { s.stage = 'result'; s.zip = true; s.prog = 100; s.file = { name: '6 files', size: '31.2 MB' }; }
        else if (v === 'configure' || v === 'queued' || v === 'result') { s.stage = v; s.file = { name: 'Report.pdf', size: '8.4 MB' }; if (v === 'result') s.prog = 100; }
        else s.stage = v;
        self.setState(s);
      },
      stateOpts: stateOpts.map(function (o) { return { v: o[0], label: o[1] }; }),
      stateValue: st.alert ? st.alert : (stage === 'processing' && st.serverRun ? 'processing-server' : (stage === 'result' && st.zip ? 'result-zip' : stage)),
      about: [
        t.purpose + '. Part of the ' + t.fam + ' family.',
        'Works on files up to 500 MB.',
        t.mode === 'unverified' ? 'The processing mode for this record is not verified, so no location is claimed.' : t.mode === 'server' ? 'This tool requires our server in Mumbai, India.' : 'Runs on your device whenever possible.',
        t.mode === 'unverified' ? 'Nothing runs for real in this prototype: the lifecycle below is a labelled simulation.' : t.mode === 'local' ? 'Nothing is uploaded for this tool.' : 'The file is processed in an isolated request in Mumbai, India. Input and output documents are not retained, and the temporary processing copy is deleted immediately after the response.',
        'Result exists only in this tab until you download it.'
      ],
      faq: [
        ['Is it free?', 'Yes. PrivaTools is free forever and owner-funded. There is no account, paid tier, watermark or advertising.'],
        ['Where is my file processed?', t.mode === 'unverified' ? 'This prototype does not know yet. The processing mode for this record has not been verified against a real implementation, so no location is claimed and nothing is processed.' : t.mode === 'local' ? 'Entirely in your browser on this device.' : t.mode === 'fallback' ? 'On your device when possible. If your device cannot finish it, the file goes to our server in Mumbai, India, and is deleted immediately after the response.' : 'On our server in Mumbai, India. The request is isolated and deleted immediately after the response.'],
        ['What is the size limit?', 'Every valid file is supported up to 500 MB per file, in every tool.'],
        ['Do you keep my documents?', 'No. Input and output documents are never retained. Security logs are content-free: 24 hours routine, 7 days suspicious, 30 days for a confirmed incident.'],
        ['Does it work offline?', t.mode === 'unverified' ? 'Unknown until the processing mode is verified. Tools confirmed as local keep working with no connection.' : t.offline ? 'Yes. Install PrivaTools and this tool keeps working with no connection.' : 'This tool needs a connection because it can use our server. Local tools continue to work offline.']
      ].map(function (f, i) {
        return { q: f[0], a: f[1], open: st.faq === i, icon: st.faq === i ? 'remove' : 'add', expanded: st.faq === i ? 'true' : 'false', go: function () { self.setState({ faq: st.faq === i ? null : i }); } };
      }),
      related: related,
      tSources: this.srcList(t)
    };
  }
  runPipeline() {
    var self = this;
    clearInterval(this._pv);
    var steps = this.state.pipe.filter(function (s) { return s.on; });
    if (!this.state.pipeFile) { this.setState({ pipeState: 'no-file' }); return; }
    if (!steps.length) { this.setState({ pipeState: 'all-disabled' }); return; }
    this.setState({ pipeState: 'running', pipeAt: 0, pipeProg: 0, pipeFailed: -1 });
    this._pv = setInterval(function () {
      var st = self.state;
      var p = st.pipeProg + 8;
      if (p >= 100) {
        var next = st.pipeAt + 1;
        var cur = st.pipe[st.pipeAt];
        if (cur && cur.fail) { clearInterval(self._pv); self.setState({ pipeState: 'failed', pipeFailed: st.pipeAt, pipeProg: 62 }); return; }
        if (next >= st.pipe.length) { clearInterval(self._pv); self.setState({ pipeState: 'complete', pipeProg: 100 }); return; }
        self.setState({ pipeAt: next, pipeProg: 0 });
      } else self.setState({ pipeProg: p });
    }, 200);
  }
  runBatch() {
    var self = this;
    clearInterval(this._bv);
    this.setState({ batchState: 'running', batch: this.state.batch.map(function (b) { return Object.assign({}, b, { status: 'queued', prog: 0 }); }) });
    this._bv = setInterval(function () {
      var rows = self.state.batch.map(function (b) {
        if (b.status === 'ok' || b.status === 'fail') return b;
        var p = b.prog + 6 + (b.size.length % 4);
        if (p >= 100) return Object.assign({}, b, { prog: 100, status: 'ok' });
        if (b.willFail && p > 62) return Object.assign({}, b, { prog: 62, status: 'fail' });
        return Object.assign({}, b, { prog: p, status: 'running' });
      });
      var done = rows.every(function (b) { return b.status === 'ok' || b.status === 'fail'; });
      self.setState({ batch: rows });
      if (done) { clearInterval(self._bv); self.setState({ batchState: 'complete' }); }
    }, 220);
  }
  vFlow(isMobile) {
    var self = this, st = this.state;
    var pipe = st.pipe;
    var ps = st.pipeState;
    var nodes = pipe.map(function (s, i) {
      var t = self.byslug(s.slug);
      var failed = ps === 'failed' && st.pipeFailed === i;
      var active = ps === 'running' && st.pipeAt === i;
      var past = (ps === 'running' && i < st.pipeAt) || ps === 'complete' || (ps === 'failed' && i < st.pipeFailed);
      var neutral = ps === 'failed' && i > st.pipeFailed;
      return {
        i: i, name: s.label || t.name, purpose: s.label ? s.sub : t.purpose,
        icon: s.icon || t.icon, color: failed ? 'var(--coral)' : (s.icon ? 'var(--em)' : t.color),
        bd: failed ? 'var(--coral)' : active ? 'var(--em)' : st.pipeSel === i ? 'var(--emLine)' : 'var(--line)',
        bg: failed ? 'var(--coralSoft)' : active ? 'var(--emSoft)' : neutral ? 'transparent' : 'var(--panel2)',
        op: s.on ? '1' : '.45', done: past, doneIcon: past ? 'check_circle' : '',
        modeLabel: self.modeShort(s.mode || t.mode), modeColor: self.modeColor(s.mode || t.mode),
        stateLabel: failed ? 'Step failed' : active ? 'Running' : past ? 'Done' : s.on ? 'Ready' : 'Disabled',
        stateColor: failed ? 'var(--coral)' : active ? 'var(--em)' : past ? 'var(--em)' : s.on ? 'var(--ink3)' : 'var(--ink3)',
        fixed: !!s.fixed, canToggle: !s.fixed,
        selected: st.pipeSel === i ? 'true' : 'false', enabled: s.on ? 'true' : 'false',
        toggleIcon: s.on ? 'toggle_on' : 'toggle_off', toggleColor: s.on ? 'var(--em)' : 'var(--ink3)',
        toggleLabel: (s.on ? 'Disable ' : 'Enable ') + (s.label || t.name),
        select: function () { self.setState({ pipeSel: i }); },
        toggle: function (e) { e.stopPropagation(); var p = pipe.slice(); p[i] = Object.assign({}, p[i], { on: !p[i].on }); self.setState({ pipe: p }); },
        remove: function (e) { e.stopPropagation(); var p = pipe.slice(); p.splice(i, 1); self.setState({ pipe: p, pipeSel: 0 }); },
        up: function (e) { e.stopPropagation(); if (i > 1) { var p = pipe.slice(); var x = p[i - 1]; p[i - 1] = p[i]; p[i] = x; self.setState({ pipe: p, pipeSel: i - 1 }); } },
        down: function (e) { e.stopPropagation(); if (i < pipe.length - 2) { var p = pipe.slice(); var x = p[i + 1]; p[i + 1] = p[i]; p[i] = x; self.setState({ pipe: p, pipeSel: i + 1 }); } },
        upLabel: 'Move ' + (s.label || t.name) + ' earlier', downLabel: 'Move ' + (s.label || t.name) + ' later', removeLabel: 'Remove ' + (s.label || t.name),
        progW: (ps === 'running' && st.pipeAt === i ? st.pipeProg : past ? 100 : failed ? st.pipeProg : 0) + '%',
        connector: i < pipe.length - 1
      };
    });
    var sel = pipe[st.pipeSel] || pipe[0];
    var selTool = this.byslug(sel.slug);
    var protectIdx = -1;
    pipe.forEach(function (s, i) { if (s.slug === 'protect-pdf' || s.slug === 'redact-pdf') protectIdx = i; });
    var orderWarn = protectIdx > -1 && protectIdx < pipe.length - 2;
    var recipes = [
      ['Email-ready', 'Compress, then strip metadata', ['compress-pdf', 'remove-pdf-metadata']],
      ['Scan cleanup', 'OCR, compress, reorder pages', ['ocr-pdf', 'compress-pdf', 'organise-pages']],
      ['Brand & Ship', 'Watermark, number, then protect', ['watermark-pdf', 'add-page-numbers', 'protect-pdf']],
      ['Archival', 'PDF/A with Bates numbering', ['pdf-to-pdfa', 'bates-numbering']]
    ].map(function (r) {
      return { name: r[0], sub: r[1], count: r[2].length + ' steps', go: function () { self.loadRecipe(r[2]); } };
    });
    var bs = st.batchState;
    var ok = st.batch.filter(function (b) { return b.status === 'ok'; });
    var bad = st.batch.filter(function (b) { return b.status === 'fail'; });
    var brow = function (b, i) {
      var c = b.status === 'ok' ? 'var(--em)' : b.status === 'fail' ? 'var(--coral)' : b.status === 'running' ? 'var(--teal)' : 'var(--ink3)';
      return {
        name: b.name, size: b.size, tool: b.tool,
        status: b.status === 'ok' ? 'Successful' : b.status === 'fail' ? 'Failed' : b.status === 'running' ? 'Running' : 'Queued',
        statusIcon: b.status === 'ok' ? 'check_circle' : b.status === 'fail' ? 'error' : b.status === 'running' ? 'progress_activity' : 'schedule',
        color: c, step: b.status === 'fail' ? b.failStep : b.status === 'ok' ? 'Completed' : b.status === 'running' ? b.step : 'Waiting',
        prog: b.prog + '%', progW: b.prog + '%', progColor: b.status === 'fail' ? 'var(--coral)' : 'var(--em)',
        retry: function () { var r = st.batch.slice(); r[i] = Object.assign({}, r[i], { status: 'queued', prog: 0, willFail: false }); self.setState({ batch: r, batchState: 'running' }); self.runBatch(); },
        skip: function () { var r = st.batch.slice(); r.splice(i, 1); self.setState({ batch: r }); },
        retryLabel: 'Retry ' + b.name, skipLabel: 'Remove ' + b.name + ' from batch'
      };
    };
    return {
      isPipeline: st.route === 'pipeline', isBatch: st.route === 'batch',
      nodes: nodes,
      pipeEmpty: ps === 'empty', pipeRunning: ps === 'running', pipeComplete: ps === 'complete',
      pipeFailedState: ps === 'failed', pipeNoFile: ps === 'no-file', pipeAllDisabled: ps === 'all-disabled',
      pipeStateLabel: ps === 'running' ? 'Running' : ps === 'complete' ? 'Complete' : ps === 'failed' ? 'Step failed' : ps === 'no-file' ? 'No file selected' : ps === 'all-disabled' ? 'All steps disabled' : ps === 'empty' ? 'Empty' : 'Configured',
      pipeStateColor: ps === 'failed' ? 'var(--coral)' : ps === 'running' || ps === 'complete' ? 'var(--em)' : ps === 'empty' ? 'var(--ink3)' : 'var(--amber)',
      pipeStates: [['configured', 'Configured'], ['empty', 'Empty'], ['no-file', 'No file'], ['all-disabled', 'All steps disabled'], ['running', 'Running'], ['complete', 'Complete'], ['failed', 'Step failed']].map(function (s) { return { v: s[0], label: s[1] }; }),
      pipeStateValue: ps,
      setPipeState: function (e) {
        var v = e.target.value; clearInterval(self._pv);
        var s = { pipeState: v, pipeProg: 0, pipeAt: 0, pipeFailed: -1 };
        if (v === 'failed') { s.pipeFailed = 2; s.pipeProg = 62; s.pipeSel = 2; }
        if (v === 'running') { s.pipeAt = 1; s.pipeProg = 45; }
        if (v === 'empty') { s.pipe = self.basePipe().filter(function (x) { return x.fixed; }); }
        if (v !== 'empty' && st.pipe.length < 3) s.pipe = self.basePipe();
        if (v === 'all-disabled') s.pipe = self.basePipe().map(function (x) { return x.fixed ? x : Object.assign({}, x, { on: false }); });
        if (v === 'no-file') s.pipeFile = false; else s.pipeFile = true;
        self.setState(s);
      },
      runPipe: function () { self.runPipeline(); },
      stopPipe: function () { clearInterval(self._pv); self.setState({ pipeState: 'configured', pipeProg: 0, pipeAt: 0, pipeFailed: -1 }); },
      retryStep: function () { self.setState({ pipeState: 'running', pipeProg: 0, pipe: st.pipe.map(function (s, i) { return i === st.pipeFailed ? Object.assign({}, s, { fail: false }) : s; }), pipeAt: st.pipeFailed }); self.runPipeline(); },
      skipStep: function () {
        var p = st.pipe.map(function (s, i) { return i === st.pipeFailed ? Object.assign({}, s, { fail: false, on: false, skipped: true }) : s; });
        self.setState({ pipe: p, pipeState: 'running', pipeAt: st.pipeFailed + 1, pipeProg: 0 });
        setTimeout(function () { self.runPipeline(); }, 30);
      },
      lastGood: st.pipeFailed > 0 ? (self.byslug(st.pipe[st.pipeFailed - 1].slug).name) : 'the original file',
      selName: sel.label || selTool.name, selPurpose: sel.label ? sel.sub : selTool.purpose,
      selIcon: sel.icon || selTool.icon, selColor: sel.icon ? 'var(--em)' : selTool.color,
      selMode: sel.mode || selTool.mode,
      selModeLabel: self.modeLabel(sel.mode || selTool.mode),
      selOpts: this.optionsFor(sel.slug).slice(0, 3).map(function (o) {
        return { label: o.label, value: o.t === 'radio' || o.t === 'select' ? (o.choices[0][1]) : o.t === 'toggle' ? 'On' : o.t === 'pages' ? 'All pages' : String(o.min || ''), isChoice: o.t === 'radio' || o.t === 'select', choices: (o.choices || []).map(function (c) { return { v: c[0], label: c[1] }; }) };
      }),
      preserveOn: st.preserve, preserveBg: st.preserve ? 'var(--em)' : 'var(--line2)', preserveKnob: st.preserve ? '18px' : '2px',
      togglePreserve: function () { self.setState({ preserve: !st.preserve }); },
      orderWarn: orderWarn, orderNote: 'Encrypt last: keep Protect PDF or Redact PDF as the final step, or later steps cannot read the file.',
      library: ['compress-pdf', 'merge-pdf', 'redact-pdf', 'pdf-to-jpg', 'ocr-pdf', 'watermark-pdf', 'protect-pdf', 'remove-pdf-metadata', 'organise-pages', 'add-page-numbers', 'bates-numbering', 'resize-image'].map(function (s) {
        var t = self.byslug(s);
        return { name: t.name, purpose: t.purpose, icon: t.icon, color: t.color, fam: t.fam, mode: self.modeShort(t.mode), modeColor: self.modeColor(t.mode), add: function () { var p = st.pipe.slice(); p.splice(p.length - 1, 0, { slug: s, on: true }); self.setState({ pipe: p, pipeSel: p.length - 2, pipeState: 'configured' }); } };
      }),
      libQ: st.libQ, onLibQ: function (e) { self.setState({ libQ: e.target.value }); },
      recipes: recipes,
      saved: st.saved.map(function (p, i) {
        return { name: p.name, count: p.steps.length + ' steps', load: function () { self.loadRecipe(p.steps, p.name); }, del: function (e) { e.stopPropagation(); var s = st.saved.slice(); s.splice(i, 1); self.setState({ saved: s }); }, delLabel: 'Delete pipeline ' + p.name };
      }),
      savePipe: function () {
        var name = 'Pipeline ' + (st.saved.length + 1);
        self.setState({ saved: st.saved.concat([{ name: name, steps: st.pipe.filter(function (s) { return !s.fixed; }).map(function (s) { return s.slug; }) }]) });
        self.toast('Saved to this device as “' + name + '”. Settings only — no documents.');
      },
      pipeFileName: st.pipeFile ? 'Report.pdf · 8.4 MB' : 'No file selected',
      pipeHasFile: st.pipeFile,
      addPipeFile: function () { self.setState({ pipeFile: true, pipeState: 'configured' }); },
      batchRows: st.batch.map(brow), batchOk: ok.map(brow), batchBad: bad.map(brow),
      batchOkCount: ok.length, batchBadCount: bad.length, batchTotal: st.batch.length,
      batchHasFailures: bad.length > 0, batchHasFiles: st.batch.length > 0,
      batchRunning: bs === 'running', batchComplete: bs === 'complete', batchIdle: bs === 'idle',
      batchNoTool: !st.batchTool, batchCancelled: bs === 'cancelled', batchInvalid: bs === 'invalid',
      batchToolName: st.batchTool ? this.byslug(st.batchTool).name : 'No tool chosen',
      batchToolPurpose: st.batchTool ? this.byslug(st.batchTool).purpose : 'Choose one tool to run across every file',
      batchToolIcon: st.batchTool ? this.byslug(st.batchTool).icon : 'help', batchToolColor: st.batchTool ? this.byslug(st.batchTool).color : 'var(--ink3)',
      batchToolMode: st.batchTool ? this.modeLabel(this.byslug(st.batchTool).mode) : '',
      batchStates: [['idle', 'Ready'], ['no-tool', 'No tool chosen'], ['no-files', 'No files'], ['invalid', 'Invalid mix'], ['running', 'Running'], ['complete', 'Some failed'], ['cancelled', 'Cancelled']].map(function (s) { return { v: s[0], label: s[1] }; }),
      batchStateValue: st.batchTool ? (st.batch.length ? bs : 'no-files') : 'no-tool',
      setBatchState: function (e) {
        var v = e.target.value; clearInterval(self._bv);
        if (v === 'no-tool') { self.setState({ batchTool: null, batchState: 'idle' }); return; }
        if (v === 'no-files') { self.setState({ batchTool: 'compress-pdf', batch: [], batchState: 'idle' }); return; }
        if (v === 'invalid') { self.setState({ batchTool: 'compress-pdf', batch: self.baseBatch(), batchState: 'invalid' }); return; }
        if (v === 'complete') { self.setState({ batchTool: 'compress-pdf', batchState: 'complete', batch: self.baseBatch().map(function (b) { return Object.assign({}, b, { status: b.willFail ? 'fail' : 'ok', prog: b.willFail ? 62 : 100 }); }) }); return; }
        if (v === 'running') { self.setState({ batchTool: 'compress-pdf', batch: self.baseBatch().map(function (b, i) { return Object.assign({}, b, { status: i < 2 ? 'ok' : i === 2 ? 'running' : 'queued', prog: i < 2 ? 100 : i === 2 ? 46 : 0 }); }), batchState: 'running' }); return; }
        if (v === 'cancelled') { self.setState({ batchTool: 'compress-pdf', batchState: 'cancelled' }); return; }
        self.setState({ batchTool: 'compress-pdf', batch: self.baseBatch(), batchState: 'idle' });
      },
      runBatchNow: function () { self.runBatch(); },
      stopBatch: function () { clearInterval(self._bv); self.setState({ batchState: 'cancelled' }); },
      retryFailed: function () { self.setState({ batch: st.batch.map(function (b) { return b.status === 'fail' ? Object.assign({}, b, { status: 'queued', prog: 0, willFail: false }) : b; }) }); setTimeout(function () { self.runBatch(); }, 30); },
      continueFailures: function () { self.setState({ batch: st.batch.filter(function (b) { return b.status !== 'fail'; }) }); },
      downloadOk: function () { self.toast('Downloading ' + ok.length + ' results as a ZIP. Nothing is stored.'); },
      addBatchFiles: function () { self.setState({ batch: self.baseBatch(), batchState: 'idle' }); },
      pickBatchTool: function (e) { self.setState({ batchTool: e.target.value }); },
      batchToolValue: st.batchTool || '',
      batchToolOpts: this.tools().slice(0, 24).map(function (t) { return { v: t.slug, label: t.name }; })
    };
  }
  setViewport(w) {
    var b = document.body;
    if (w) { b.style.maxWidth = w + 'px'; b.style.margin = '0 auto'; b.style.boxShadow = '0 0 0 1px var(--line2)'; }
    else { b.style.maxWidth = ''; b.style.margin = ''; b.style.boxShadow = ''; }
    this.setState({ forceW: w });
  }
  vShow() {
    var self = this, st = this.state;
    var routes = [
      ['/', 'Home / discovery'], ['/tools', 'All Tools catalogue'], ['/tool/:slug', 'Tool template (lifecycle)'],
      ['/tools/:slug', 'Tool template (alias)'],
      ['/pipeline', 'Pipeline'], ['/batch', 'Batch'], ['/my-stuff', 'My Stuff'], ['/my-stuff/vault', 'Vault'],
      ['/compare', 'Compare hub'], ['/compare/:competitor', 'Competitor comparison'], ['/blog', 'Blog index'], ['/blog/:slug', 'Article'], ['/about', 'About'],
      ['/privacy', 'Privacy Policy'], ['/security', 'Security Policy'], ['/terms', 'Terms'],
      ['/acceptable-use', 'Acceptable Use'], ['/processors', 'Processors'], ['/accessibility', 'Accessibility'],
      ['/support', 'Support'], ['/status', 'Status'], ['/pwa-errors', 'PWA & error atlas'], ['/404', 'Not found'], ['/_showcase', 'Component showcase']
    ];
    var states = [
      ['Input & sources', 'Empty · hover · scanning · inspected · wrong type · over 500 MB · password needed'],
      ['Tool lifecycle', 'Input · configure · queued · processing (local) · processing (server) · cancelled · result · result ZIP · error · server unavailable'],
      ['Pipeline', 'Empty · configured · no file · all steps disabled · running · complete · step failed (retry / skip / stop)'],
      ['Batch', 'No tool · no files · invalid mix · running · some failed · cancelled · complete'],
      ['My Stuff', 'Populated · per-item delete · erase all · encrypted export · connections connected / disconnected'],
      ['Vault', 'Locked · PIN entry with error and retry · passphrase entry with show/hide and error · unlocking · unlocked · relocked · add · edit · delete with confirmation · protection device / PIN / passphrase'],
      ['Destructive actions', 'Encrypted export: idle · preparing · done · failed, with passphrase, confirm, show/hide and strength. Erase everything: explicit confirmation listing what is removed'],
      ['Compare', 'Unprioritised · reordered by priority · evidence drawer · mobile cards · 13 competitor slots · illustrative profile page · pending-verification page'],
      ['Blog', 'Index · category filter · text search · empty result · article with contents, table, code and callout · article not found with recovery'],
      ['Support', 'Idle · validation errors · sent · error · offline'],
      ['Status', 'Operational · best effort · maintenance · temporary unavailable · log windows 24h / 7d / 30d · incident history · no-incident empty state'],
      ['PWA & errors', 'Install · installed · offline ready · offline · update · permission · unsupported · too large · server required · server unavailable · cleared storage']
    ];
    var qa = [
      ['Contrast', 'Body text, muted labels and status colours checked against panel backgrounds in both themes. Status is never colour-only — every badge carries text.'],
      ['Keyboard', 'Every control is a real button or field — no clickable spans. Pipeline reorder, enable/disable and remove are buttons with labels. Modals set aria-modal, trap Tab, close on Escape and return focus to the trigger.'],
      ['Announcements', 'A polite live region announces route changes, and progress, completion and error text is exposed in-place with role="alert" or aria-live.'],
      ['Focus', 'A single global focus-visible ring, independent of hover, on a 2 px offset.'],
      ['Motion', 'Ambient waves drift on 14–25 s loops; transitions are 140–320 ms on transform, opacity, colour and width only. Reduced motion removes loops, staggering and travelling nodes.'],
      ['Overflow', 'Wide tables scroll inside their panel; long labels ellipsis rather than clip; mobile converts comparison and batch tables to labelled cards.'],
      ['Responsive', 'Three shells: full 216 px rail at ≥1280, compact 64 px icon rail with expandable drawer at 768–1279, top bar plus bottom navigation below 768. Verified at 360×800, 390×844, 768×1024, 1024×768, 1440×900 and 1920×1080.'],
      ['Routing', 'Every screen has a URL, Back and Forward work, refresh restores the screen, and tool, article and comparison URLs are shareable. Titles and descriptions update per route.'],
      ['Persistence', 'Permitted local data is written to localStorage and restored on load. Documents, results, filenames and run state are session-only by construction.'],
      ['Privacy copy', 'No claim of unlimited size, no hidden server use, no filenames in Activity, no analytics language, no fake ratings or testimonials, and every server mention names Mumbai, India.']
    ];
    var limits = [
      'Catalogue: a PrivaTools registry of 65 records. Every mode shows as unverified because none is sourced from a real implementation. Assign window.PRIVATOOLS_CATALOGUE before load to supply the real catalogue; records with modeVerified: true show their real mode.',
      'Comparison: 3 of 13 slots carry layout-example rows with no source, dates or currency; the other 10 show Verified comparison data pending. No competitor fact is asserted.',
      'Blog: 1 complete article body (local-first privacy). The other 5 entries are metadata and layout samples whose body text is placeholder structure, clearly labelled as such. 9 of the 15 planned slugs have no record at all and resolve to article-not-found.',
      'Processing, uploads, cloud OAuth and form submission are simulated in memory. No file is read, written or transmitted, and no lifecycle run reflects a real implementation.',
      'Competitor columns are labelled illustrative placeholders. Four of thirteen slots are shown until each claim is verified.',
      'Vault protection is simulated. The secret you create is stored unencrypted in localStorage, so it must never be a real password.'
    ];
    return {
      showRoutes: routes.map(function (r) { return { path: r[0], name: r[1] }; }),
      showStates: states.map(function (s) { return { area: s[0], list: s[1] }; }),
      showQA: qa.map(function (q) { return { area: q[0], text: q[1] }; }),
      showLimits: limits.map(function (l) { return { t: l }; }),
      badges: [['Local', 'var(--em)'], ['Server', 'var(--amber)'], ['Offline', 'var(--teal)'], ['Ready', 'var(--em)'], ['Best effort', 'var(--amber)'], ['Temporary unavailable', 'var(--coral)']].map(function (b) { return { label: b[0], color: b[1] }; }),
      alerts: [['Information', 'info', 'var(--blue)', 'var(--blueSoft)'], ['Success', 'check_circle', 'var(--em)', 'var(--emSoft)'], ['Warning', 'warning', 'var(--amber)', 'var(--amberSoft)'], ['Error', 'error', 'var(--coral)', 'var(--coralSoft)']].map(function (a) { return { label: a[0], icon: a[1], color: a[2], bg: a[3] }; }),
      btnStates: [['Primary', 'var(--em)', 'var(--em-ink,#04231A)', '1'], ['Hover', 'var(--teal)', 'var(--em-ink,#04231A)', '1'], ['Disabled', 'var(--line2)', 'var(--ink3)', '.6']].map(function (b) { return { label: b[0], bg: b[1], fg: b[2], op: b[3] }; }),
      viewports: [[0, 'Window'], [1440, 'Desktop 1440'], [768, 'Tablet 768'], [390, 'Mobile 390'], [360, 'Mobile 360']].map(function (v) {
        var on = (st.forceW || 0) === v[0];
        return { label: v[1], bg: on ? 'var(--emSoft)' : 'var(--panel2)', fg: on ? 'var(--em)' : 'var(--ink2)', bd: on ? 'var(--emLine)' : 'var(--line)', go: function () { self.setViewport(v[0]); } };
      }),
      goAtlas: function () { self.go('atlas'); }, go404: function () { self.go('404'); }, goShowcase: function () { self.go('showcase'); },
      goAbout: function () { self.go('about'); }, goCompare: function () { self.go('compare'); }, goBlog: function () { self.go('blog'); },
      goSupport: function () { self.go('support'); },
      persistRows: [
        ['Theme preference', 'Stored', 'localStorage — system, dark or light'],
        ['Saved pipelines', 'Stored', 'Names and step settings only'],
        ['Tool defaults', 'Stored', 'Your preferred option values'],
        ['Activity', 'Stored', 'Tool, date and settings — never filenames'],
        ['Reusable assets', 'Stored', 'Signatures, logos, stamps you saved on purpose'],
        ['Bates counters', 'Stored', 'Counter name and next number'],
        ['Cloud connections', 'Stored', 'Connected flag only, until you disconnect'],
        ['Vault protection level', 'Stored', 'Device only, PIN or passphrase'],
        ['Vault entries (demo)', 'Stored', 'Password records for this demo'],
        ['Input documents', 'Never stored', 'Session only — gone on refresh'],
        ['Output documents / results', 'Never stored', 'Exist in the active tab only'],
        ['Filenames', 'Never persisted', 'Visible in the active batch only'],
        ['Active pipeline run state', 'Never stored', 'Session only'],
        ['Analytics or telemetry', 'Never collected', 'No tracking of any kind']
      ].map(function (r) { return { k: r[0], v: r[1], note: r[2], color: r[1] === 'Stored' ? 'var(--em)' : 'var(--ink3)' }; }),
      catMeta: (function () { var m = self.catalogue().meta; return { loaded: self.tools().length, declared: m.declaredTotal, pdf: m.declaredPdf, nonPdf: m.declaredNonPdf, dataset: m.dataset, publicCopy: m.publicCopy }; })(),
      schemaRows: [
        ['slug', 'string', 'Unique URL segment. Routes /tool/<slug> and /tools/<slug>'],
        ['name', 'string', 'Display name, sentence case'],
        ['purpose', 'string', 'One short line'],
        ['family', 'enum', 'PDF · Images · Video · Audio · Archives · Documents & Data'],
        ['subfamily', 'string', 'PDF only: Convert to PDF, Edit, Convert from PDF, Organise, Optimise, Advanced, Security'],
        ['task', 'enum', 'Convert · Compress · Edit · Organise · Extract · Security · Automate'],
        ['mode', 'enum', 'local · fallback · server — drives the disclosure badge'],
        ['input / output', 'string', 'Primary accepted and produced type'],
        ['popular', 'boolean', 'Optional. Surfaces on Home'],
        ['options', 'array', 'Optional per-tool control schema']
      ].map(function (r) { return { field: r[0], type: r[1], note: r[2] }; }),
      footerLinks: [['about', 'About'], ['privacy', 'Privacy'], ['security', 'Security'], ['terms', 'Terms'], ['acceptable-use', 'Acceptable use'], ['processors', 'Processors'], ['accessibility', 'Accessibility'], ['support', 'Support'], ['status', 'Status'], ['atlas', 'PWA & errors'], ['404', '404'], ['showcase', 'Components (dev)']].map(function (l) {
        return { label: l[1], go: function () { self.go(l[0]); } };
      })
    };
  }
  policies() {
    return {
      privacy: {
        title: 'Privacy Policy', sub: 'How your privacy is protected.', updated: 'Updated 1 August 2026',
        secs: [
          ['Local-first by default', ['PrivaTools runs in your browser and uses your device whenever possible. For local tools, no file, filename or file content is transmitted anywhere.', 'Each tool states its processing mode before you start: Local, Local with server fallback, or Server required.']],
          ['Disclosed server processing', ['When a tool cannot finish on your device, processing may fall back to our server in Mumbai, India. This is always disclosed on the tool and never hidden. There is no pre-upload confirmation step, because the mode is stated up front and shown while the work runs.', 'Each request is processed in isolation and the temporary copy is deleted immediately after the response. Server capacity is best effort only, with no failover.']],
          ['Immediate deletion, no document retention', ['Server processing necessarily reads the file it is given: that is what processing means. What matters is what happens afterwards. Input and output documents are not retained, and the temporary processing copy is deleted immediately after the response is returned.', 'Nothing is written to a document store, backup or queue, because none exists.']],
          ['No analytics', ['There is no product analytics, tracking pixel, advertising identifier or behavioural telemetry. There is no account system, so there is nothing to profile.']],
          ['Transient results', ['A result exists only in the current tab. Refreshing or closing the tab removes it, unless you deliberately downloaded it or saved it to a provider you chose.']],
          ['Content-free security logs', ['Logs exist for abuse protection and contain no filenames, no file contents and no analytics.', 'Routine records: 24 hours. Suspicious activity: 7 days. Confirmed incident: 30 days.']],
          ['Third-party processors', ['A small number of vetted third-party processors may be used for specific tools. Where that happens, it is disclosed on that tool. See the Processors page for the current list.']],
          ['Your local data', ['My Stuff, Vault, defaults, saved pipelines, Bates counters and cloud connection state live on your device. You can inspect, delete or erase all of it at any time.']]
        ]
      },
      security: {
        title: 'Security Policy', sub: 'Safeguards and how to report a problem.', updated: 'Updated 1 August 2026',
        secs: [
          ['Transport and isolation', ['All traffic is encrypted in transit. Server work runs in an isolated context per request and is destroyed after the response.']],
          ['Data minimisation', ['The safest way to protect a document is not to hold it. No documents are stored, so there is no document store to breach.']],
          ['Abuse protection', ['Anonymous use has no visible limits. Protective measures apply only to abuse, and produce content-free records under the retention windows in the Privacy Policy.']],
          ['Vulnerability disclosure', ['Report security issues to security@priva.tools. Please include reproduction steps and avoid testing against other people’s data. We aim to acknowledge reports within three working days.']],
          ['Client-side scope', ['Because most work happens in your browser, keeping your browser and device updated is a meaningful part of the security model.']]
        ]
      },
      terms: {
        title: 'Terms of Service', sub: 'The agreement for using PrivaTools.', updated: 'Updated 1 August 2026',
        secs: [
          ['Free service, no account', ['PrivaTools is free forever and owner-funded. There is no paid tier, watermark, advertisement or account.']],
          ['Provided as is', ['The website and local tools target 99.9% availability. Server-backed tools are best effort in Mumbai, India with no failover, and may be temporarily unavailable when capacity is exhausted.']],
          ['Your files, your responsibility', ['You keep all rights to your files. You are responsible for having the right to process the documents you use, including any password you supply to unlock a file.']],
          ['Limits', ['Every valid file is supported up to 500 MB per file in every tool.']],
          ['Governing terms', ['The service is operated by an individual in India. English is the governing language of these terms.']]
        ]
      },
      'acceptable-use': {
        title: 'Acceptable Use Policy', sub: 'What the service may not be used for.', updated: 'Updated 1 August 2026',
        secs: [
          ['Do not attack the service', ['No automated scraping of server-backed tools, no attempts to exhaust capacity, and no probing of other users. Server capacity is shared and finite.']],
          ['Do not process content you have no right to', ['Do not use the tools to remove protection from documents you do not own or are not authorised to unlock.']],
          ['Do not use it for unlawful material', ['Processing illegal content is prohibited. Because nothing is retained, enforcement relies on blocking abusive traffic rather than inspecting documents.']],
          ['Consequences', ['Abusive traffic may be blocked. Blocks produce content-free records under the retention windows in the Privacy Policy.']]
        ]
      },
      processors: {
        title: 'Processors', sub: 'Every third party that can touch a request.', updated: 'Updated 1 August 2026',
        secs: [
          ['Principle', ['Vetted third-party processors may be used only where a tool genuinely needs one, and only with clear per-tool disclosure.']],
          ['Current position', ['Server processing runs on infrastructure in Mumbai, India. Any additional processor is named on the tool that uses it, before you start.']],
          ['What they receive', ['Only the file needed to complete the request, for the duration of that request. No account data exists to share, and no analytics are collected or forwarded.']],
          ['Changes', ['This page is reviewed monthly. If a processor is added or removed, the affected tools are updated at the same time.']]
        ]
      },
      accessibility: {
        title: 'Accessibility', sub: 'Built for everyone.', updated: 'Updated 1 August 2026',
        secs: [
          ['Standard', ['The interface targets WCAG 2.2 AA: visible focus, full keyboard operation, status conveyed by icon and text, and live announcements for progress and errors.']],
          ['Keyboard and screen readers', ['Every drag or reorder action has a keyboard and button alternative. Tested with NVDA, JAWS and VoiceOver.']],
          ['Motion and contrast', ['Reduced motion removes ambient loops, animated reordering and travelling nodes while keeping clear state changes. High contrast follows your system setting.']],
          ['Zoom and text size', ['Layouts hold at 200% zoom and respect browser text size. Tables convert to labelled cards on small screens.']],
          ['Feedback', ['Accessibility problems can be reported to support@priva.tools and are treated as defects, not enhancements.']]
        ]
      }
    };
  }
  vTrust(isMobile) {
    var self = this, st = this.state;
    var P = this.policies();
    var slug = P[st.route] ? st.route : 'privacy';
    var cur = P[slug];
    var order = ['privacy', 'security', 'terms', 'acceptable-use', 'processors', 'accessibility'];
    var logs = { '24h': [['Files processed locally', '1,842'], ['Files sent to server (fallback)', '312'], ['Server errors', '2'], ['Security events', '0']], '7d': [['Files processed locally', '11,904'], ['Files sent to server (fallback)', '2,067'], ['Server errors', '14'], ['Security events', '1']], '30d': [['Files processed locally', '48,517'], ['Files sent to server (fallback)', '8,930'], ['Server errors', '61'], ['Security events', '3']] };
    var comps = [
      ['Website', 'operational', '99.9% target'],
      ['Local tools', 'operational', '99.9% target — runs on your device'],
      ['Server tools (fallback)', this.props.serverState === 'unavailable' ? 'unavailable' : 'best', 'Best effort · Mumbai, India · no failover'],
      ['PWA / offline cache', 'operational', 'Cached local tools work offline'],
      ['Help centre', 'maintenance', 'Scheduled content update']
    ];
    var stateMap = { operational: ['Operational', 'var(--em)', 'check_circle'], best: ['Best effort', 'var(--amber)', 'error_outline'], degraded: ['Degraded', 'var(--amber)', 'error_outline'], maintenance: ['Maintenance', 'var(--blue)', 'build_circle'], unavailable: ['Temporary unavailable', 'var(--coral)', 'cancel'] };
    var atlas = [
      ['Install PrivaTools', 'Install this app on your device for quick access and offline use.', 'install_mobile', 'var(--em)', 'Install'],
      ['Installed', 'PrivaTools is installed. Launch it from your home screen or dock.', 'check_circle', 'var(--em)', 'Open app'],
      ['Offline ready', 'Cached local tools are ready. Server tools will wait for a connection.', 'cloud_done', 'var(--teal)', 'View local tools'],
      ['You are offline', 'PrivaTools works fully offline for local tools. Server tools are unavailable.', 'wifi_off', 'var(--amber)', 'View local tools'],
      ['Update available', 'A new version is ready. Reload to get the latest tools.', 'system_update', 'var(--blue)', 'Update'],
      ['Permission needed', 'Storage access is required to add or save files from this source.', 'lock', 'var(--amber)', 'Grant permission'],
      ['Unsupported file', 'This file type is not supported by this tool. Accepted types are listed on the tool.', 'block', 'var(--coral)', 'Choose another'],
      ['File exceeds 500 MB', 'Every tool supports up to 500 MB per file. Choose a file of 500 MB or smaller.', 'scale', 'var(--coral)', 'Choose another'],
      ['This tool needs a server', 'Server fallback is required for this conversion, in Mumbai, India.', 'dns', 'var(--amber)', 'Continue'],
      ['Server unavailable', 'We cannot reach our server. Local tools and the rest of the site keep working.', 'cloud_off', 'var(--coral)', 'Retry'],
      ['Storage was cleared', 'Local defaults, pipelines and vault entries were removed by your browser.', 'delete_sweep', 'var(--ink2)', 'Start fresh']
    ];
    var probe = (st.badSlug || (st.failedPath || '').split('/').pop() || '').toLowerCase().replace(/[^a-z0-9]+/g, ' ').trim();
    var words = probe.split(' ').filter(function (w) { return w.length > 2; });
    var scored = this.tools().map(function (t) {
      var hay = (t.slug + ' ' + t.name + ' ' + t.purpose).toLowerCase();
      var score = 0;
      words.forEach(function (w) { if (hay.indexOf(w) >= 0) score += w.length; for (var i = 0; i < w.length - 2; i++) { if (hay.indexOf(w.slice(i, i + 3)) >= 0) score += 1; } });
      return { t: t, score: score };
    }).sort(function (a, b) { return b.score - a.score; });
    var top = scored[0] && scored[0].score > 0 ? scored.slice(0, 3) : [{ t: self.byslug('compress-pdf') }, { t: self.byslug('pdf-to-jpg') }, { t: self.byslug('merge-pdf') }];
    var fuzzy = top.map(function (x) { var t = x.t; return { name: t.name, purpose: t.purpose, icon: t.icon, color: t.color, go: function () { self.go('tool', t.slug); } }; });
    var f = st.form;
    var errs = { subject: st.formTouched && !f.subject, desc: st.formTouched && f.desc.length < 12 };
    return {
      isPolicy: order.indexOf(st.route) >= 0, isSupport: st.route === 'support', isStatus: st.route === 'status',
      is404: st.route === '404', isAtlas: st.route === 'atlas', isShowcase: st.route === 'showcase',
      pTitle: cur.title, pSub: cur.sub, pUpdated: cur.updated,
      pTabs: order.map(function (o) {
        var on = o === slug;
        return { label: P[o].title.replace(' Policy', '').replace('Terms of Service', 'Terms'), bg: on ? 'var(--emSoft)' : 'var(--panel)', fg: on ? 'var(--em)' : 'var(--ink2)', bd: on ? 'var(--emLine)' : 'var(--line)', go: function () { self.go(o); } };
      }),
      pSecs: cur.secs.map(function (s, i) {
        return { h: s[0], anchor: 'sec-' + i, ps: s[1].map(function (p) { return { t: p }; }), copy: function () { self.toast('Link to “' + s[0] + '” copied.'); }, copyLabel: 'Copy link to ' + s[0] };
      }),
      pToc: cur.secs.map(function (s, i) { return { label: s[0], go: function () { self.toast('Jumped to “' + s[0] + '”.'); } }; }),
      logTabs: ['24h', '7d', '30d'].map(function (k) {
        var on = st.logWin === k;
        return { label: k === '24h' ? '24 hours' : k === '7d' ? '7 days' : '30 days', bg: on ? 'var(--emSoft)' : 'transparent', fg: on ? 'var(--em)' : 'var(--ink3)', go: function () { self.setState({ logWin: k }); } };
      }),
      logRows: logs[st.logWin].map(function (r) { return { k: r[0], v: r[1] }; }),
      components: comps.map(function (c) {
        var m = stateMap[c[1]];
        return { name: c[0], note: c[2], label: m[0], color: m[1], icon: m[2] };
      }),
      incidents: (st.hideIncidents ? [] : [
        ['18 Aug 2026, 14:22 IST', 'Server tools (fallback)', 'Major', 'Resolved', '18 Aug, 15:40 IST', [['14:22', 'Capacity exhausted in Mumbai. Server-backed tools returned Temporary unavailable.'], ['14:35', 'Local tools and the website confirmed unaffected.'], ['15:40', 'Capacity restored. Server tools accepting requests again.']]],
        ['2 Aug 2026, 03:10 IST', 'Help centre', 'Minor', 'Resolved', '2 Aug, 04:05 IST', [['03:10', 'Scheduled content update took the help centre offline.'], ['04:05', 'Content update completed.']]],
        ['21 Jul 2026, 19:48 IST', 'Server tools (fallback)', 'Minor', 'Resolved', '21 Jul, 20:12 IST', [['19:48', 'Elevated error rate on PDF to Word requests.'], ['20:12', 'Error rate back to baseline. No document was retained at any point.']]]
      ]).map(function (i) {
        return {
          when: i[0], service: i[1], severity: i[2], state: i[3], resolved: i[4],
          sevColor: i[2] === 'Major' ? 'var(--coral)' : 'var(--amber)',
          stateColor: i[3] === 'Resolved' ? 'var(--em)' : 'var(--amber)',
          updates: i[5].map(function (u) { return { t: u[0], text: u[1] }; })
        };
      }),
      noIncidents: !!st.hideIncidents,
      toggleIncidents: function () { self.setState({ hideIncidents: !st.hideIncidents }); },
      incidentsLabel: st.hideIncidents ? 'Show incident history' : 'Preview empty state',
      statusUpdated: 'Last updated 22 Aug 2026, 09:41 IST',
      refresh: function () { self.toast('Status refreshed.'); },
      serverOutageNote: 'If server tools are unavailable, the website and every local tool keep working. Nothing you have in the current tab is lost.',
      atlas: atlas.map(function (a) { return { title: a[0], text: a[1], icon: a[2], color: a[3], action: a[4], go: function () { self.toast(a[4] + ' — demo only.'); } }; }),
      fuzzy: fuzzy,
      is404Tool: (st.failedPath || '').indexOf('/tool') === 0,
      notFoundTitle: (st.failedPath || '').indexOf('/tool') === 0 ? 'That tool does not exist' : 'That page does not exist',
      notFoundLead: 'No tool is published at this address. The closest matches from the catalogue are below.',
      probeBadTool: function () { self.go('tool', 'not-real'); },
      reportLink: function () { self.setState({ form: Object.assign({}, f, { cat: 'Broken link' }) }); self.go('support'); },
      form: f,
      formSubject: f.subject, formDesc: f.desc, formCat: f.cat, formFile: f.file,
      formCats: ['Report an issue', 'Feature request', 'Broken link', 'Security vulnerability', 'Accessibility problem'].map(function (c) { return { v: c, label: c }; }),
      onSubject: function (e) { self.setState({ form: Object.assign({}, f, { subject: e.target.value }) }); },
      onDesc: function (e) { self.setState({ form: Object.assign({}, f, { desc: e.target.value }) }); },
      onCat: function (e) { self.setState({ form: Object.assign({}, f, { cat: e.target.value }) }); },
      attach: function () { self.setState({ form: Object.assign({}, f, { file: 'screenshot.png · 240 KB' }) }); },
      detach: function () { self.setState({ form: Object.assign({}, f, { file: null }) }); },
      errSubject: errs.subject, errDesc: errs.desc,
      formState: st.formState,
      formSent: st.formState === 'sent', formError: st.formState === 'error', formOffline: st.formState === 'offline',
      submit: function () {
        self.setState({ formTouched: true });
        if (!f.subject || f.desc.length < 12) return;
        self.setState({ formState: 'sent' });
      },
      resetForm: function () { self.setState({ form: { subject: '', desc: '', cat: 'Report an issue', file: null }, formState: 'idle', formTouched: false }); },
      simulateError: function () { self.setState({ formState: 'error' }); },
      simulateOffline: function () { self.setState({ formState: 'offline' }); },
      channels: [
        ['Help centre', 'Guides and how-tos', 'help_center', 'Open'],
        ['Email support', 'support@priva.tools', 'mail', 'Copy'],
        ['Public tracker', 'Open issues and progress', 'bug_report', 'Open'],
        ['Security vulnerability', 'security@priva.tools', 'shield_lock', 'Copy']
      ].map(function (c) { return { name: c[0], sub: c[1], icon: c[2], action: c[3], go: function () { self.toast(c[0] + ' — demo only.'); } }; })
    };
  }
  vData(isMobile) {
    var self = this, st = this.state;
    var act = st.activity.map(function (a, i) {
      var t = self.byslug(a.slug);
      return { name: t.name, icon: t.icon, color: t.color, date: a.date, settings: a.settings, del: function () { var x = st.activity.slice(); x.splice(i, 1); self.setState({ activity: x }); }, delLabel: 'Delete this activity entry' };
    });
    var conns = st.conns.map(function (c, i) {
      return {
        name: c.name, icon: c.icon, on: c.on, state: c.on ? 'Connected' : 'Not connected',
        color: c.on ? 'var(--em)' : 'var(--ink3)', action: c.on ? 'Disconnect' : 'Connect',
        toggle: function () { var x = st.conns.slice(); x[i] = Object.assign({}, x[i], { on: !x[i].on }); self.setState({ conns: x }); self.toast(x[i].on ? c.name + ' connected on this device only.' : c.name + ' disconnected. The local token was removed.'); }
      };
    });
    var entries = st.vault.map(function (v, i) {
      return {
        name: v.name, user: v.user, icon: v.icon, shown: st.reveal === i,
        secret: st.reveal === i ? v.pw : '••••••••••',
        revealIcon: st.reveal === i ? 'visibility_off' : 'visibility',
        revealLabel: (st.reveal === i ? 'Hide' : 'Reveal') + ' password for ' + v.name,
        reveal: function () { self.setState({ reveal: st.reveal === i ? null : i }); },
        copy: function () { self.toast('Copied to the clipboard. Nothing left this device.'); },
        del: function () { self.setState({ dialog: 'delete-entry', pendingDelete: i }); },
        edit: function () { self.setState({ entryEdit: i, entryDraft: { name: v.name, user: v.user, pw: v.pw } }); },
        editLabel: 'Edit ' + v.name,
        delLabel: 'Delete ' + v.name, delay: (120 + i * 30) + 'ms'
      };
    });
    var rows = [
      ['Free to use', ['Yes', 'Yes', 'Yes', 'Yes'], [1, 1, 1, 1], 'No source supplied', 'price'],
      ['No account required', ['Yes', 'Yes', 'No', 'Yes'], [1, 1, 0, 1], 'No source supplied', 'price'],
      ['Works offline', ['Yes — installable PWA', 'Limited', 'No', 'Limited'], [1, 2, 0, 2], 'No source supplied', 'offline'],
      ['Local-first processing', ['Yes', 'Yes', 'No', 'No'], [1, 1, 0, 0], 'No source supplied', 'privacy'],
      ['Server use disclosed per tool', ['Yes', 'Partly', 'No', 'No'], [1, 2, 0, 0], 'No source supplied', 'privacy'],
      ['Max file size', ['500 MB per file', '100 MB', '100 MB', '50 MB'], [1, 2, 2, 0], 'No source supplied', 'size'],
      ['No document retention', ['Yes', 'Yes', 'No', 'No'], [1, 1, 0, 0], 'No source supplied', 'privacy'],
      ['No analytics or tracking', ['Yes', 'Yes', 'No', 'No'], [1, 1, 0, 0], 'No source supplied', 'privacy'],
      ['Installable app (PWA)', ['Yes', 'Yes', 'No', 'Yes'], [1, 1, 0, 1], 'No source supplied', 'offline']
    ];
    var pri = st.priority;
    var sorted = rows.slice().sort(function (a, b) {
      if (pri === 'all') return 0;
      var aw = a[4] === pri ? 0 : 1, bw = b[4] === pri ? 0 : 1;
      return aw - bw;
    });
    var cmpRows = sorted.map(function (r) {
      return {
        feature: r[0], evidence: r[3], changed: pri !== 'all' && r[4] === pri,
        bg: pri !== 'all' && r[4] === pri ? 'var(--emSoft)' : 'transparent',
        cells: r[1].map(function (v, i) {
          var s = r[2][i];
          return { v: v, icon: s === 1 ? 'check_circle' : s === 2 ? 'error_outline' : 'cancel', color: s === 1 ? 'var(--em)' : s === 2 ? 'var(--amber)' : 'var(--coral)' };
        }),
        openEvidence: function () { self.setState({ drawer2: r[0] }); }
      };
    });
    var posts = this.postData();
    var cats = ['All', 'Privacy', 'How-to', 'Product', 'Formats'];
    var bq = (st.blogQ || '').toLowerCase();
    var blogList = posts.filter(function (p) {
      if (st.blogCat !== 'All' && p.cat !== st.blogCat) return false;
      if (bq && (p.title + ' ' + p.lead + ' ' + p.cat).toLowerCase().indexOf(bq) < 0) return false;
      return true;
    });
    var post = this.postBySlug(st.postSlug);
    var comp = this.competitorBySlug(st.competitor) || this.competitorData()[0];
    return {
      isStuff: st.route === 'mystuff', isVault: st.route === 'vault',
      isCompare: st.route === 'compare', isBlog: st.route === 'blog', isPost: st.route === 'post', isAbout: st.route === 'about',
      stuffTiles: [
        { label: 'Reusable assets', value: st.assets.length + ' items', icon: 'draw', color: 'var(--em)' },
        { label: 'Saved pipelines', value: String(st.saved.length), icon: 'account_tree', color: 'var(--blue)' },
        { label: 'Bates counters', value: String(st.bates.length), icon: 'pin', color: 'var(--violet)' },
        { label: 'Connections', value: String(st.conns.filter(function (c) { return c.on; }).length), icon: 'cloud_sync', color: 'var(--teal)' }
      ],
      activity: act, hasActivity: act.length > 0,
      eraseActivity: function () { self.setState({ activity: [] }); self.toast('Activity erased from this device.'); },
      assets: st.assets.map(function (a, i) {
        return { name: a.name, kind: a.kind, icon: a.icon, del: function () { var x = st.assets.slice(); x.splice(i, 1); self.setState({ assets: x }); }, delLabel: 'Delete ' + a.name };
      }),
      bates: st.bates.map(function (b, i) {
        return { name: b.name, next: b.next, inc: function () { var x = st.bates.slice(); x[i] = Object.assign({}, x[i], { next: x[i].next + 1 }); self.setState({ bates: x }); }, incLabel: 'Advance counter ' + b.name };
      }),
      defaults: st.defaults.map(function (d) { var t = self.byslug(d.slug); return { name: t.name, icon: t.icon, color: t.color, value: d.value }; }),
      conns: conns,
      exportBackup: function () { self.setState({ dialog: 'backup', bk: { pass: '', confirm: '', show: false, state: 'idle' } }); },
      eraseAll: function () { self.setState({ dialog: 'erase' }); },
      goVault: function () { self.go('vault'); },
      vaultLocked: st.vaultStage === 'locked', vaultOpen: st.vaultStage === 'unlocked',
      vaultPinStage: st.vaultStage === 'pin', vaultPassStage: st.vaultStage === 'passphrase', vaultUnlocking: st.vaultStage === 'unlocking',
      vaultStage: st.vaultStage,
      vaultProtectLabel: st.protect === 'device' ? 'Device only' : st.protect === 'pin' ? 'PIN' : 'Passphrase',
      needsSetup: st.protect !== 'device' && !st.vSecret,
      hasSecret: !!st.vSecret,
      secretKind: st.protect === 'pin' ? 'PIN' : 'passphrase',
      vaultPinKind: st.protect === 'pin', vaultPassKind: st.protect === 'passphrase',
      protoWarning: 'Design prototype only. Vault protection is simulated and stored unencrypted in localStorage. Do not enter real passwords.',
      beginUnlock: function () {
        if (st.protect === 'device') { self.setState({ vaultStage: 'unlocking', pinErr: '' }); setTimeout(function () { self.setState({ vaultStage: 'unlocked' }); self.announce('Vault unlocked.'); }, 420); return; }
        if (!st.vSecret) { self.setState({ vaultStage: 'setup', pin: '', pass: '', pinErr: '', passErr: '' }); return; }
        self.setState({ vaultStage: st.protect === 'pin' ? 'pin' : 'passphrase', pin: '', pass: '', pinErr: '', passErr: '' });
      },
      vaultSetupStage: st.vaultStage === 'setup', vaultConfirmStage: st.vaultStage === 'confirm',
      setupSecret: function () {
        var v = st.protect === 'pin' ? st.pin : st.pass;
        var min = st.protect === 'pin' ? 4 : 8;
        if ((v || '').length < min) { var e1 = {}; e1[st.protect === 'pin' ? 'pinErr' : 'passErr'] = 'Use at least ' + min + (st.protect === 'pin' ? ' digits.' : ' characters.'); self.setState(e1); return; }
        self.setState({ draftSecret: v, vaultStage: 'confirm', pin: '', pass: '', pinErr: '', passErr: '' });
      },
      confirmSecret: function () {
        var v = st.protect === 'pin' ? st.pin : st.pass;
        if (v !== st.draftSecret) { var e2 = {}; e2[st.protect === 'pin' ? 'pinErr' : 'passErr'] = 'The two entries do not match. Start again.'; self.setState(e2); return; }
        self.setState({ vSecret: v, draftSecret: '', vaultStage: 'unlocked', pin: '', pass: '', pinErr: '', passErr: '' });
        self.announce('Vault protection created. Vault unlocked.');
      },
      restartSetup: function () { self.setState({ vaultStage: 'setup', draftSecret: '', pin: '', pass: '', pinErr: '', passErr: '' }); },
      clearSecret: function () { self.setState({ vSecret: '', draftSecret: '', vaultStage: 'locked', pin: '', pass: '', pinErr: '', passErr: '' }); self.toast('Protection removed. Choose a level to set it up again.'); },
      usePin: function () { self.setState({ protect: 'pin', vaultStage: st.vSecret && st.protect === 'pin' ? 'pin' : 'setup', pin: '', pinErr: '', draftSecret: '' }); },
      usePassphrase: function () { self.setState({ protect: 'passphrase', vaultStage: st.vSecret && st.protect === 'passphrase' ? 'passphrase' : 'setup', pass: '', passErr: '', draftSecret: '' }); },
      cancelUnlock: function () { self.setState({ vaultStage: 'locked', pin: '', pass: '', pinErr: '', passErr: '' }); },
      pin: st.pin, onPin: function (e) { self.setState({ pin: (e.target.value || '').replace(/[^0-9]/g, '').slice(0, 6), pinErr: '' }); },
      pinErr: st.pinErr, hasPinErr: !!st.pinErr, pinTries: st.pinTries,
      submitPin: function () {
        if (st.pin.length < 4) { self.setState({ pinErr: 'Enter at least four digits.' }); return; }
        if (st.pin !== st.vSecret) { self.setState({ pinErr: 'That PIN does not match the one you created.', pinTries: st.pinTries + 1, pin: '' }); return; }
        self.setState({ vaultStage: 'unlocking', pinErr: '', pinTries: 0 });
        setTimeout(function () { self.setState({ vaultStage: 'unlocked', pin: '' }); self.announce('Vault unlocked.'); }, 420);
      },
      pass: st.pass, onPass: function (e) { self.setState({ pass: e.target.value, passErr: '' }); },
      passShow: st.passShow, passType: st.passShow ? 'text' : 'password',
      togglePassShow: function () { self.setState({ passShow: !st.passShow }); },
      passErr: st.passErr, hasPassErr: !!st.passErr,
      submitPass: function () {
        if (st.pass.length < 8) { self.setState({ passErr: 'Use at least eight characters.' }); return; }
        if (st.pass !== st.vSecret) { self.setState({ passErr: 'That passphrase does not match the one you created.', pass: '' }); return; }
        self.setState({ vaultStage: 'unlocking', passErr: '' });
        setTimeout(function () { self.setState({ vaultStage: 'unlocked', pass: '' }); self.announce('Vault unlocked.'); }, 420);
      },
      lock: function () { self.setState({ vaultStage: 'locked', reveal: null, entryEdit: null }); self.announce('Vault locked.'); },
      relocked: st.vaultStage === 'locked' && st.pinTries === 0,
      vaultEntries: entries, vaultCount: st.vault.length, vaultEmpty: st.vault.length === 0,
      protection: st.protect,
      protections: [['device', 'Device only', 'No prompt on this device'], ['pin', 'PIN', 'Short numeric code'], ['passphrase', 'Passphrase', 'Strongest option']].map(function (p) {
        var on = st.protect === p[0];
        return { label: p[1], sub: p[2], bd: on ? 'var(--em)' : 'var(--line)', bg: on ? 'var(--emSoft)' : 'var(--panel2)', dot: on ? 'var(--em)' : 'transparent', ring: on ? 'var(--em)' : 'var(--line2)', checked: on ? 'true' : 'false', go: function () { self.setState({ protect: p[0], vSecret: p[0] === 'device' ? '' : '', draftSecret: '', pin: '', pass: '', pinErr: '', passErr: '', vaultStage: p[0] === 'device' ? 'unlocked' : 'setup' }); } };
      }),
      addPassword: function () { self.setState({ entryEdit: 'new', entryDraft: { name: '', user: '', pw: '' } }); },
      entryEditing: !!st.entryEdit, entryIsNew: st.entryEdit === 'new',
      entryDraft: st.entryDraft, dName: st.entryDraft.name, dUser: st.entryDraft.user, dPw: st.entryDraft.pw,
      onDName: function (e) { self.setState({ entryDraft: Object.assign({}, st.entryDraft, { name: e.target.value }) }); },
      onDUser: function (e) { self.setState({ entryDraft: Object.assign({}, st.entryDraft, { user: e.target.value }) }); },
      onDPw: function (e) { self.setState({ entryDraft: Object.assign({}, st.entryDraft, { pw: e.target.value }) }); },
      cancelEntry: function () { self.setState({ entryEdit: null }); },
      saveEntry: function () {
        var d = st.entryDraft;
        if (!d.name || !d.pw) { self.toast('A name and a password are required.'); return; }
        var v = st.vault.slice();
        if (st.entryEdit === 'new') v.push({ name: d.name, user: d.user || '—', pw: d.pw, icon: 'key' });
        else v[st.entryEdit] = Object.assign({}, v[st.entryEdit], { name: d.name, user: d.user || '—', pw: d.pw });
        self.setState({ vault: v, entryEdit: null });
        self.toast('Saved on this device.');
      },
      bkPass: st.bk.pass, bkConfirm: st.bk.confirm, bkShow: st.bk.show, bkType: st.bk.show ? 'text' : 'password',
      onBkPass: function (e) { self.setState({ bk: Object.assign({}, st.bk, { pass: e.target.value }) }); },
      onBkConfirm: function (e) { self.setState({ bk: Object.assign({}, st.bk, { confirm: e.target.value }) }); },
      toggleBkShow: function () { self.setState({ bk: Object.assign({}, st.bk, { show: !st.bk.show }) }); },
      bkStrength: st.bk.pass.length === 0 ? 'Enter a passphrase' : st.bk.pass.length < 10 ? 'Weak — use at least ten characters' : st.bk.pass.length < 16 ? 'Reasonable — longer is better' : 'Strong',
      bkStrengthColor: st.bk.pass.length === 0 ? 'var(--ink3)' : st.bk.pass.length < 10 ? 'var(--coral)' : st.bk.pass.length < 16 ? 'var(--amber)' : 'var(--em)',
      bkBarW: Math.min(100, st.bk.pass.length * 6) + '%',
      bkIdle: st.bk.state === 'idle', bkBusy: st.bk.state === 'preparing', bkDone: st.bk.state === 'done', bkFailed: st.bk.state === 'failed',
      bkMismatch: st.bk.confirm.length > 0 && st.bk.pass !== st.bk.confirm,
      runBackup: function () {
        if (st.bk.pass.length < 10 || st.bk.pass !== st.bk.confirm) { self.setState({ bk: Object.assign({}, st.bk, { state: 'failed' }) }); return; }
        self.setState({ bk: Object.assign({}, st.bk, { state: 'preparing' }) });
        setTimeout(function () { self.setState({ bk: Object.assign({}, self.state.bk, { state: 'done' }) }); }, 900);
      },
      retryBackup: function () { self.setState({ bk: Object.assign({}, st.bk, { state: 'idle' }) }); },
      dialogOpen: !!st.dialog, dialogIsBackup: st.dialog === 'backup', dialogIsErase: st.dialog === 'erase', dialogIsDelEntry: st.dialog === 'delete-entry',
      closeDialog: function () { self.setState({ dialog: null }); },
      confirmErase: function () {
        self.setState({ activity: [], assets: [], bates: [], defaults: [], vault: [], saved: [], conns: st.conns.map(function (c) { return Object.assign({}, c, { on: false }); }), dialog: null, vaultStage: 'locked' });
        self.toast('Everything local was erased on this device.');
      },
      pendingDelete: st.pendingDelete === undefined || st.pendingDelete === null ? '' : (st.vault[st.pendingDelete] ? st.vault[st.pendingDelete].name : ''),
      confirmDeleteEntry: function () {
        var v = st.vault.slice(); v.splice(st.pendingDelete, 1);
        self.setState({ vault: v, dialog: null, pendingDelete: null, reveal: null });
      },
      cmpRows: cmpRows,
      cmpCols: [{ label: 'PrivaTools', sub: 'Local-first, cached tools', badge: 'Verified', badgeColor: 'var(--em)', go: function () { self.go('compare'); } }].concat(this.competitorData().slice(0, 3).map(function (c) {
        return { label: c.name, sub: c.sub, badge: 'Illustrative', badgeColor: 'var(--amber)', go: function () { self.go('competitor', c.slug); } };
      })),
      cmpSlotCount: '13 competitor slots · 3 illustrative · 10 pending verification',
      priorities: [['all', 'No priority'], ['privacy', 'Privacy'], ['offline', 'Offline'], ['size', 'File size'], ['price', 'Price']].map(function (p) {
        var on = st.priority === p[0];
        return { label: p[1], bg: on ? 'var(--emSoft)' : 'var(--panel)', fg: on ? 'var(--em)' : 'var(--ink2)', bd: on ? 'var(--emLine)' : 'var(--line)', go: function () { self.setState({ priority: p[0] }); } };
      }),
      advantages: [['PrivaTools', 'Free, no account, 500 MB per file and no analytics. These are our own product rules, not a comparison claim.'], ['Reference profile A', 'Layout example only — an honest advantage sentence goes here once a source is supplied.'], ['Reference profile B', 'Layout example only — an honest advantage sentence goes here once a source is supplied.'], ['Reference profile C', 'Layout example only — an honest advantage sentence goes here once a source is supplied.']].map(function (a) { return { name: a[0], text: a[1] }; }),
      isCompetitor: st.route === 'competitor',
      cName: comp.name, cSub: comp.sub, cIllustrative: comp.illustrative, cPending: !comp.illustrative,
      cAdvantage: comp.advantage, cCol: comp.col, cAdvantageNote: 'Layout example. No competitor fact is asserted until a source is supplied.',
      cRows: comp.illustrative ? rows.map(function (r) {
        var mine = r[2][0], theirs = r[2][comp.col];
        var f = function (s) { return { icon: s === 1 ? 'check_circle' : s === 2 ? 'error_outline' : 'cancel', color: s === 1 ? 'var(--em)' : s === 2 ? 'var(--amber)' : 'var(--coral)' }; };
        return { feature: r[0], mine: r[1][0], mineIcon: f(mine).icon, mineColor: f(mine).color, theirs: r[1][comp.col], theirIcon: f(theirs).icon, theirColor: f(theirs).color, evidence: r[3] };
      }) : [],
      cSlots: this.competitorData().map(function (c) {
        var on = c.slug === comp.slug;
        return {
          name: c.name, sub: c.sub, badge: c.illustrative ? 'Illustrative' : 'Pending',
          badgeColor: c.illustrative ? 'var(--amber)' : 'var(--ink3)',
          bd: on ? 'var(--emLine)' : 'var(--line)', bg: on ? 'var(--emSoft)' : 'var(--panel2)',
          go: function () { self.go('competitor', c.slug); }
        };
      }),
      cChecked: 'Awaiting evidence', cCurrency: 'No source supplied',
      cPendingNote: 'Verified comparison data pending. No source has been supplied for this slot, so it stays empty. We do not publish invented features, prices, dates or ratings.',
      evidenceOpen: !!st.drawer2, evidenceTitle: st.drawer2 || '',
      closeEvidence: function () { self.setState({ drawer2: null }); },
      evidenceRows: [['Source', 'No source supplied'], ['Checked', 'Awaiting evidence'], ['Currency', 'No source supplied'], ['Status', 'Layout example — not a competitor claim'], ['Method', 'None yet. Verification requires a published source per row.']].map(function (r) { return { k: r[0], v: r[1] }; }),
      blogCats: cats.map(function (c) {
        var on = st.blogCat === c;
        return { label: c, bg: on ? 'var(--emSoft)' : 'var(--panel)', fg: on ? 'var(--em)' : 'var(--ink2)', bd: on ? 'var(--emLine)' : 'var(--line)', go: function () { self.setState({ blogCat: c }); } };
      }),
      posts: blogList.map(function (p) {
        return { title: p.title, cat: p.cat, date: p.date, read: p.read, excerpt: p.lead, go: function () { self.go('post', p.slug); } };
      }),
      blogEmpty: blogList.length === 0,
      blogQ: st.blogQ, onBlogQ: function (e) { self.setState({ blogQ: e.target.value }); },
      blogPending: 'One article has a complete body. The other five entries are metadata and layout samples: their titles and introductions are real, but the body below them is placeholder structure, not the article named. Nine of the fifteen planned slugs have no record yet and resolve to article-not-found.',
      clearBlog: function () { self.setState({ blogCat: 'All', blogQ: '' }); },
      postFound: !!post, postMissing: !post, postSlugRaw: st.postSlug,
      postComplete: !!(post && post.complete), postSample: !!(post && !post.complete),
      postSampleNote: 'Layout sample. The headings, table, code block and callout below are structural placeholders for this template — they are not the body of this article, which has not been written.',
      postTitle: post ? post.title : 'Article not found', postCat: post ? post.cat : '', postDate: post ? post.date : '', postRead: post ? post.read : '', postLead: post ? post.lead : '',
      postToc: [['what', 'What local-first means'], ['how', 'How the fallback is disclosed'], ['limits', 'Limits and file sizes'], ['logs', 'What we keep, and for how long']].map(function (t) { return { label: t[1] }; }),
      relatedPosts: posts.slice(0, 3).map(function (p) { return { title: p.title, date: p.date, go: function () { self.go('post', p.slug); } }; }),
      relatedTools: ['compress-pdf', 'remove-exif', 'redact-pdf'].map(function (s) { var t = self.byslug(s); return { name: t.name, purpose: t.purpose, icon: t.icon, color: t.color, go: function () { self.go('tool', s); } }; }),
      journey: [
        ['You choose a file', 'Nothing is read until you pick a source.', 'devices'],
        ['Inspected on your device', 'File type and properties are read locally.', 'search'],
        ['Processed locally when possible', 'The browser does the work with no upload.', 'developer_board'],
        ['Server only if needed, always disclosed', 'Mumbai, India. Isolated per request.', 'dns'],
        ['Deleted immediately after the response', 'No input or output document is retained.', 'delete_forever'],
        ['Result lives in this tab only', 'Until you download it or save it yourself.', 'visibility_lock']
      ].map(function (j) { return { title: j[0], text: j[1], icon: j[2] }; })
    };
  }
  basePipe() {
    return [
      { slug: 'compress-pdf', on: true, fixed: true, label: 'Add files', sub: 'From device', icon: 'note_add', mode: 'local' },
      { slug: 'compress-pdf', on: true },
      { slug: 'redact-pdf', on: true, fail: true },
      { slug: 'pdf-to-jpg', on: true },
      { slug: 'compress-pdf', on: true, fixed: true, label: 'Save output', sub: 'To device', icon: 'download', mode: 'local' }
    ];
  }
  baseBatch() {
    return [
      { name: 'report-q1.pdf', size: '4.2 MB', tool: 'Compress PDF', status: 'queued', prog: 0, step: 'Reading locally', failStep: 'Redact' },
      { name: 'invoice-042.pdf', size: '2.1 MB', tool: 'Compress PDF', status: 'queued', prog: 0, step: 'Reading locally', failStep: 'Redact' },
      { name: 'contract-draft.pdf', size: '9.8 MB', tool: 'Compress PDF', status: 'queued', prog: 0, step: 'Optimising', failStep: 'Redact', willFail: true },
      { name: 'slides-deck.pdf', size: '22.4 MB', tool: 'Compress PDF', status: 'queued', prog: 0, step: 'Optimising', failStep: 'Convert' },
      { name: 'notes.pdf', size: '1.1 MB', tool: 'Compress PDF', status: 'queued', prog: 0, step: 'Writing output', failStep: 'Convert to JPG', willFail: true },
      { name: 'photo-set.zip', size: '31.2 MB', tool: 'Compress PDF', status: 'queued', prog: 0, step: 'Extracting', failStep: 'Extract' }
    ];
  }
  loadRecipe(slugs, name) {
    var base = this.basePipe();
    var pipe = [base[0]].concat(slugs.map(function (s) { return { slug: s, on: true }; })).concat([base[base.length - 1]]);
    this.setState({ pipe: pipe, pipeSel: 1, pipeState: 'configured', pipeFailed: -1, pipeProg: 0, pipeAt: 0 });
    if (name) this.toast('Loaded “' + name + '” from this device.');
  }
  srcList(t) {
    var self = this;
    return [['Device', 'Browse files', 'devices'], ['Folder', 'Choose folder', 'folder'], ['Clipboard', 'Paste content', 'content_paste'], ['Camera', 'Take a photo', 'photo_camera'], ['Public URL', 'From the web', 'link'], ['Google Drive', 'My Drive', 'add_to_drive'], ['Dropbox', 'My files', 'inventory_2'], ['OneDrive', 'My files', 'cloud'], ['Box', 'My files', 'inbox']].map(function (s) {
      return { label: s[0], sub: s[1], icon: s[2], color: 'var(--ink2)', go: function () { self.setState({ file: { name: 'Report.pdf', size: '8.4 MB', kind: t.inp }, stage: 'configure', alert: null }); } };
    });
  }
}

/** Bridges the design's renderVals() contract to React's render(). */
Component.prototype.render = function render() {
  const v = Object.assign({}, this.props, this.renderVals());
  return (
    <>

      <div style={css("display:flex;min-height:100vh;background:var(--bg);color:var(--ink);transition:background 260ms ease,color 260ms ease")}>

        {Boolean(v.showRail) && (<>
    
      <aside role="navigation" aria-label="Primary" style={css("width:216px;flex:none;position:sticky;top:0;align-self:flex-start;height:100vh;display:flex;flex-direction:column;background:var(--bg2);border-right:1px solid var(--line)")}>
      <button onClick={v.goHome} style={css("display:flex;align-items:center;gap:10px;padding:18px 16px 16px")} aria-label="PrivaTools home">
      <svg viewBox="0 0 40 40" aria-hidden="true" style={css("width:26px;height:26px;flex:none")}>
      <g fill="none" stroke="var(--em)" strokeWidth="1.6" strokeLinejoin="round">
      <path d="M20 5 L34 13 L34 27 L20 35 L6 27 L6 13 Z"></path>
      <path d="M6 13 L20 21 L34 13"></path>
      <path d="M20 21 L20 35"></path>
      </g>
      </svg>
      <span style={css("font-size:16px;font-weight:600;letter-spacing:-.01em")}>PrivaTools</span>
      </button>
      <nav style={css("display:flex;flex-direction:column;gap:2px;padding:4px 10px;overflow-y:auto;flex:1")}>

              {(v.navMain ?? []).map((n, nI) => (
          
      <button key={nI} onClick={n.go} aria-current={n.cur} style={css(`display:flex;align-items:center;gap:10px;padding:0 10px;height:36px;border-radius:8px;font-size:13.5px;font-weight:${n.fw};color:${n.fg};background:${n.bg};border:1px solid ${n.bd};transition:background 160ms ease,color 160ms ease,border-color 160ms ease`)}>
      <span className="ms" style={css(`font-size:19px;color:${n.ic}`)}>{ICON[n.icon]}</span>
      <span style={css("flex:1;min-width:0;overflow:hidden;text-overflow:ellipsis")}>{n.label}</span>
      </button>

              ))}
      <div style={css("padding:16px 10px 6px;font-size:10.5px;letter-spacing:.1em;text-transform:uppercase;color:var(--ink3)")}>Categories</div>

              {(v.navCats ?? []).map((c, cI) => (
          
      <button key={cI} onClick={c.go} style={css(`display:flex;align-items:center;gap:10px;padding:0 10px;height:32px;border-radius:8px;font-size:13px;color:${c.fg};background:${c.bg};transition:background 160ms ease,color 160ms ease`)}>
      <span className="ms" style={css(`font-size:18px;color:${c.ic}`)}>{ICON[c.icon]}</span>
      <span style={css("flex:1;min-width:0;overflow:hidden;text-overflow:ellipsis")}>{c.label}</span>
      </button>

              ))}
      </nav>
      <div style={css("border-top:1px solid var(--line);padding:10px;display:flex;flex-direction:column;gap:2px")}>

              {(v.health ?? []).map((h, hI) => (
          
      <button key={hI} onClick={h.go} title={h.title} style={css(`display:flex;align-items:center;gap:10px;padding:7px 10px;border-radius:8px;background:${h.bg}`)}>
      <span className="ms" style={css(`font-size:18px;color:${h.color}`)}>{ICON[h.icon]}</span>
      <span style={css("display:block;flex:1;min-width:0")}>
      <span style={css("display:block;font-size:12.5px;color:var(--ink);line-height:1.3")}>{h.label}</span>
      <span style={css("display:block;font-size:11px;color:var(--ink3);line-height:1.3")}>{h.state}</span>
      </span>
      <span style={css(`width:7px;height:7px;border-radius:50%;background:${h.color};flex:none;animation:${h.pulse}`)}></span>
      </button>

              ))}
      </div>
      </aside>

        </>)}

        {Boolean(v.showIconRail) && (<>
    
      <aside role="navigation" aria-label="Primary" style={css("width:64px;flex:none;position:sticky;top:0;align-self:flex-start;height:100vh;display:flex;flex-direction:column;align-items:center;background:var(--bg2);border-right:1px solid var(--line)")}>
      <button onClick={v.goHome} aria-label="PrivaTools home" style={css("display:flex;align-items:center;justify-content:center;width:44px;height:52px")}>
      <svg viewBox="0 0 40 40" aria-hidden="true" style={css("width:24px;height:24px")}><g fill="none" stroke="var(--em)" strokeWidth="1.8" strokeLinejoin="round"><path d="M20 5 L34 13 L34 27 L20 35 L6 27 L6 13 Z"></path><path d="M6 13 L20 21 L34 13"></path><path d="M20 21 L20 35"></path></g></svg>
      </button>
      <button onClick={v.railExpand} aria-label="Expand navigation" style={css("display:flex;align-items:center;justify-content:center;width:40px;height:36px;border-radius:9px;border:1px solid var(--line);background:var(--panel)")}><span className="ms" style={css("font-size:18px;color:var(--ink3)")}>{ICON.right_panel_open}</span></button>
      <nav style={css("display:flex;flex-direction:column;gap:3px;padding:10px 0;overflow-y:auto;flex:1")}>

              {(v.navMain ?? []).map((n, nI) => (
          
      <button key={nI} onClick={n.go} aria-current={n.cur} title={n.label} aria-label={n.label} style={css(`display:flex;align-items:center;justify-content:center;width:44px;height:40px;border-radius:9px;background:${n.bg};border:1px solid ${n.bd};transition:background 160ms ease`)}>
      <span className="ms" style={css(`font-size:20px;color:${n.ic}`)}>{ICON[n.icon]}</span>
      </button>

              ))}
      </nav>
      <div style={css("border-top:1px solid var(--line);padding:8px 0;display:flex;flex-direction:column;gap:4px;align-items:center")}>

              {(v.health ?? []).map((h, hI) => (
          
      <button key={hI} onClick={h.go} title={`${h.label} — ${h.state}`} aria-label={`${h.label} — ${h.state}`} style={css("position:relative;display:flex;align-items:center;justify-content:center;width:40px;height:34px;border-radius:8px")}>
      <span className="ms" style={css(`font-size:18px;color:${h.color}`)}>{ICON[h.icon]}</span>
      <span aria-hidden="true" style={css(`position:absolute;top:5px;right:5px;width:6px;height:6px;border-radius:50%;background:${h.color}`)}></span>
      </button>

              ))}
      </div>
      </aside>

        </>)}
      <div style={css("flex:1;min-width:0;display:flex;flex-direction:column")}>
      <p aria-live="polite" aria-atomic="true" style={css("position:absolute;width:1px;height:1px;overflow:hidden;clip:rect(0 0 0 0);white-space:nowrap")}>{v.live}</p>
      <header style={css("position:sticky;top:0;z-index:30;display:flex;align-items:center;gap:12px;padding:10px 20px;min-height:56px;background:color-mix(in srgb,var(--bg) 88%,transparent);backdrop-filter:blur(10px);border-bottom:1px solid var(--line)")}>

            {Boolean(v.isMobile) && (<>
        
      <button onClick={v.openDrawer} aria-label="Open menu" style={css("display:flex;align-items:center;justify-content:center;width:44px;height:44px;margin-left:-10px;border-radius:10px")}><span className="ms" style={css("font-size:22px")}>{ICON.menu}</span></button>
      <span style={css("display:flex;align-items:center;gap:8px;font-size:15px;font-weight:600")}>
      <svg viewBox="0 0 40 40" aria-hidden="true" style={css("width:22px;height:22px")}><g fill="none" stroke="var(--em)" strokeWidth="1.8" strokeLinejoin="round"><path d="M20 5 L34 13 L34 27 L20 35 L6 27 L6 13 Z"></path><path d="M6 13 L20 21 L34 13"></path><path d="M20 21 L20 35"></path></g></svg>
                PrivaTools
              </span>

            </>)}

            {Boolean(v.crumbShown) && (<>
        
      <div style={css("display:flex;align-items:center;gap:8px;min-width:0;flex:0 1 auto;overflow:hidden")}>
      <button onClick={v.back} aria-label="Back" style={css("display:flex;align-items:center;justify-content:center;width:32px;height:32px;border-radius:8px;border:1px solid var(--line);background:var(--panel)")}><span className="ms" style={css("font-size:18px;color:var(--ink2)")}>{ICON.arrow_back}</span></button>
      <span style={css("font-size:13px;color:var(--ink3);white-space:nowrap;overflow:hidden;text-overflow:ellipsis")}>{v.crumbParent}</span>
      <span className="ms" style={css("font-size:16px;color:var(--ink3)")}>{ICON.chevron_right}</span>
      <span style={css("font-size:13px;color:var(--ink);white-space:nowrap;overflow:hidden;text-overflow:ellipsis")}>{v.crumbCurrent}</span>
      </div>

            </>)}

            {Boolean(v.searchInBar) && (<>
        
      <button onClick={v.openPalette} style={css("flex:1;max-width:520px;display:flex;align-items:center;gap:8px;height:36px;padding:0 10px;border:1px solid var(--line);border-radius:9px;background:var(--panel);color:var(--ink3);transition:border-color 160ms ease")} className="dc-i1">
      <span className="ms" style={css("font-size:18px")}>{ICON.search}</span>
      <span style={css("flex:1;min-width:0;font-size:13px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;text-align:left")}>Search tools, files and actions</span>
      <kbd style={css("font-family:'Geist Mono',ui-monospace,monospace;font-size:10.5px;padding:2px 5px;border:1px solid var(--line);border-radius:5px;color:var(--ink3)")}>⌘K</kbd>
      </button>

            </>)}
      <div style={css("flex:1")}></div>
      <div style={css("display:flex;align-items:center;gap:8px")}>

              {Boolean(v.notMobile) && (<>
          
      <div role="group" aria-label="Theme" style={css("display:flex;align-items:center;gap:2px;padding:2px;border:1px solid var(--line);border-radius:9px;background:var(--panel)")}>

                  {(v.themes ?? []).map((t, tI) => (
              
      <button key={tI} onClick={t.go} aria-pressed={t.on} title={t.title} style={css(`display:flex;align-items:center;justify-content:center;width:30px;height:26px;border-radius:7px;background:${t.bg};color:${t.fg};transition:background 200ms ease,color 200ms ease`)}><span className="ms" style={css("font-size:17px")}>{ICON[t.icon]}</span></button>

                  ))}
      </div>

              </>)}
      <button onClick={v.goStatus} style={css("display:flex;align-items:center;gap:7px;height:30px;padding:0 10px;border:1px solid var(--line);border-radius:99px;background:var(--panel);font-size:12px;color:var(--ink2)")}>
      <span style={css("width:7px;height:7px;border-radius:50%;background:var(--em)")}></span>
                Local
              </button>
      </div>
      </header>
      <main id="routeMain" style={css("flex:1;min-width:0;padding:0 0 96px")}>

            {Boolean(v.isHome) && (<>
        
      <div style={css("animation:slidein 200ms ease both")}>
      <section style={css("position:relative;overflow:hidden;border-bottom:1px solid var(--line);background:linear-gradient(180deg,var(--bg2),var(--bg))")}>
      <div aria-hidden="true" style={css("position:absolute;inset:0;opacity:var(--wave);pointer-events:none")}>
      <svg viewBox="0 0 1400 220" preserveAspectRatio="none" style={css("position:absolute;inset:0;width:130%;height:100%")}>
      <g fill="none" stroke="var(--em)" strokeWidth="1">
      <path d="M-40 150 C 220 70 420 190 700 130 S 1120 40 1440 110" opacity=".5" style={css("animation:drift 17s linear infinite alternate")}></path>
      <path d="M-40 172 C 240 100 460 208 720 152 S 1140 66 1440 134" opacity=".34" style={css("animation:drift 21s linear infinite alternate-reverse")}></path>
      <path d="M-40 128 C 200 52 400 168 680 104 S 1100 20 1440 88" opacity=".22" style={css("animation:drift 14s linear infinite alternate")}></path>
      <path d="M-40 196 C 260 130 480 226 740 176 S 1160 94 1440 158" opacity=".16" style={css("animation:drift 25s linear infinite alternate-reverse")}></path>
      </g>
      </svg>
      </div>
      <div style={css("position:relative;max-width:1180px;margin:0 auto;padding:clamp(28px,4vw,44px) 24px 26px;display:flex;flex-wrap:wrap;align-items:center;gap:clamp(16px,3vw,32px)")}>
      <div style={css("flex:1 1 260px;min-width:0")}>
      <h1 style={css("font-size:clamp(30px,5.6vw,44px);line-height:1.05;font-weight:600;letter-spacing:-.03em")}>PrivaTools</h1>
      <p style={css("margin-top:8px;font-size:clamp(16px,2.6vw,19px);color:var(--ink2)")}>Work with files. Keep them <span style={css("color:var(--em);font-weight:500")}>private.</span></p>
      <p style={css("margin-top:10px;font-size:13px;color:var(--ink3);max-width:52ch;text-wrap:pretty")}>200+ free file tools. Local-first in your browser, with disclosed server fallback. No account, no monetisation, no analytics.</p>
      </div>
      <div id="cubeMark" style={css("width:clamp(92px,18vw,150px);height:clamp(74px,14vw,120px);flex:0 0 auto;transition:transform 220ms ease")}>
      <svg viewBox="0 0 160 130" aria-hidden="true" style={css("width:100%;height:100%")}>
      <g fill="none" stroke="var(--em)" strokeWidth="1.5" strokeLinejoin="round">
      <path d="M80 16 L128 42 L128 92 L80 118 L32 92 L32 42 Z"></path>
      <path d="M32 42 L80 68 L128 42"></path>
      <path d="M80 68 L80 118"></path>
      <path d="M44 34 L62 34 L68 42 L104 42" opacity=".6"></path>
      </g>
      </svg>
      </div>
      </div>
      <div style={css("position:relative;max-width:1180px;margin:0 auto;padding:0 24px 24px")}>
      <ul style={css("display:grid;grid-template-columns:repeat(auto-fit,minmax(148px,1fr));gap:8px")}>

                      {(v.trust ?? []).map((t, tI) => (
                  
      <li key={tI} style={css("display:flex;align-items:flex-start;gap:9px;padding:10px 11px;border:1px solid var(--line);border-radius:10px;background:var(--panel);box-shadow:var(--shadow)")}>
      <span className="ms" style={css(`font-size:19px;color:${t.color};margin-top:1px`)}>{ICON[t.icon]}</span>
      <span style={css("display:block;min-width:0")}>
      <span style={css("display:block;font-size:12.5px;font-weight:500;line-height:1.25")}>{t.label}</span>
      <span style={css("display:block;font-size:11px;color:var(--ink3);line-height:1.3;margin-top:2px")}>{t.sub}</span>
      </span>
      </li>

                      ))}
      </ul>
      </div>
      </section>
      <div style={css("max-width:1180px;margin:0 auto;padding:20px 24px 0")}>

                  {Boolean(v.returning) && (<>
              
      <div style={css("display:flex;align-items:center;gap:10px;padding:9px 12px;margin-bottom:14px;border:1px solid var(--line);border-radius:10px;background:var(--panel);font-size:12.5px;color:var(--ink2)")}>
      <span className="ms" style={css("font-size:18px;color:var(--em)")}>{ICON.history}</span>
      <span style={css("flex:1")}>Welcome back. Your history holds tool, date and settings only — never files or filenames.</span>
      <button onClick={v.firstVisit} style={css("font-size:12px;color:var(--em)")}>Show first-visit state</button>
      </div>

                  </>)}
      <section aria-label="Add files" style={css("border:1px solid var(--line);border-radius:14px;background:var(--panel);overflow:hidden")}>
      <div onDragOver={v.onDragOver} onDragLeave={v.onDragLeave} onDrop={v.onDrop} style={css(`position:relative;margin:14px;border:1.5px dashed ${v.dzBorder};border-radius:12px;background:${v.dzBg};transition:border-color 180ms ease,background 180ms ease`)}>

                      {Boolean(v.dzScanning) && (<>
                  
      <div aria-hidden="true" style={css("position:absolute;left:8px;right:8px;top:8px;height:1px;background:linear-gradient(90deg,transparent,var(--em),transparent);animation:scanline 1.1s ease-in-out infinite")}></div>

                      </>)}
      <div style={css("padding:26px 18px 18px;text-align:center")}>
      <span className="ms" style={css("font-size:38px;color:var(--em);font-variation-settings:'wght' 200")}>{ICON.folder_open}</span>
      <h2 style={css("margin-top:8px;font-size:18px;font-weight:500")}>{v.dzTitle}</h2>
      <p style={css("margin-top:4px;font-size:12.5px;color:var(--ink3)")}>{v.dzSub}</p>
      </div>
      <ul style={css("display:grid;grid-template-columns:repeat(auto-fit,minmax(112px,1fr));gap:8px;padding:0 14px 16px")}>

                        {(v.sources ?? []).map((s, sI) => (
                    
      <li key={sI}>
      <button onClick={s.go} style={css("width:100%;display:flex;flex-direction:column;align-items:flex-start;gap:2px;min-height:56px;padding:9px 10px;border:1px solid var(--line);border-radius:10px;background:var(--panel2);transition:border-color 160ms ease,transform 160ms ease")} className="dc-i2">
      <span className="ms" style={css(`font-size:18px;color:${s.color}`)}>{ICON[s.icon]}</span>
      <span style={css("font-size:12.5px;font-weight:500")}>{s.label}</span>
      <span style={css("font-size:10.5px;color:var(--ink3)")}>{s.sub}</span>
      </button>
      </li>

                        ))}
      </ul>
      <div style={css("display:flex;flex-wrap:wrap;gap:12px;align-items:center;justify-content:center;padding:0 14px 16px;font-size:11.5px;color:var(--ink3)")}>
      <span>Max file size 500 MB — every tool, every file type</span>
      <span style={css("color:var(--line2)")}>•</span>
      <span>No document retention</span>
      </div>
      </div>
      </section>

                  {Boolean(v.hasFile) && (<>
              
      <section style={css("display:grid;grid-template-columns:repeat(auto-fit,minmax(320px,1fr));gap:14px;margin-top:14px;animation:rise 200ms ease both")}>
      <div style={css("border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:16px")}>
      <h3 style={css("font-size:15px;font-weight:500")}>Inspect file locally</h3>
      <p style={css("margin-top:4px;font-size:12.5px;color:var(--ink3);max-width:44ch")}>We read and analyse the file on your device. No upload. No sharing.</p>
      <div style={css("display:flex;gap:14px;margin-top:14px;align-items:flex-start")}>
      <ul style={css("flex:1;display:flex;flex-direction:column;gap:9px")}>

                            {(v.inspect ?? []).map((i, iI) => (
                        
      <li key={iI} style={css(`display:flex;align-items:center;gap:8px;font-size:12.5px;color:${i.fg}`)}>
      <span className="ms" style={css(`font-size:17px;color:${i.color}`)}>{ICON[i.icon]}</span>
      <span style={css("flex:1")}>{i.label}</span>
      <span style={css("font-family:'Geist Mono',ui-monospace,monospace;font-size:11px;color:var(--ink3)")}>{i.value}</span>
      </li>

                            ))}
      </ul>
      <div aria-hidden="true" style={css("width:92px;height:74px;flex:none;border:1px solid var(--line);border-radius:8px;background:var(--panel2);display:flex;align-items:center;justify-content:center")}>
      <span className="ms" style={css("font-size:26px;color:var(--ink3)")}>{ICON.description}</span>
      </div>
      </div>
      <p style={css("margin-top:12px;padding-top:10px;border-top:1px solid var(--line);font-size:11.5px;color:var(--ink3)")}>You can review details before choosing an action. Nothing runs automatically.</p>
      </div>
      <div style={css("border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:16px")}>
      <div style={css("display:flex;align-items:baseline;justify-content:space-between;gap:10px")}>
      <h3 style={css("font-size:15px;font-weight:500")}>Suggested actions</h3>
      <span style={css("font-size:11.5px;color:var(--ink3)")}>Compatible with this file</span>
      </div>
      <ul style={css("display:grid;grid-template-columns:repeat(auto-fit,minmax(150px,1fr));gap:8px;margin-top:12px")}>

                          {(v.suggested ?? []).map((s, sI) => (
                      
      <li key={sI}>
      <button onClick={s.go} style={css("width:100%;display:flex;align-items:center;gap:9px;padding:9px 10px;border:1px solid var(--line);border-radius:10px;background:var(--panel2);transition:border-color 160ms ease,background 160ms ease")} className="dc-i3">
      <span className="ms" style={css(`font-size:19px;color:${s.color}`)}>{ICON[s.icon]}</span>
      <span style={css("display:block;min-width:0;flex:1")}>
      <span style={css("display:block;font-size:12.5px;font-weight:500;white-space:nowrap;overflow:hidden;text-overflow:ellipsis")}>{s.name}</span>
      <span style={css("display:block;font-size:10.5px;color:var(--ink3);white-space:nowrap;overflow:hidden;text-overflow:ellipsis")}>{s.purpose}</span>
      </span>
      <span className="ms" style={css("font-size:16px;color:var(--ink3)")}>{ICON.chevron_right}</span>
      </button>
      </li>

                          ))}
      </ul>
      <button onClick={v.goTools} style={css("margin-top:10px;font-size:12.5px;color:var(--em)")}>See all 200+ tools →</button>
      </div>
      </section>

                  </>)}
      <section style={css("margin-top:22px")}>
      <div style={css("display:flex;align-items:baseline;justify-content:space-between;gap:10px")}>
      <h2 style={css("font-size:15px;font-weight:500")}>Popular tools</h2>
      <button onClick={v.goTools} style={css("font-size:12.5px;color:var(--em)")}>View all</button>
      </div>
      <ul style={css("display:grid;grid-template-columns:repeat(auto-fit,minmax(158px,1fr));gap:10px;margin-top:10px")}>

                      {(v.popular ?? []).map((p, pI) => (
                  
      <li key={pI}>
      <button onClick={p.go} style={css("width:100%;height:100%;display:flex;flex-direction:column;gap:7px;padding:12px;border:1px solid var(--line);border-radius:12px;background:var(--panel);box-shadow:var(--shadow);transition:border-color 160ms ease,transform 160ms ease")} className="dc-i4">
      <span style={css(`display:flex;align-items:center;justify-content:center;width:30px;height:30px;border-radius:8px;background:${p.tint}`)}><span className="ms" style={css(`font-size:19px;color:${p.color}`)}>{ICON[p.icon]}</span></span>
      <span style={css("font-size:13px;font-weight:500")}>{p.name}</span>
      <span style={css("font-size:11px;color:var(--ink3);line-height:1.35")}>{p.purpose}</span>
      <span style={css(`margin-top:auto;display:inline-flex;align-items:center;gap:5px;font-size:10.5px;color:${p.modeColor}`)}><span style={css(`width:6px;height:6px;border-radius:50%;background:${p.modeColor}`)}></span>{p.modeLabel}</span>
      </button>
      </li>

                      ))}
      </ul>
      </section>
      <section style={css("display:grid;grid-template-columns:repeat(auto-fit,minmax(300px,1fr));gap:14px;margin-top:22px")}>
      <div style={css("border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:16px")}>
      <div style={css("display:flex;align-items:baseline;justify-content:space-between;gap:10px")}>
      <h2 style={css("font-size:15px;font-weight:500")}>Explore by category</h2>
      <button onClick={v.goTools} style={css("font-size:12.5px;color:var(--em)")}>View all tools</button>
      </div>
      <ul style={css("display:grid;grid-template-columns:repeat(auto-fit,minmax(128px,1fr));gap:8px;margin-top:12px")}>

                        {(v.cats ?? []).map((c, cI) => (
                    
      <li key={cI}>
      <button onClick={c.go} style={css("width:100%;display:flex;align-items:center;gap:8px;min-height:44px;padding:8px 10px;border:1px solid var(--line);border-radius:10px;background:var(--panel2);transition:border-color 160ms ease")} className="dc-i5">
      <span className="ms" style={css(`font-size:18px;color:${c.color}`)}>{ICON[c.icon]}</span>
      <span style={css("display:block;min-width:0")}>
      <span style={css("display:block;font-size:12.5px;font-weight:500")}>{c.label}</span>
      <span style={css("display:block;font-size:10.5px;color:var(--ink3)")}>{c.count}</span>
      </span>
      </button>
      </li>

                        ))}
      </ul>
      </div>
      <div style={css("display:flex;flex-direction:column;gap:14px")}>
      <div style={css("display:grid;grid-template-columns:repeat(auto-fit,minmax(140px,1fr));gap:10px")}>
      <button onClick={v.goPipeline} style={css("display:flex;flex-direction:column;gap:6px;padding:14px;border:1px solid var(--line);border-radius:12px;background:var(--panel);transition:border-color 160ms ease")} className="dc-i6">
      <span className="ms" style={css("font-size:20px;color:var(--em)")}>{ICON.account_tree}</span>
      <span style={css("font-size:13px;font-weight:500")}>Pipeline</span>
      <span style={css("font-size:11px;color:var(--ink3);line-height:1.35")}>Chain tools in order and run one file through the whole chain.</span>
      </button>
      <button onClick={v.goBatch} style={css("display:flex;flex-direction:column;gap:6px;padding:14px;border:1px solid var(--line);border-radius:12px;background:var(--panel);transition:border-color 160ms ease")} className="dc-i7">
      <span className="ms" style={css("font-size:20px;color:var(--blue)")}>{ICON.grid_view}</span>
      <span style={css("font-size:13px;font-weight:500")}>Batch</span>
      <span style={css("font-size:11px;color:var(--ink3);line-height:1.35")}>Run one tool across many files with per-item status.</span>
      </button>
      </div>
      <div style={css("border:1px solid var(--line);border-radius:12px;background:var(--panel);padding:14px")}>
      <div style={css("display:flex;align-items:center;gap:10px")}>
      <span className="ms" style={css("font-size:20px;color:var(--em)")}>{ICON.install_mobile}</span>
      <span style={css("display:block;flex:1;min-width:0")}>
      <span style={css("display:block;font-size:13px;font-weight:500")}>Install PrivaTools</span>
      <span style={css("display:block;font-size:11px;color:var(--ink3)")}>Works offline as an app. Cached local tools keep working with no connection.</span>
      </span>
      <button onClick={v.openInstall} style={css("flex:none;height:32px;padding:0 14px;border-radius:8px;background:var(--em);color:var(--em-ink,#04231A);font-size:12.5px;font-weight:500")}>Install</button>
      </div>
      </div>
      <div style={css("border:1px solid var(--line);border-radius:12px;background:var(--panel);padding:6px")}>

                        {(v.health ?? []).map((h, hI) => (
                    
      <div key={hI} style={css("display:flex;align-items:center;gap:10px;padding:9px 10px")}>
      <span className="ms" style={css(`font-size:18px;color:${h.color}`)}>{ICON[h.icon]}</span>
      <span style={css("flex:1;font-size:12.5px")}>{h.label}</span>
      <span style={css(`font-size:11.5px;color:${h.color}`)}>{h.state}</span>
      </div>

                        ))}
      </div>
      </div>
      </section>

                  {Boolean(v.returning) && (<>
              
      <section style={css("margin-top:22px;border:1px solid var(--line);border-radius:14px;background:var(--panel);overflow:hidden")}>
      <div style={css("display:flex;align-items:baseline;justify-content:space-between;gap:10px;padding:14px 16px 10px")}>
      <h2 style={css("font-size:15px;font-weight:500")}>Recent activity</h2>
      <span style={css("font-size:11.5px;color:var(--ink3)")}>Tool, date and settings only — no filenames</span>
      </div>
      <ul>

                        {(v.recentRows ?? []).map((r, rI) => (
                    
      <li key={rI} style={css("display:flex;align-items:center;gap:12px;padding:10px 16px;border-top:1px solid var(--line)")}>
      <span className="ms" style={css(`font-size:18px;color:${r.color}`)}>{ICON[r.icon]}</span>
      <button onClick={r.go} style={css("flex:1;min-width:0;font-size:13px")}>{r.name}</button>
      <span style={css("font-size:11.5px;color:var(--ink3);font-family:'Geist Mono',ui-monospace,monospace")}>{r.date}</span>
      <span style={css("font-size:11.5px;color:var(--ink2);max-width:38%;white-space:nowrap;overflow:hidden;text-overflow:ellipsis")}>{r.settings}</span>
      </li>

                        ))}
      </ul>
      </section>

                  </>)}
      </div>
      </div>

            </>)}

            {Boolean(v.isTools) && (<>
        
      <div style={css("animation:slidein 200ms ease both")}>
      <div style={css("max-width:1180px;margin:0 auto;padding:20px 24px 0")}>
      <div style={css("display:flex;flex-wrap:wrap;align-items:flex-end;gap:12px")}>
      <div style={css("flex:1;min-width:200px")}>
      <h1 style={css("font-size:clamp(21px,3.6vw,26px);font-weight:600;letter-spacing:-.02em")}>All Tools</h1>
      <p style={css("margin-top:3px;font-size:12.5px;color:var(--ink3)")}>200+ free file tools · {v.toolCount} shown</p>
      </div>
      <div style={css("display:flex;align-items:center;gap:8px")}>
      <div style={css("display:flex;align-items:center;gap:8px;height:36px;padding:0 10px;border:1px solid var(--line);border-radius:9px;background:var(--panel);min-width:220px")}>
      <span className="ms" style={css("font-size:18px;color:var(--ink3)")}>{ICON.search}</span>
      <input value={v.q} onChange={v.onQ} placeholder="Search 200+ tools" aria-label="Search tools" style={css("flex:1;min-width:0;border:0;background:none;outline:none;font-size:13px")} />

                        {Boolean(v.hasQ) && (<>
                          <button onClick={v.clearQ} aria-label="Clear search" style={css("display:flex")}><span className="ms" style={css("font-size:16px;color:var(--ink3)")}>{ICON.close}</span></button>
                        </>)}
      </div>

                      {Boolean(v.isMobile) && (<>
                  
      <button onClick={v.openFilters} style={css("display:flex;align-items:center;gap:6px;height:44px;padding:0 12px;border:1px solid var(--line);border-radius:9px;background:var(--panel);font-size:13px")}><span className="ms" style={css("font-size:18px")}>{ICON.tune}</span>Filters</button>

                      </>)}
      <div role="group" aria-label="View mode" style={css("display:flex;gap:2px;padding:2px;border:1px solid var(--line);border-radius:9px;background:var(--panel)")}>
      <button onClick={v.setList} aria-pressed={v.isList} title="List view" style={css(`display:flex;align-items:center;justify-content:center;width:32px;height:28px;border-radius:7px;background:${v.listBg};color:${v.listFg}`)}><span className="ms" style={css("font-size:18px")}>{ICON.view_list}</span></button>
      <button onClick={v.setCards} aria-pressed={v.isCards} title="Card view" style={css(`display:flex;align-items:center;justify-content:center;width:32px;height:28px;border-radius:7px;background:${v.cardsBg};color:${v.cardsFg}`)}><span className="ms" style={css("font-size:18px")}>{ICON.grid_view}</span></button>
      </div>
      </div>
      </div>
      <ul style={css("display:grid;grid-template-columns:repeat(auto-fit,minmax(132px,1fr));gap:6px;margin-top:14px")}>

                    {(v.trust ?? []).map((t, tI) => (
                
      <li key={tI} style={css("display:flex;align-items:center;gap:7px;padding:7px 9px;border:1px solid var(--line);border-radius:9px;background:var(--panel)")}>
      <span className="ms" style={css(`font-size:16px;color:${t.color}`)}>{ICON[t.icon]}</span>
      <span style={css("display:block;min-width:0")}><span style={css("display:block;font-size:11.5px;font-weight:500;white-space:nowrap;overflow:hidden;text-overflow:ellipsis")}>{t.label}</span><span style={css("display:block;font-size:10px;color:var(--ink3);white-space:nowrap;overflow:hidden;text-overflow:ellipsis")}>{t.sub}</span></span>
      </li>

                    ))}
      </ul>
      <div style={css("display:flex;flex-wrap:wrap;gap:14px;margin-top:16px;align-items:flex-start")}>
      <div style={css("flex:3 1 460px;min-width:0")}>
      <div style={css("border:1px solid var(--line);border-radius:14px;background:var(--panel);overflow:hidden")}>
      <div style={css("padding:13px 14px 11px;border-bottom:1px solid var(--line)")}>
      <div style={css("display:flex;align-items:baseline;justify-content:space-between;gap:10px")}>
      <h2 style={css("font-size:14px;font-weight:500;white-space:nowrap")}>Browse by family and task</h2>

                            {Boolean(v.filtersActive) && (<>
                              <button onClick={v.clearFilters} style={css("font-size:12px;color:var(--em)")}>Clear filters</button>
                            </>)}
      </div>
      <ul style={css("display:flex;flex-wrap:wrap;gap:6px;margin-top:10px")}>

                            {(v.famChips ?? []).map((c, cI) => (
                        
      <li key={cI}><button onClick={c.go} style={css(`height:28px;padding:0 11px;border:1px solid ${c.bd};border-radius:99px;background:${c.bg};color:${c.fg};font-size:12px;white-space:nowrap;transition:background 160ms ease,border-color 160ms ease`)}>{c.label}</button></li>

                            ))}
      </ul>
      </div>

                        {Boolean(v.noMatch) && (<>
                    
      <div style={css("padding:40px 20px;text-align:center")}>
      <span className="ms" style={css("font-size:32px;color:var(--ink3)")}>{ICON.search_off}</span>
      <h3 style={css("margin-top:8px;font-size:15px;font-weight:500")}>No tool matches these filters</h3>
      <p style={css("margin-top:5px;font-size:12.5px;color:var(--ink3)")}>We only list tools that exist. Try clearing a filter or searching a different task.</p>
      <button onClick={v.clearFilters} style={css("margin-top:12px;height:34px;padding:0 14px;border-radius:8px;background:var(--em);color:var(--em-ink,#04231A);font-size:12.5px;font-weight:500")}>Clear all filters</button>
      </div>

                        </>)}

                        {Boolean(v.showTable) && (<>
                    
      <table style={css("width:100%;font-size:13px")}>
      <thead>
      <tr style={css("text-align:left;color:var(--ink3)")}>
      <th scope="col" style={css("font-weight:400;font-size:11px;letter-spacing:.06em;text-transform:uppercase;padding:9px 14px;width:32%")}>Tool</th>
      <th scope="col" style={css("font-weight:400;font-size:11px;letter-spacing:.06em;text-transform:uppercase;padding:9px 8px")}>Description</th>
      <th scope="col" style={css("font-weight:400;font-size:11px;letter-spacing:.06em;text-transform:uppercase;padding:9px 8px;width:56px")}>Local</th>
      <th scope="col" style={css("font-weight:400;font-size:11px;letter-spacing:.06em;text-transform:uppercase;padding:9px 8px;width:60px")}>Server</th>
      <th scope="col" style={css("font-weight:400;font-size:11px;letter-spacing:.06em;text-transform:uppercase;padding:9px 8px;width:60px")}>Offline</th>
      <th scope="col" style={css("padding:9px 14px;width:70px")}><span style={css("position:absolute;width:1px;height:1px;overflow:hidden;clip:rect(0 0 0 0)")}>Actions</span></th>
      </tr>
      </thead>
      <tbody>

                              {(v.rows ?? []).map((r, rI) => (
                          
      <tr key={rI} onMouseEnter={r.hover} style={css(`border-top:1px solid var(--line);background:${r.bg};transition:background 140ms ease`)}>
      <td style={css("padding:0 14px")}>
      <button onClick={r.go} style={css("display:flex;align-items:center;gap:9px;height:var(--rowH);width:100%;font-size:13px")}>
      <span style={css(`display:flex;align-items:center;justify-content:center;width:24px;height:24px;border-radius:6px;background:${r.tint};flex:none`)}><span className="ms" style={css(`font-size:16px;color:${r.color}`)}>{ICON[r.icon]}</span></span>
      <span style={css("white-space:nowrap;overflow:hidden;text-overflow:ellipsis")}>{r.name}</span>
      </button>
      </td>
      <td style={css("padding:0 8px;color:var(--ink3);font-size:12.5px")}>{r.purpose}</td>
      <td style={css("padding:0 8px")}><span title={r.localTitle} style={css(`display:inline-block;width:8px;height:8px;border-radius:50%;background:${r.dotLocal}`)}></span></td>
      <td style={css("padding:0 8px")}><span title={r.serverTitle} style={css(`display:inline-block;width:8px;height:8px;border-radius:50%;background:${r.dotServer}`)}></span></td>
      <td style={css("padding:0 8px")}><span title={r.offlineTitle} style={css(`display:inline-block;width:8px;height:8px;border-radius:50%;background:${r.dotOffline}`)}></span></td>
      <td style={css("padding:0 14px")}>
      <span style={css("display:flex;align-items:center;gap:4px;justify-content:flex-end")}>
      <button onClick={r.toggleFav} aria-label={r.favLabel} style={css("display:flex;align-items:center;justify-content:center;width:28px;height:28px;border-radius:7px")}><span className="ms" style={css(`font-size:17px;color:${r.favColor}`)}>{ICON[r.favIcon]}</span></button>
      <button onClick={r.go} aria-label={`Open ${r.name}`} style={css("display:flex;align-items:center;justify-content:center;width:28px;height:28px;border-radius:7px")}><span className="ms" style={css("font-size:17px;color:var(--ink3)")}>{ICON.chevron_right}</span></button>
      </span>
      </td>
      </tr>

                              ))}
      </tbody>
      </table>

                        </>)}

                        {Boolean(v.showCards) && (<>
                    
      <ul style={css("display:grid;grid-template-columns:repeat(auto-fit,minmax(184px,1fr));gap:10px;padding:14px")}>

                            {(v.rows ?? []).map((r, rI) => (
                        
      <li key={rI}>
      <button onClick={r.go} style={css("width:100%;height:100%;display:flex;flex-direction:column;gap:6px;padding:12px;border:1px solid var(--line);border-radius:12px;background:var(--panel2);transition:border-color 160ms ease")} className="dc-i8">
      <span style={css("display:flex;align-items:center;gap:8px;width:100%")}>
      <span style={css(`display:flex;align-items:center;justify-content:center;width:28px;height:28px;border-radius:7px;background:${r.tint}`)}><span className="ms" style={css(`font-size:17px;color:${r.color}`)}>{ICON[r.icon]}</span></span>
      <span style={css("flex:1;font-size:13px;font-weight:500;min-width:0;white-space:nowrap;overflow:hidden;text-overflow:ellipsis")}>{r.name}</span>
      </span>
      <span style={css("font-size:11.5px;color:var(--ink3);line-height:1.4")}>{r.purpose}</span>
      <span style={css("margin-top:auto;display:flex;align-items:center;gap:8px;font-size:10.5px;color:var(--ink3)")}>
      <span style={css("font-family:'Geist Mono',ui-monospace,monospace")}>{r.io}</span>
      <span style={css(`margin-left:auto;display:inline-flex;align-items:center;gap:4px;color:${r.modeColor}`)}><span style={css(`width:6px;height:6px;border-radius:50%;background:${r.modeColor}`)}></span>{r.modeLabel}</span>
      </span>
      </button>
      </li>

                            ))}
      </ul>

                        </>)}
      <div style={css("padding:11px 14px;border-top:1px solid var(--line);font-size:11.5px;color:var(--ink3)")}>{v.catalogueNote}</div>
      </div>
      <div style={css("display:flex;flex-wrap:wrap;gap:14px;margin-top:14px")}>

                        {Boolean(v.hasFavs) && (<>
                    
      <div style={css("flex:1 1 260px;border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:13px 14px")}>
      <h3 style={css("font-size:13px;font-weight:500;display:flex;align-items:center;gap:7px")}><span className="ms" style={css("font-size:17px;color:var(--amber)")}>{ICON.star}</span>Favourites</h3>
      <ul style={css("margin-top:9px;display:flex;flex-direction:column;gap:6px")}>

                              {(v.favRows ?? []).map((r, rI) => (
                          
      <li key={rI}><button onClick={r.go} style={css("display:flex;align-items:center;gap:8px;width:100%;font-size:12.5px")}><span className="ms" style={css(`font-size:16px;color:${r.color}`)}>{ICON[r.icon]}</span><span style={css("flex:1")}>{r.name}</span><span className="ms" style={css("font-size:15px;color:var(--ink3)")}>{ICON.chevron_right}</span></button></li>

                              ))}
      </ul>
      </div>

                        </>)}
      <div style={css("flex:1 1 260px;border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:13px 14px")}>
      <h3 style={css("font-size:13px;font-weight:500;display:flex;align-items:center;gap:7px")}><span className="ms" style={css("font-size:17px;color:var(--em)")}>{ICON.history}</span>Recent</h3>
      <ul style={css("margin-top:9px;display:flex;flex-direction:column;gap:6px")}>

                            {(v.recentTools ?? []).map((r, rI) => (
                        
      <li key={rI}><button onClick={r.go} style={css("display:flex;align-items:center;gap:8px;width:100%;font-size:12.5px")}><span className="ms" style={css(`font-size:16px;color:${r.color}`)}>{ICON[r.icon]}</span><span style={css("flex:1")}>{r.name}</span><span className="ms" style={css("font-size:15px;color:var(--ink3)")}>{ICON.chevron_right}</span></button></li>

                            ))}
      </ul>
      </div>
      </div>
      </div>
      <aside style={css("flex:1 1 272px;min-width:0;display:flex;flex-direction:column;gap:14px")}>
      <div style={css("border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:13px 14px")}>
      <h3 style={css("font-size:13px;font-weight:500")}>Quick search</h3>
      <button onClick={v.openPalette} style={css("display:flex;align-items:center;gap:8px;width:100%;height:32px;margin-top:9px;padding:0 9px;border:1px solid var(--line);border-radius:8px;background:var(--panel2);color:var(--ink3);font-size:12.5px")}>
      <span className="ms" style={css("font-size:16px")}>{ICON.search}</span><span style={css("flex:1")}>Search tools or commands</span><kbd style={css("font-family:'Geist Mono',ui-monospace,monospace;font-size:10px")}>⌘K</kbd>
      </button>
      <ul style={css("margin-top:10px;display:flex;flex-direction:column;gap:1px")}>

                          {(v.quickSearches ?? []).map((s, sI) => (
                      
      <li key={sI}><button onClick={s.go} style={css("display:flex;align-items:center;gap:8px;width:100%;padding:6px 4px;font-size:12.5px;color:var(--ink2)")}><span className="ms" style={css("font-size:15px;color:var(--ink3)")}>{ICON.search}</span>{s.label}</button></li>

                          ))}
      </ul>
      </div>

                      {Boolean(v.notMobile) && (<>
                  
      <div style={css("border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:13px 14px")}>
      <h3 style={css("font-size:13px;font-weight:500")}>Filters</h3>
      <div style={css("display:flex;flex-direction:column;gap:11px;margin-top:11px")}>

                            {(v.selectFilters ?? []).map((f, fI) => (
                        
      <label key={fI} style={css("display:block")}>
      <span style={css("display:block;font-size:11px;color:var(--ink3);margin-bottom:5px")}>{f.label}</span>
      <select value={f.value} onChange={f.onChange} style={css("width:100%;height:34px;padding:0 8px;border:1px solid var(--line);border-radius:8px;background:var(--panel2);font-size:12.5px")}>

                                  {(f.options ?? []).map((o, oI) => (
                              
      <option key={oI} value={o.v}>{o.label}</option>

                                  ))}
      </select>
      </label>

                            ))}
      <label style={css("display:flex;align-items:center;gap:9px;font-size:12.5px;cursor:pointer")}>
      <button onClick={v.toggleFavFilter} role="switch" aria-checked={v.favOn} aria-label="Favourites only" style={css(`position:relative;width:34px;height:20px;border-radius:99px;background:${v.favToggleBg};flex:none;transition:background 180ms ease`)}><span style={css(`position:absolute;top:2px;left:${v.favKnob};width:16px;height:16px;border-radius:50%;background:var(--panel);transition:left 180ms ease`)}></span></button>
                          Favourites only
                        </label>

                            {Boolean(v.filtersActive) && (<>
                              <button onClick={v.clearFilters} style={css("font-size:12.5px;color:var(--em)")}>Clear filters</button>
                            </>)}
      </div>
      </div>

                      </>)}
      <div style={css("border:1px solid var(--line);border-radius:14px;background:var(--panel);overflow:hidden")}>
      <div style={css("padding:13px 14px 0")}><h3 style={css("font-size:13px;font-weight:500")}>Tool preview</h3></div>
      <div style={css("padding:12px 14px")}>
      <div style={css("display:flex;align-items:center;gap:9px")}>
      <span style={css(`display:flex;align-items:center;justify-content:center;width:30px;height:30px;border-radius:8px;background:${v.pv.tint}`)}><span className="ms" style={css(`font-size:18px;color:${v.pv.color}`)}>{ICON[v.pv.icon]}</span></span>
      <span style={css("display:block;min-width:0")}><span style={css("display:block;font-size:13px;font-weight:500")}>{v.pv.name}</span><span style={css("display:block;font-size:11px;color:var(--ink3)")}>{v.pv.purpose}</span></span>
      </div>
      <div style={css("display:flex;flex-wrap:wrap;gap:6px;margin-top:10px")}>
      <span style={css(`display:inline-flex;align-items:center;gap:5px;padding:3px 8px;border:1px solid var(--line);border-radius:6px;font-size:10.5px;color:${v.pv.modeColor}`)}><span style={css(`width:6px;height:6px;border-radius:50%;background:${v.pv.modeColor}`)}></span>{v.pv.mode}</span>
      <span style={css("padding:3px 8px;border:1px solid var(--line);border-radius:6px;font-size:10.5px;color:var(--ink3);font-family:'Geist Mono',ui-monospace,monospace")}>{v.pv.io}</span>
      <span style={css("padding:3px 8px;border:1px solid var(--line);border-radius:6px;font-size:10.5px;color:var(--ink3)")}>Up to 500 MB</span>
      </div>
      <button onClick={v.pv.go} style={css("width:100%;margin-top:11px;display:flex;flex-direction:column;align-items:center;gap:4px;padding:18px 12px;border:1.5px dashed var(--line2);border-radius:10px;background:var(--panel2);transition:border-color 160ms ease")} className="dc-i9">
      <span className="ms" style={css("font-size:24px;color:var(--em)")}>{ICON.note_add}</span>
      <span style={css("font-size:12.5px")}>Drop a file here</span>
      <span style={css("font-size:11px;color:var(--ink3)")}>or choose a file</span>
      </button>
      <p style={css("margin-top:8px;font-size:10.5px;color:var(--ink3);text-align:center")}>Max file size: 500 MB</p>
      </div>
      </div>
      </aside>
      </div>
      </div>
      </div>

            </>)}

            {Boolean(v.isTool) && (<>
        
      <div style={css("animation:slidein 200ms ease both")}>
      <section style={css("position:relative;overflow:hidden;border-bottom:1px solid var(--line);background:linear-gradient(180deg,var(--bg2),var(--bg))")}>
      <div aria-hidden="true" style={css("position:absolute;inset:0;opacity:var(--wave);pointer-events:none")}>
      <svg viewBox="0 0 1400 160" preserveAspectRatio="none" style={css("position:absolute;inset:0;width:130%;height:100%")}>
      <g fill="none" stroke="var(--em)" strokeWidth="1">
      <path d="M-40 110 C 240 46 460 140 720 96 S 1140 30 1440 88" opacity=".4" style={css("animation:drift 18s linear infinite alternate")}></path>
      <path d="M-40 132 C 260 74 480 158 740 118 S 1160 52 1440 110" opacity=".26" style={css("animation:drift 24s linear infinite alternate-reverse")}></path>
      <path d="M-40 88 C 220 30 420 120 700 74 S 1120 14 1440 66" opacity=".16" style={css("animation:drift 15s linear infinite alternate")}></path>
      </g>
      </svg>
      </div>
      <div style={css("position:relative;max-width:1180px;margin:0 auto;padding:26px 24px 18px;display:flex;flex-wrap:wrap;align-items:flex-start;gap:20px")}>
      <div style={css("flex:1;min-width:0")}>
      <div style={css("display:flex;align-items:center;gap:10px")}>
      <span style={css(`display:flex;align-items:center;justify-content:center;width:34px;height:34px;border-radius:9px;background:${v.tTint}`)}><span className="ms" style={css(`font-size:21px;color:${v.tColor}`)}>{ICON[v.tIcon]}</span></span>
      <h1 style={css("font-size:clamp(21px,3.6vw,27px);font-weight:600;letter-spacing:-.02em")}>{v.tName}</h1>
      </div>
      <p style={css("margin-top:7px;font-size:15px;color:var(--ink2)")}>{v.tPurpose}</p>
      <div style={css("display:flex;flex-wrap:wrap;gap:6px;margin-top:12px")}>
      <span style={css(`display:inline-flex;align-items:center;gap:6px;padding:4px 9px;border:1px solid var(--line);border-radius:7px;background:var(--panel);font-size:11.5px;white-space:nowrap;color:${v.tModeColor}`)}><span style={css(`width:6px;height:6px;border-radius:50%;background:${v.tModeColor}`)}></span>{v.tMode}</span>
      <span style={css("padding:4px 9px;border:1px solid var(--line);border-radius:7px;background:var(--panel);font-size:11.5px;color:var(--ink2);white-space:nowrap;font-family:'Geist Mono',ui-monospace,monospace")}>{v.tIO}</span>
      <span style={css("padding:4px 9px;border:1px solid var(--line);border-radius:7px;background:var(--panel);font-size:11.5px;color:var(--ink2);white-space:nowrap")}>Up to 500 MB per file</span>
      <span style={css("padding:4px 9px;border:1px solid var(--line);border-radius:7px;background:var(--panel);font-size:11.5px;color:var(--ink2);white-space:nowrap")}>{v.tFam}</span>
      </div>
      </div>
      <div style={css("width:clamp(78px,12vw,110px);height:clamp(62px,10vw,88px);flex:0 0 auto;opacity:.9")}>
      <svg viewBox="0 0 160 130" aria-hidden="true" style={css("width:100%;height:100%")}>
      <g fill="none" stroke="var(--em)" strokeWidth="1.5" strokeLinejoin="round">
      <path d="M80 16 L128 42 L128 92 L80 118 L32 92 L32 42 Z"></path>
      <path d="M32 42 L80 68 L128 42"></path><path d="M80 68 L80 118"></path>
      </g>
      </svg>
      </div>
      </div>
      <div style={css("position:relative;max-width:1180px;margin:0 auto;padding:0 24px 18px")}>
      <ul style={css("display:grid;grid-template-columns:repeat(auto-fit,minmax(140px,1fr));gap:6px")}>

                      {(v.trust ?? []).map((t, tI) => (
                  
      <li key={tI} style={css("display:flex;align-items:center;gap:7px;padding:7px 9px;border:1px solid var(--line);border-radius:9px;background:var(--panel)")}>
      <span className="ms" style={css(`font-size:16px;color:${t.color}`)}>{ICON[t.icon]}</span>
      <span style={css("display:block;min-width:0")}><span style={css("display:block;font-size:11.5px;font-weight:500;white-space:nowrap;overflow:hidden;text-overflow:ellipsis")}>{t.label}</span><span style={css("display:block;font-size:10px;color:var(--ink3);white-space:nowrap;overflow:hidden;text-overflow:ellipsis")}>{t.sub}</span></span>
      </li>

                      ))}
      </ul>
      </div>
      </section>
      <div style={css("max-width:1180px;margin:0 auto;padding:18px 24px 0")}>
      <section style={css("border:1px solid var(--line);border-radius:14px;background:var(--panel);overflow:hidden")}>
      <div style={css("display:flex;flex-wrap:wrap;align-items:center;gap:10px;padding:13px 16px;border-bottom:1px solid var(--line)")}>
      <h2 style={css("flex:1;min-width:160px;font-size:14px;font-weight:500")}>Run this tool</h2>
      <label style={css("display:flex;align-items:center;gap:7px;font-size:11.5px;color:var(--ink3)")}>
                      Preview state
                      <select value={v.stateValue} onChange={v.setStateDemo} aria-label="Preview a lifecycle state" style={css("height:30px;padding:0 8px;border:1px solid var(--line);border-radius:8px;background:var(--panel2);font-size:12px;color:var(--ink2)")}>

                          {(v.stateOpts ?? []).map((o, oI) => (
                            <option key={oI} value={o.v}>{o.label}</option>
                          ))}
      </select>
      </label>
      </div>

                    {Boolean(v.modeUnverified) && (<>
                
      <p style={css("display:flex;gap:10px;margin:14px 16px 0;padding:11px 12px;border:1px solid var(--amber);border-radius:10px;background:var(--amberSoft);font-size:12px;color:var(--ink2);line-height:1.5")}><span className="ms" style={css("font-size:18px;color:var(--amber)")}>{ICON.science}</span>Processing mode unverified for this record. The lifecycle below is a labelled simulation: no file is read, processed or sent, and no processing location is claimed.</p>

                    </>)}
      <div style={css("display:flex;flex-wrap:wrap;gap:0")}>
      <ol style={css("flex:0 0 210px;min-width:180px;padding:14px 12px;border-right:1px solid var(--line);display:flex;flex-direction:column;gap:2px")}>

                        {(v.steps ?? []).map((s, sI) => (
                    
      <li key={sI} style={css(`display:flex;align-items:flex-start;gap:10px;padding:8px 9px;border-radius:9px;background:${s.bg}`)}>
      <span style={css(`display:flex;align-items:center;justify-content:center;width:22px;height:22px;flex:none;border-radius:50%;border:1px solid ${s.ring};color:${s.color};font-size:11px;font-family:'Geist Mono',ui-monospace,monospace`)}>{s.n}</span>
      <span style={css("display:block;min-width:0")}>
      <span style={css(`display:block;font-size:12.5px;font-weight:${s.fw};color:${s.fg}`)}>{s.label}</span>
      <span style={css("display:block;font-size:10.5px;color:var(--ink3)")}>{s.sub}</span>
      </span>
      </li>

                        ))}
      </ol>
      <div style={css("flex:1 1 420px;min-width:0;padding:16px")}>

                        {Boolean(v.hasAlert) && (<>
                    
      <div role="alert" style={css(`display:flex;gap:11px;padding:12px 13px;margin-bottom:14px;border:1px solid ${v.alert.color};border-radius:11px;background:${v.alert.bg}`)}>
      <span className="ms" style={css(`font-size:20px;color:${v.alert.color}`)}>{ICON[v.alert.icon]}</span>
      <span style={css("display:block;flex:1;min-width:0")}>
      <span style={css("display:block;font-size:13px;font-weight:500")}>{v.alert.title}</span>
      <span style={css("display:block;margin-top:3px;font-size:12px;color:var(--ink2);line-height:1.45")}>{v.alert.body}</span>
      </span>
      <button onClick={v.clearAlert} style={css("flex:none;align-self:flex-start;height:30px;padding:0 11px;border:1px solid var(--line2);border-radius:8px;font-size:12px")}>{v.alert.action}</button>
      </div>

                        </>)}

                        {Boolean(v.stageInput) && (<>
                    
      <h3 style={css("font-size:13.5px;font-weight:500")}>Add a file from any source</h3>
      <p style={css("margin-top:4px;font-size:12px;color:var(--ink3)")}>{v.tServerNote}</p>
      <ul style={css("display:grid;grid-template-columns:repeat(auto-fit,minmax(104px,1fr));gap:7px;margin-top:12px")}>

                            {(v.tSources ?? []).map((s, sI) => (
                        
      <li key={sI}><button onClick={s.go} style={css("width:100%;min-height:44px;display:flex;align-items:center;gap:7px;padding:8px 9px;border:1px solid var(--line);border-radius:9px;background:var(--panel2);transition:border-color 160ms ease")} className="dc-i10"><span className="ms" style={css("font-size:17px;color:var(--ink2)")}>{ICON[s.icon]}</span><span style={css("display:block;min-width:0")}><span style={css("display:block;font-size:12px;font-weight:500")}>{s.label}</span><span style={css("display:block;font-size:10px;color:var(--ink3)")}>{s.sub}</span></span></button></li>

                            ))}
      </ul>
      <div style={css("display:flex;flex-wrap:wrap;align-items:center;gap:10px;margin-top:12px;padding-top:12px;border-top:1px solid var(--line);font-size:11.5px;color:var(--ink3)")}>
      <span>Max file size 500 MB</span><span style={css("color:var(--line2)")}>•</span><span>Settings appear after you choose a file</span><span style={css("color:var(--line2)")}>•</span><span>No document retention</span>
      </div>

                        </>)}

                        {Boolean(v.stageConfigure) && (<>
                    
      <div style={css("display:flex;align-items:center;gap:10px;padding:10px 12px;border:1px solid var(--line);border-radius:10px;background:var(--panel2)")}>
      <span className="ms" style={css(`font-size:19px;color:${v.tColor}`)}>{ICON.draft}</span>
      <span style={css("display:block;flex:1;min-width:0")}><span style={css("display:block;font-size:12.5px;font-weight:500")}>{v.fileName}</span><span style={css("display:block;font-size:11px;color:var(--ink3)")}>{v.fileSize} · inspected locally</span></span>
      <button onClick={v.startOver} aria-label="Remove file" style={css("display:flex;align-items:center;justify-content:center;width:30px;height:30px;border-radius:8px")}><span className="ms" style={css("font-size:17px;color:var(--ink3)")}>{ICON.close}</span></button>
      </div>
      <div style={css("display:flex;flex-direction:column;gap:16px;margin-top:16px")}>

                            {(v.optGroups ?? []).map((o, oI) => (
                        
      <div key={oI}>
      <div style={css("display:flex;align-items:baseline;flex-wrap:wrap;gap:8px")}>
      <span style={css("font-size:12.5px;font-weight:500")}>{o.label}</span>

                                  {Boolean(o.hasHelp) && (<>
                                    <span style={css("font-size:11px;color:var(--ink3)")}>{o.help}</span>
                                  </>)}
      </div>

                                {Boolean(o.isRadio) && (<>
                            
      <ul role="radiogroup" style={css("display:grid;grid-template-columns:repeat(auto-fit,minmax(132px,1fr));gap:7px;margin-top:8px")}>

                                    {(o.choices ?? []).map((c, cI) => (
                                
      <li key={cI}><button onClick={c.go} role="radio" aria-checked={c.checked} style={css(`width:100%;min-height:44px;display:flex;align-items:center;gap:9px;padding:9px 10px;border:1px solid ${c.bd};border-radius:9px;background:${c.bg};transition:border-color 160ms ease,background 160ms ease`)}>
      <span style={css(`display:flex;align-items:center;justify-content:center;width:15px;height:15px;flex:none;border-radius:50%;border:1px solid ${c.ring}`)}><span style={css(`width:7px;height:7px;border-radius:50%;background:${c.dot}`)}></span></span>
      <span style={css("display:block;min-width:0")}><span style={css("display:block;font-size:12.5px")}>{c.label}</span><span style={css("display:block;font-size:10.5px;color:var(--ink3)")}>{c.sub}</span></span>
      </button></li>

                                    ))}
      </ul>

                                </>)}

                                {Boolean(o.isSelect) && (<>
                            
      <select value={o.value} onChange={o.onChange} aria-label={o.label} style={css("width:100%;max-width:280px;height:36px;margin-top:8px;padding:0 9px;border:1px solid var(--line);border-radius:9px;background:var(--panel2);font-size:12.5px")}>

                                    {(o.choices ?? []).map((c, cI) => (
                                      <option key={cI} value={c.v}>{c.label}</option>
                                    ))}
      </select>

                                </>)}

                                {Boolean(o.isToggle) && (<>
                            
      <button onClick={o.onToggle} role="switch" aria-checked={o.ariaChecked} style={css("display:flex;align-items:center;gap:10px;margin-top:8px;min-height:32px;font-size:12.5px;color:var(--ink2)")}>
      <span style={css(`position:relative;width:36px;height:21px;border-radius:99px;background:${o.toggleBg};flex:none;transition:background 180ms ease`)}><span style={css(`position:absolute;top:2px;left:${o.knob};width:17px;height:17px;border-radius:50%;background:var(--panel);transition:left 180ms ease`)}></span></span>
                                  {o.display}
                                </button>

                                </>)}

                                {Boolean(o.isSlider) && (<>
                            
      <div style={css("display:flex;align-items:center;gap:12px;margin-top:8px;max-width:340px")}>
      <input type="range" min={o.min} max={o.max} step={o.step} value={o.value} onChange={o.onChange} aria-label={o.label} style={css("flex:1;accent-color:var(--em)")} />
      <span style={css("font-size:12px;font-family:'Geist Mono',ui-monospace,monospace;color:var(--ink2);min-width:44px;text-align:right")}>{o.display}</span>
      </div>

                                </>)}

                                {Boolean(o.isNumber) && (<>
                            
      <div style={css("display:flex;align-items:center;gap:8px;margin-top:8px")}>
      <input type="number" min={o.min} max={o.max} step={o.step} value={o.value} onChange={o.onNum} aria-label={o.label} style={css("width:110px;height:36px;padding:0 9px;border:1px solid var(--line);border-radius:9px;background:var(--panel2);font-size:12.5px")} />
      <span style={css("font-size:11.5px;color:var(--ink3)")}>{o.unit}</span>
      </div>

                                </>)}

                                {Boolean(o.isText) && (<>
                            
      <input type="text" value={o.value} onChange={o.onChange} placeholder={o.placeholder} aria-label={o.label} style={css("width:100%;max-width:300px;height:36px;margin-top:8px;padding:0 10px;border:1px solid var(--line);border-radius:9px;background:var(--panel2);font-size:12.5px")} />

                                </>)}

                                {Boolean(o.isPassword) && (<>
                            
      <input type="password" value={o.value} onChange={o.onChange} placeholder="••••••••" aria-label={o.label} style={css("width:100%;max-width:300px;height:36px;margin-top:8px;padding:0 10px;border:1px solid var(--line);border-radius:9px;background:var(--panel2);font-size:12.5px;font-family:'Geist Mono',ui-monospace,monospace")} />

                                </>)}

                                {Boolean(o.isMulti) && (<>
                            
      <ul style={css("display:flex;flex-wrap:wrap;gap:6px;margin-top:8px")}>

                                    {(o.choices ?? []).map((c, cI) => (
                                
      <li key={cI}><button onClick={c.go} aria-pressed={c.pressed} style={css(`display:flex;align-items:center;gap:6px;height:30px;padding:0 10px;border:1px solid ${c.bd};border-radius:99px;background:${c.bg};color:${c.fg};font-size:12px`)}><span className="ms" style={css("font-size:15px")}>{ICON[c.icon]}</span>{c.label}</button></li>

                                    ))}
      </ul>

                                </>)}

                                {Boolean(o.isPages) && (<>
                            
      <input type="text" value={o.value} onChange={o.onChange} placeholder="All pages — or 1-3, 7, 10-12" aria-label={o.label} style={css("width:100%;max-width:300px;height:36px;margin-top:8px;padding:0 10px;border:1px solid var(--line);border-radius:9px;background:var(--panel2);font-size:12.5px;font-family:'Geist Mono',ui-monospace,monospace")} />

                                </>)}

                                {Boolean(o.isColor) && (<>
                            
      <ul style={css("display:flex;gap:7px;margin-top:8px")}>

                                    {(o.choices ?? []).map((c, cI) => (
                                
      <li key={cI}><button onClick={c.go} aria-label={`Colour ${c.v}`} style={css(`width:30px;height:30px;border-radius:8px;background:${c.v};border:${c.on}`)}></button></li>

                                    ))}
      </ul>

                                </>)}

                                {Boolean(o.isReorder) && (<>
                            
      <ul style={css("display:grid;grid-template-columns:repeat(auto-fit,minmax(96px,1fr));gap:8px;margin-top:8px")}>

                                    {(o.pages ?? []).map((p, pI) => (
                                
      <li key={pI} style={css("border:1px solid var(--line);border-radius:9px;background:var(--panel2);overflow:hidden")}>
      <div aria-hidden="true" style={css("height:64px;display:flex;align-items:center;justify-content:center;background:repeating-linear-gradient(135deg,var(--panel3) 0 6px,transparent 6px 12px)")}><span className="ms" style={css("font-size:20px;color:var(--ink3)")}>{ICON.description}</span></div>
      <div style={css("display:flex;align-items:center;gap:2px;padding:5px 6px;border-top:1px solid var(--line)")}>
      <span style={css("flex:1;font-size:10.5px;color:var(--ink2)")}>{p.n}</span>
      <button onClick={p.up} aria-label={p.upLabel} style={css("display:flex;width:22px;height:22px;align-items:center;justify-content:center;border-radius:5px")}><span className="ms" style={css("font-size:14px;color:var(--ink3)")}>{ICON.arrow_upward}</span></button>
      <button onClick={p.down} aria-label={p.downLabel} style={css("display:flex;width:22px;height:22px;align-items:center;justify-content:center;border-radius:5px")}><span className="ms" style={css("font-size:14px;color:var(--ink3)")}>{ICON.arrow_downward}</span></button>
      <button onClick={p.del} aria-label={p.delLabel} style={css("display:flex;width:22px;height:22px;align-items:center;justify-content:center;border-radius:5px")}><span className="ms" style={css("font-size:14px;color:var(--coral)")}>{ICON.delete}</span></button>
      </div>
      </li>

                                    ))}
      </ul>

                                </>)}

                                {Boolean(o.isCrop) && (<>
                            
      <div style={css("display:flex;flex-wrap:wrap;gap:12px;margin-top:8px;align-items:flex-start")}>
      <div aria-hidden="true" style={css("position:relative;width:150px;height:104px;border:1px solid var(--line);border-radius:8px;background:repeating-linear-gradient(135deg,var(--panel3) 0 6px,transparent 6px 12px)")}>
      <div style={css("position:absolute;left:22px;top:26px;width:76px;height:32px;border:1.5px solid var(--coral);background:var(--coralSoft)")}></div>
      </div>
      <div style={css("display:grid;grid-template-columns:repeat(2,minmax(88px,1fr));gap:7px")}>
      <label style={css("font-size:10.5px;color:var(--ink3)")}>X<input type="number" defaultValue="24" style={css("display:block;width:100%;height:32px;margin-top:3px;padding:0 8px;border:1px solid var(--line);border-radius:7px;background:var(--panel2);font-size:12px")} /></label>
      <label style={css("font-size:10.5px;color:var(--ink3)")}>Y<input type="number" defaultValue="38" style={css("display:block;width:100%;height:32px;margin-top:3px;padding:0 8px;border:1px solid var(--line);border-radius:7px;background:var(--panel2);font-size:12px")} /></label>
      <label style={css("font-size:10.5px;color:var(--ink3)")}>Width<input type="number" defaultValue="112" style={css("display:block;width:100%;height:32px;margin-top:3px;padding:0 8px;border:1px solid var(--line);border-radius:7px;background:var(--panel2);font-size:12px")} /></label>
      <label style={css("font-size:10.5px;color:var(--ink3)")}>Height<input type="number" defaultValue="46" style={css("display:block;width:100%;height:32px;margin-top:3px;padding:0 8px;border:1px solid var(--line);border-radius:7px;background:var(--panel2);font-size:12px")} /></label>
      </div>
      </div>

                                </>)}
      </div>

                            ))}
      </div>
      <div style={css("display:flex;flex-wrap:wrap;align-items:center;gap:9px;margin-top:18px;padding-top:14px;border-top:1px solid var(--line)")}>
      <button onClick={v.runLocal} style={css("height:38px;padding:0 16px;border-radius:9px;background:var(--em);color:var(--em-ink,#04231A);font-size:13px;font-weight:500")}>{v.runLabel}</button>

                            {Boolean(v.canRunServer) && (<>
                        
      <button onClick={v.runServer} style={css("height:38px;padding:0 14px;border:1px solid var(--line2);border-radius:9px;font-size:12.5px;color:var(--ink2)")}>Use server fallback</button>

                            </>)}
      <button onClick={v.addToPipeline} style={css("height:38px;padding:0 14px;border:1px solid var(--line);border-radius:9px;font-size:12.5px;color:var(--ink2)")}>Add to pipeline</button>
      <span style={css("flex:1")}></span>
      <span style={css("font-size:11.5px;color:var(--ink3)")}>Nothing runs until you press a button.</span>
      </div>

                        </>)}

                        {Boolean(v.stageQueued) && (<>
                    
      <h3 style={css("font-size:13.5px;font-weight:500")}>Queued</h3>
      <p style={css("margin-top:4px;font-size:12px;color:var(--ink3)")}>1 file · {v.fileSize} · waiting to start</p>
      <div style={css("display:flex;align-items:center;gap:10px;margin-top:12px;padding:11px 12px;border:1px solid var(--line);border-radius:10px;background:var(--panel2)")}>
      <span className="ms" style={css(`font-size:19px;color:${v.tColor}`)}>{ICON.draft}</span>
      <span style={css("display:block;flex:1;min-width:0")}><span style={css("display:block;font-size:12.5px")}>{v.fileName}</span><span style={css("display:block;font-size:11px;color:var(--ink3)")}>{v.fileSize}</span></span>
      <button onClick={v.cancel} style={css("height:30px;padding:0 12px;border:1px solid var(--line2);border-radius:8px;font-size:12px")}>Cancel</button>
      </div>

                        </>)}

                        {Boolean(v.stageProcessing) && (<>
                    
      <div style={css("display:flex;align-items:baseline;justify-content:space-between;gap:10px")}>
      <h3 style={css("font-size:13.5px;font-weight:500")}>{v.stageText}</h3>
      <span style={css("font-size:13px;font-family:'Geist Mono',ui-monospace,monospace;color:var(--em)")}>{v.prog}%</span>
      </div>
      <div role="progressbar" aria-valuenow={v.prog} aria-valuemin="0" aria-valuemax="100" aria-label="Processing progress" style={css("height:7px;margin-top:10px;border-radius:99px;background:var(--panel3);overflow:hidden")}>
      <div style={css(`height:100%;width:${v.progW};border-radius:99px;background:linear-gradient(90deg,var(--teal),var(--em));transition:width 240ms linear`)}></div>
      </div>
      <p aria-live="polite" style={css("margin-top:9px;font-size:12px;color:var(--ink3)")}>{v.processingNote}</p>
      <div style={css("display:flex;gap:9px;margin-top:14px")}>
      <button onClick={v.cancel} style={css("height:34px;padding:0 13px;border:1px solid var(--line2);border-radius:8px;font-size:12.5px")}>Cancel</button>
      </div>

                        </>)}

                        {Boolean(v.stageCancelled) && (<>
                    
      <div style={css("display:flex;gap:11px;padding:13px;border:1px solid var(--line2);border-radius:11px;background:var(--panel2)")}>
      <span className="ms" style={css("font-size:20px;color:var(--amber)")}>{ICON.cancel}</span>
      <span style={css("flex:1")}><span style={css("display:block;font-size:13px;font-weight:500")}>Cancelled</span><span style={css("display:block;margin-top:3px;font-size:12px;color:var(--ink2)")}>Nothing was produced and nothing was kept. Your settings are still here.</span></span>
      </div>
      <div style={css("display:flex;gap:9px;margin-top:13px")}>
      <button onClick={v.resume} style={css("height:36px;padding:0 14px;border-radius:9px;background:var(--em);color:var(--em-ink,#04231A);font-size:12.5px;font-weight:500")}>Back to settings</button>
      <button onClick={v.startOver} style={css("height:36px;padding:0 13px;border:1px solid var(--line);border-radius:9px;font-size:12.5px;color:var(--ink2)")}>Start over</button>
      </div>

                        </>)}

                        {Boolean(v.stageResult) && (<>
                    
      <div style={css("display:flex;flex-wrap:wrap;align-items:center;gap:12px;padding:14px;border:1px solid var(--emLine);border-radius:11px;background:var(--emSoft)")}>
      <span className="ms" style={css("font-size:26px;color:var(--em)")}>{ICON.task_alt}</span>
      <span style={css("display:block;flex:1;min-width:150px")}><span style={css("display:block;font-size:13.5px;font-weight:500")}>{v.resultName}</span><span style={css("display:block;margin-top:2px;font-size:11.5px;color:var(--ink2)")}>{v.resultSize} · {v.resultSaving}</span></span>
      <button onClick={v.download} style={css("height:38px;padding:0 16px;border-radius:9px;background:var(--em);color:var(--em-ink,#04231A);font-size:13px;font-weight:500")}>Download</button>
      </div>
      <div style={css("display:flex;flex-wrap:wrap;gap:9px;margin-top:13px")}>
      <button onClick={v.startOver} style={css("height:34px;padding:0 13px;border:1px solid var(--line);border-radius:8px;font-size:12.5px;color:var(--ink2)")}>Start over</button>
      <button onClick={v.sendOn} style={css("height:34px;padding:0 13px;border:1px solid var(--line);border-radius:8px;font-size:12.5px;color:var(--ink2)")}>Send to another tool</button>
      <button onClick={v.addToPipeline} style={css("height:34px;padding:0 13px;border:1px solid var(--line);border-radius:8px;font-size:12.5px;color:var(--ink2)")}>Save as pipeline</button>
      </div>
      <div style={css("display:flex;align-items:center;gap:8px;margin-top:13px;padding:10px 12px;border:1px solid var(--line);border-radius:10px;background:var(--panel2);font-size:11.5px;color:var(--ink2)")}>
      <span className="ms" style={css("font-size:17px;color:var(--teal)")}>{ICON.visibility_lock}</span>
                          Result exists only in this tab. Refreshing or closing it removes the result. Saving to a cloud provider is optional and your choice.
                        </div>

                        </>)}

                        {Boolean(v.stageError) && (<>
                    
      <div role="alert" style={css("display:flex;gap:11px;padding:13px;border:1px solid var(--coral);border-radius:11px;background:var(--coralSoft)")}>
      <span className="ms" style={css("font-size:20px;color:var(--coral)")}>{ICON.error}</span>
      <span style={css("flex:1")}><span style={css("display:block;font-size:13px;font-weight:500")}>Something went wrong</span><span style={css("display:block;margin-top:3px;font-size:12px;color:var(--ink2)")}>The file could not be processed. Nothing was stored. You can try again, or try a different setting.</span></span>
      </div>
      <div style={css("display:flex;gap:9px;margin-top:13px")}>
      <button onClick={v.retry} style={css("height:36px;padding:0 14px;border-radius:9px;background:var(--em);color:var(--em-ink,#04231A);font-size:12.5px;font-weight:500")}>Try again</button>
      <button onClick={v.resume} style={css("height:36px;padding:0 13px;border:1px solid var(--line);border-radius:9px;font-size:12.5px;color:var(--ink2)")}>Change settings</button>
      </div>

                        </>)}

                        {Boolean(v.stageServerDown) && (<>
                    
      <div role="alert" style={css("display:flex;gap:11px;padding:13px;border:1px solid var(--amber);border-radius:11px;background:var(--amberSoft)")}>
      <span className="ms" style={css("font-size:20px;color:var(--amber)")}>{ICON.cloud_off}</span>
      <span style={css("flex:1")}><span style={css("display:block;font-size:13px;font-weight:500")}>Server temporarily unavailable</span><span style={css("display:block;margin-top:3px;font-size:12px;color:var(--ink2)")}>Our server in Mumbai, India is at capacity and has no failover. Server-backed tools are unavailable right now. Local tools and the rest of the site keep working.</span></span>
      </div>
      <div style={css("display:flex;flex-wrap:wrap;gap:9px;margin-top:13px")}>
      <button onClick={v.tryLocal} style={css("height:36px;padding:0 14px;border-radius:9px;background:var(--em);color:var(--em-ink,#04231A);font-size:12.5px;font-weight:500")}>Try on this device</button>
      <button onClick={v.retry} style={css("height:36px;padding:0 13px;border:1px solid var(--line);border-radius:9px;font-size:12.5px;color:var(--ink2)")}>Try again</button>
      <button onClick={v.goStatus} style={css("height:36px;padding:0 13px;border:1px solid var(--line);border-radius:9px;font-size:12.5px;color:var(--ink2)")}>View status</button>
      </div>

                        </>)}
      </div>
      </div>
      </section>
      <div style={css("display:flex;flex-wrap:wrap;gap:14px;margin-top:14px;align-items:flex-start")}>
      <section style={css("flex:2 1 420px;min-width:0;border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:16px")}>
      <h2 style={css("font-size:15px;font-weight:500")}>About this tool</h2>
      <ul style={css("margin-top:10px;display:flex;flex-direction:column;gap:7px")}>

                        {(v.about ?? []).map((a, aI) => (
                    
      <li key={aI} style={css("display:flex;gap:9px;font-size:12.5px;color:var(--ink2);line-height:1.5")}><span style={css("width:5px;height:5px;border-radius:50%;background:var(--em);flex:none;margin-top:7px")}></span>{a}</li>

                        ))}
      </ul>
      <h3 style={css("margin-top:18px;font-size:13.5px;font-weight:500")}>Frequently asked</h3>
      <ul style={css("margin-top:8px;border:1px solid var(--line);border-radius:11px;overflow:hidden")}>

                        {(v.faq ?? []).map((f, fI) => (
                    
      <li key={fI} style={css("border-top:1px solid var(--line)")}>
      <button onClick={f.go} aria-expanded={f.expanded} style={css("display:flex;align-items:center;gap:10px;width:100%;min-height:44px;padding:10px 12px;font-size:12.5px")}>
      <span style={css("flex:1")}>{f.q}</span><span className="ms" style={css("font-size:17px;color:var(--ink3)")}>{ICON[f.icon]}</span>
      </button>

                            {Boolean(f.open) && (<>
                              <p style={css("padding:0 12px 12px;font-size:12px;color:var(--ink2);line-height:1.55;max-width:64ch")}>{f.a}</p>
                            </>)}
      </li>

                        ))}
      </ul>
      </section>
      <section style={css("flex:1 1 260px;min-width:0;border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:16px")}>
      <h2 style={css("font-size:15px;font-weight:500")}>Related tools</h2>
      <ul style={css("margin-top:10px;display:flex;flex-direction:column;gap:6px")}>

                        {(v.related ?? []).map((r, rI) => (
                    
      <li key={rI}><button onClick={r.go} style={css("display:flex;align-items:center;gap:9px;width:100%;min-height:40px;padding:6px 8px;border-radius:9px;transition:background 140ms ease")} className="dc-i11">
      <span className="ms" style={css(`font-size:17px;color:${r.color}`)}>{ICON[r.icon]}</span>
      <span style={css("display:block;flex:1;min-width:0")}><span style={css("display:block;font-size:12.5px")}>{r.name}</span><span style={css("display:block;font-size:10.5px;color:var(--ink3);white-space:nowrap;overflow:hidden;text-overflow:ellipsis")}>{r.purpose}</span></span>
      <span className="ms" style={css("font-size:16px;color:var(--ink3)")}>{ICON.chevron_right}</span>
      </button></li>

                        ))}
      </ul>
      <button onClick={v.goTools} style={css("margin-top:11px;font-size:12.5px;color:var(--em)")}>Browse the full catalogue →</button>
      </section>
      </div>
      </div>
      </div>

            </>)}

            {Boolean(v.isPipeline) && (<>
        
      <div style={css("max-width:1180px;margin:0 auto;padding:20px 24px 0;animation:slidein 200ms ease both")}>
      <div style={css("display:flex;flex-wrap:wrap;align-items:flex-end;gap:12px")}>
      <div style={css("flex:1;min-width:200px")}>
      <h1 style={css("font-size:clamp(21px,3.6vw,26px);font-weight:600;letter-spacing:-.02em")}>Pipeline</h1>
      <p style={css("margin-top:3px;font-size:12.5px;color:var(--ink3)")}>Chain tools in order. One file feeds the whole chain, and each step says where it runs.</p>
      </div>
      <div style={css("display:flex;align-items:center;gap:8px")}>
      <span style={css(`display:inline-flex;align-items:center;gap:6px;height:30px;padding:0 10px;border:1px solid var(--line);border-radius:99px;background:var(--panel);font-size:11.5px;color:${v.pipeStateColor}`)}><span style={css(`width:7px;height:7px;border-radius:50%;background:${v.pipeStateColor}`)}></span>{v.pipeStateLabel}</span>
      <label style={css("display:flex;align-items:center;gap:7px;font-size:11.5px;color:var(--ink3)")}>Preview state
                    <select value={v.pipeStateValue} onChange={v.setPipeState} aria-label="Preview a pipeline state" style={css("height:30px;padding:0 8px;border:1px solid var(--line);border-radius:8px;background:var(--panel2);font-size:12px;color:var(--ink2)")}>

                        {(v.pipeStates ?? []).map((s, sI) => (
                          <option key={sI} value={s.v}>{s.label}</option>
                        ))}
      </select>
      </label>
      </div>
      </div>
      <div style={css("display:flex;flex-wrap:wrap;gap:14px;margin-top:16px;align-items:flex-start")}>
      <div style={css("flex:3 1 500px;min-width:0;display:flex;flex-direction:column;gap:14px")}>
      <section style={css("border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:14px")}>
      <div style={css("display:flex;flex-wrap:wrap;align-items:center;gap:10px")}>
      <h2 style={css("flex:1;min-width:120px;font-size:14px;font-weight:500")}>Chain</h2>
      <span style={css("display:flex;align-items:center;gap:7px;font-size:11.5px;color:var(--ink2)")}><span className="ms" style={css("font-size:16px;color:var(--em)")}>{ICON.draft}</span>{v.pipeFileName}</span>

                        {Boolean(v.pipeNoFile) && (<>
                          <button onClick={v.addPipeFile} style={css("height:28px;padding:0 10px;border:1px solid var(--emLine);border-radius:8px;font-size:12px;color:var(--em)")}>Add a file</button>
                        </>)}
      </div>

                      {Boolean(v.pipeEmpty) && (<>
                  
      <div style={css("margin-top:14px;padding:32px 18px;text-align:center;border:1.5px dashed var(--line2);border-radius:12px")}>
      <span className="ms" style={css("font-size:30px;color:var(--ink3)")}>{ICON.account_tree}</span>
      <h3 style={css("margin-top:8px;font-size:14px;font-weight:500")}>No steps yet</h3>
      <p style={css("margin-top:4px;font-size:12.5px;color:var(--ink3)")}>Add steps from the library below, or start from a recipe.</p>
      </div>

                      </>)}
      <ul style={css("display:flex;flex-wrap:wrap;align-items:stretch;gap:6px;margin-top:14px")}>

                        {(v.nodes ?? []).map((n, nI) => (
                    
      <li key={nI} style={css("display:flex;align-items:center;gap:6px")}>
      <div style={css(`width:172px;display:flex;flex-direction:column;gap:6px;padding:10px;border:1px solid ${n.bd};border-radius:11px;background:${n.bg};opacity:${n.op};transition:border-color 160ms ease,background 160ms ease`)}>
      <div style={css("display:flex;align-items:center;gap:7px;width:100%")}>
      <span className="ms" style={css(`font-size:17px;color:${n.color}`)}>{ICON[n.icon]}</span>
      <button onClick={n.select} aria-pressed={n.selected} style={css("flex:1;min-width:0;font-size:12.5px;font-weight:500;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;text-align:left")}>{n.name}</button>

                                {Boolean(n.canToggle) && (<>
                            
      <button onClick={n.toggle} role="switch" aria-checked={n.enabled} aria-label={n.toggleLabel} style={css("display:flex;align-items:center;justify-content:center;width:24px;height:24px;border-radius:6px")}><span className="ms" style={css(`font-size:18px;color:${n.toggleColor}`)}>{ICON[n.toggleIcon]}</span></button>

                                </>)}
      </div>
      <span style={css("display:block;font-size:10.5px;color:var(--ink3);line-height:1.35")}>{n.purpose}</span>
      <span style={css("display:block;height:3px;width:100%;border-radius:99px;background:var(--panel3);overflow:hidden")}><span style={css(`display:block;height:100%;width:${n.progW};background:var(--em);transition:width 200ms linear`)}></span></span>
      <div style={css("display:flex;align-items:center;gap:6px;width:100%")}>
      <span style={css(`display:inline-flex;align-items:center;gap:4px;font-size:10px;color:${n.modeColor}`)}><span style={css(`width:5px;height:5px;border-radius:50%;background:${n.modeColor}`)}></span>{n.modeLabel}</span>
      <span style={css(`margin-left:auto;font-size:10px;color:${n.stateColor}`)}>{n.stateLabel}</span>
      </div>

                              {Boolean(n.canToggle) && (<>
                          
      <div style={css("display:flex;gap:2px;width:100%;padding-top:4px;border-top:1px solid var(--line)")}>
      <button onClick={n.up} aria-label={n.upLabel} style={css("display:flex;align-items:center;justify-content:center;width:26px;height:26px;border-radius:6px")}><span className="ms" style={css("font-size:15px;color:var(--ink3)")}>{ICON.arrow_back}</span></button>
      <button onClick={n.down} aria-label={n.downLabel} style={css("display:flex;align-items:center;justify-content:center;width:26px;height:26px;border-radius:6px")}><span className="ms" style={css("font-size:15px;color:var(--ink3)")}>{ICON.arrow_forward}</span></button>
      <button onClick={n.remove} aria-label={n.removeLabel} style={css("display:flex;align-items:center;justify-content:center;width:26px;height:26px;margin-left:auto;border-radius:6px")}><span className="ms" style={css("font-size:15px;color:var(--coral)")}>{ICON.delete}</span></button>
      </div>

                              </>)}
      </div>

                            {Boolean(n.connector) && (<>
                              <span className="ms" aria-hidden="true" style={css("font-size:18px;color:var(--line2)")}>{ICON.arrow_forward}</span>
                            </>)}
      </li>

                        ))}
      </ul>

                      {Boolean(v.orderWarn) && (<>
                  
      <div style={css("display:flex;gap:9px;margin-top:12px;padding:10px 11px;border:1px solid var(--amber);border-radius:10px;background:var(--amberSoft);font-size:12px;color:var(--ink2)")}>
      <span className="ms" style={css("font-size:17px;color:var(--amber)")}>{ICON.rule}</span>{v.orderNote}
                      </div>

                      </>)}

                      {Boolean(v.pipeFailedState) && (<>
                  
      <div role="alert" style={css("display:flex;flex-wrap:wrap;align-items:center;gap:10px;margin-top:12px;padding:12px;border:1px solid var(--coral);border-radius:11px;background:var(--coralSoft)")}>
      <span className="ms" style={css("font-size:20px;color:var(--coral)")}>{ICON.error}</span>
      <span style={css("display:block;flex:1;min-width:180px")}><span style={css("display:block;font-size:13px;font-weight:500")}>Step failed — pipeline stopped</span><span style={css("display:block;margin-top:2px;font-size:12px;color:var(--ink2)")}>Last good output from {v.lastGood} is preserved. Later steps are neutral until you choose.</span></span>
      <button onClick={v.retryStep} style={css("height:32px;padding:0 13px;border-radius:8px;background:var(--em);color:var(--em-ink,#04231A);font-size:12.5px;font-weight:500")}>Retry</button>
      <button onClick={v.skipStep} style={css("height:32px;padding:0 13px;border:1px solid var(--line2);border-radius:8px;font-size:12.5px")}>Skip</button>
      <button onClick={v.stopPipe} style={css("height:32px;padding:0 13px;border:1px solid var(--coral);border-radius:8px;font-size:12.5px;color:var(--coral)")}>Stop pipeline</button>
      </div>

                      </>)}

                      {Boolean(v.pipeAllDisabled) && (<>
                  
      <div role="alert" style={css("display:flex;gap:9px;margin-top:12px;padding:11px;border:1px solid var(--amber);border-radius:10px;background:var(--amberSoft);font-size:12px;color:var(--ink2)")}><span className="ms" style={css("font-size:17px;color:var(--amber)")}>{ICON.toggle_off}</span>Every step is disabled. Enable at least one step to run the pipeline.</div>

                      </>)}

                      {Boolean(v.pipeComplete) && (<>
                  
      <div style={css("display:flex;flex-wrap:wrap;align-items:center;gap:10px;margin-top:12px;padding:12px;border:1px solid var(--emLine);border-radius:11px;background:var(--emSoft)")}>
      <span className="ms" style={css("font-size:20px;color:var(--em)")}>{ICON.task_alt}</span>
      <span style={css("flex:1;min-width:160px;font-size:12.5px")}>Pipeline complete. Result exists only in this tab until you download it.</span>
      <button onClick={v.download} style={css("height:32px;padding:0 14px;border-radius:8px;background:var(--em);color:var(--em-ink,#04231A);font-size:12.5px;font-weight:500")}>Download result</button>
      </div>

                      </>)}
      <div style={css("display:flex;flex-wrap:wrap;gap:9px;margin-top:14px;padding-top:13px;border-top:1px solid var(--line)")}>
      <button onClick={v.runPipe} style={css("height:36px;padding:0 15px;border-radius:9px;background:var(--em);color:var(--em-ink,#04231A);font-size:12.5px;font-weight:500")}>Run pipeline</button>

                        {Boolean(v.pipeRunning) && (<>
                          <button onClick={v.stopPipe} style={css("height:36px;padding:0 13px;border:1px solid var(--coral);border-radius:9px;font-size:12.5px;color:var(--coral)")}>Stop</button>
                        </>)}
      <button onClick={v.savePipe} style={css("height:36px;padding:0 13px;border:1px solid var(--line);border-radius:9px;font-size:12.5px;color:var(--ink2)")}>Save pipeline</button>
      <span style={css("flex:1")}></span>
      <span style={css("align-self:center;font-size:11.5px;color:var(--ink3)")}>Saved pipelines stay on this device.</span>
      </div>
      </section>
      <section style={css("border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:14px")}>
      <div style={css("display:flex;flex-wrap:wrap;align-items:center;gap:10px")}>
      <h2 style={css("flex:1;min-width:120px;font-size:14px;font-weight:500")}>Step library</h2>
      <div style={css("display:flex;align-items:center;gap:7px;height:32px;padding:0 9px;border:1px solid var(--line);border-radius:8px;background:var(--panel2);min-width:180px")}>
      <span className="ms" style={css("font-size:16px;color:var(--ink3)")}>{ICON.search}</span>
      <input value={v.libQ} onChange={v.onLibQ} placeholder="Search steps" aria-label="Search steps" style={css("flex:1;min-width:0;border:0;background:none;outline:none;font-size:12.5px")} />
      </div>
      </div>
      <ul style={css("display:grid;grid-template-columns:repeat(auto-fit,minmax(158px,1fr));gap:8px;margin-top:12px")}>

                        {(v.library ?? []).map((l, lI) => (
                    
      <li key={lI}>
      <button onClick={l.add} style={css("width:100%;height:100%;display:flex;flex-direction:column;gap:5px;padding:10px;border:1px solid var(--line);border-radius:10px;background:var(--panel2);transition:border-color 160ms ease")} className="dc-i12">
      <span style={css("display:flex;align-items:center;gap:7px;width:100%")}><span className="ms" style={css(`font-size:17px;color:${l.color}`)}>{ICON[l.icon]}</span><span style={css("display:block;flex:1;min-width:0;font-size:12.5px;font-weight:500;white-space:nowrap;overflow:hidden;text-overflow:ellipsis")}>{l.name}</span><span className="ms" style={css("font-size:16px;color:var(--em)")}>{ICON.add}</span></span>
      <span style={css("font-size:10.5px;color:var(--ink3);text-align:left;line-height:1.35")}>{l.purpose}</span>
      <span style={css(`display:inline-flex;align-items:center;gap:4px;font-size:10px;color:${l.modeColor}`)}><span style={css(`width:5px;height:5px;border-radius:50%;background:${l.modeColor}`)}></span>{l.mode}</span>
      </button>
      </li>

                        ))}
      </ul>
      </section>
      </div>
      <aside style={css("flex:1 1 280px;min-width:0;display:flex;flex-direction:column;gap:14px")}>
      <div style={css("border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:14px")}>
      <h2 style={css("font-size:14px;font-weight:500")}>Step settings</h2>
      <div style={css("display:flex;align-items:center;gap:9px;margin-top:11px;padding-bottom:11px;border-bottom:1px solid var(--line)")}>
      <span className="ms" style={css(`font-size:19px;color:${v.selColor}`)}>{ICON[v.selIcon]}</span>
      <span style={css("display:block;min-width:0")}><span style={css("display:block;font-size:12.5px;font-weight:500")}>{v.selName}</span><span style={css("display:block;font-size:10.5px;color:var(--ink3)")}>{v.selPurpose}</span></span>
      </div>
      <div style={css("display:flex;flex-direction:column;gap:11px;margin-top:11px")}>

                        {(v.selOpts ?? []).map((o, oI) => (
                    
      <div key={oI}>
      <span style={css("display:block;font-size:11px;color:var(--ink3);margin-bottom:4px")}>{o.label}</span>
      <div style={css("display:flex;align-items:center;height:32px;padding:0 9px;border:1px solid var(--line);border-radius:8px;background:var(--panel2);font-size:12px;color:var(--ink2)")}>{o.value}</div>
      </div>

                        ))}
      <div>
      <span style={css("display:block;font-size:11px;color:var(--ink3);margin-bottom:4px")}>Processing</span>
      <div style={css("display:flex;align-items:center;gap:7px;height:32px;padding:0 9px;border:1px solid var(--line);border-radius:8px;background:var(--panel2);font-size:12px;color:var(--ink2)")}><span className="ms" style={css("font-size:15px;color:var(--em)")}>{ICON.devices}</span>{v.selModeLabel}</div>
      </div>
      <div style={css("display:flex;gap:9px;padding:10px;border:1px solid var(--line);border-radius:9px;background:var(--panel2)")}>
      <span className="ms" style={css("font-size:17px;color:var(--em)")}>{ICON.lock_clock}</span>
      <span style={css("display:block;font-size:11.5px;color:var(--ink2);line-height:1.5")}>Last good output is always preserved. If a step fails, the output of the last successful step stays available — this is product behaviour, not a setting.</span>
      </div>
      </div>
      </div>
      <div style={css("border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:14px")}>
      <h2 style={css("font-size:14px;font-weight:500")}>Starter recipes</h2>
      <ul style={css("margin-top:10px;display:flex;flex-direction:column;gap:6px")}>

                        {(v.recipes ?? []).map((r, rI) => (
                    
      <li key={rI}><button onClick={r.go} style={css("display:flex;align-items:center;gap:9px;width:100%;min-height:44px;padding:7px 9px;border:1px solid var(--line);border-radius:9px;background:var(--panel2);transition:border-color 160ms ease")} className="dc-i13">
      <span style={css("display:block;flex:1;min-width:0")}><span style={css("display:block;font-size:12.5px;font-weight:500")}>{r.name}</span><span style={css("display:block;font-size:10.5px;color:var(--ink3)")}>{r.sub}</span></span>
      <span style={css("font-size:10.5px;color:var(--ink3)")}>{r.count}</span>
      </button></li>

                        ))}
      </ul>
      </div>
      <div style={css("border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:14px")}>
      <h2 style={css("font-size:14px;font-weight:500")}>Saved on this device</h2>
      <ul style={css("margin-top:10px;display:flex;flex-direction:column;gap:4px")}>

                        {(v.saved ?? []).map((s, sI) => (
                    
      <li key={sI} style={css("display:flex;align-items:center;gap:8px")}>
      <button onClick={s.load} style={css("flex:1;min-width:0;display:flex;align-items:center;gap:8px;min-height:36px;font-size:12.5px")}><span className="ms" style={css("font-size:16px;color:var(--em)")}>{ICON.account_tree}</span><span style={css("flex:1;white-space:nowrap;overflow:hidden;text-overflow:ellipsis")}>{s.name}</span><span style={css("font-size:10.5px;color:var(--ink3)")}>{s.count}</span></button>
      <button onClick={s.del} aria-label={s.delLabel} style={css("display:flex;width:28px;height:28px;align-items:center;justify-content:center;border-radius:7px")}><span className="ms" style={css("font-size:16px;color:var(--ink3)")}>{ICON.close}</span></button>
      </li>

                        ))}
      </ul>
      <p style={css("margin-top:10px;padding-top:10px;border-top:1px solid var(--line);font-size:11px;color:var(--ink3)")}>Names and settings only. No documents and no filenames are stored.</p>
      </div>
      </aside>
      </div>
      </div>

            </>)}

            {Boolean(v.isBatch) && (<>
        
      <div style={css("max-width:1180px;margin:0 auto;padding:20px 24px 0;animation:slidein 200ms ease both")}>
      <div style={css("display:flex;flex-wrap:wrap;align-items:flex-end;gap:12px")}>
      <div style={css("flex:1;min-width:200px")}>
      <h1 style={css("font-size:clamp(21px,3.6vw,26px);font-weight:600;letter-spacing:-.02em")}>Batch</h1>
      <p style={css("margin-top:3px;font-size:12.5px;color:var(--ink3)")}>One tool across many files. A failure never stops the rest.</p>
      </div>
      <label style={css("display:flex;align-items:center;gap:7px;font-size:11.5px;color:var(--ink3)")}>Preview state
                  <select value={v.batchStateValue} onChange={v.setBatchState} aria-label="Preview a batch state" style={css("height:30px;padding:0 8px;border:1px solid var(--line);border-radius:8px;background:var(--panel2);font-size:12px;color:var(--ink2)")}>

                      {(v.batchStates ?? []).map((s, sI) => (
                        <option key={sI} value={s.v}>{s.label}</option>
                      ))}
      </select>
      </label>
      </div>
      <section style={css("margin-top:16px;border:1px solid var(--line);border-radius:14px;background:var(--panel);overflow:hidden")}>
      <div style={css("display:flex;flex-wrap:wrap;align-items:center;gap:12px;padding:14px")}>
      <span style={css("display:flex;align-items:center;gap:9px;flex:1;min-width:200px")}>
      <span className="ms" style={css(`font-size:20px;color:${v.batchToolColor}`)}>{ICON[v.batchToolIcon]}</span>
      <span style={css("display:block;min-width:0")}><span style={css("display:block;font-size:13px;font-weight:500")}>{v.batchToolName}</span><span style={css("display:block;font-size:11px;color:var(--ink3)")}>{v.batchToolPurpose}</span></span>
      </span>
      <select value={v.batchToolValue} onChange={v.pickBatchTool} aria-label="Choose the batch tool" style={css("height:34px;padding:0 9px;border:1px solid var(--line);border-radius:9px;background:var(--panel2);font-size:12.5px")}>

                      {(v.batchToolOpts ?? []).map((o, oI) => (
                        <option key={oI} value={o.v}>{o.label}</option>
                      ))}
      </select>
      <div style={css("display:flex;gap:8px")}>
      <span style={css("display:flex;flex-direction:column;align-items:center;min-width:64px;padding:6px 10px;border:1px solid var(--line);border-radius:9px;background:var(--panel2)")}><span style={css("font-size:16px;font-weight:600;color:var(--em)")}>{v.batchOkCount}</span><span style={css("font-size:10px;color:var(--ink3)")}>Successful</span></span>
      <span style={css("display:flex;flex-direction:column;align-items:center;min-width:64px;padding:6px 10px;border:1px solid var(--line);border-radius:9px;background:var(--panel2)")}><span style={css("font-size:16px;font-weight:600;color:var(--coral)")}>{v.batchBadCount}</span><span style={css("font-size:10px;color:var(--ink3)")}>Failed</span></span>
      </div>
      </div>

                  {Boolean(v.batchNoTool) && (<>
              
      <div style={css("padding:34px 20px;text-align:center;border-top:1px solid var(--line)")}>
      <span className="ms" style={css("font-size:30px;color:var(--ink3)")}>{ICON.apps}</span>
      <h3 style={css("margin-top:8px;font-size:14px;font-weight:500")}>Choose a tool first</h3>
      <p style={css("margin-top:4px;font-size:12.5px;color:var(--ink3)")}>A batch runs one tool across every file you add.</p>
      <button onClick={v.goTools} style={css("margin-top:11px;height:34px;padding:0 14px;border-radius:8px;background:var(--em);color:var(--em-ink,#04231A);font-size:12.5px;font-weight:500")}>Browse tools</button>
      </div>

                  </>)}

                  {Boolean(v.batchInvalid) && (<>
              
      <div role="alert" style={css("display:flex;gap:9px;margin:0 14px 14px;padding:11px;border:1px solid var(--amber);border-radius:10px;background:var(--amberSoft);font-size:12px;color:var(--ink2)")}><span className="ms" style={css("font-size:17px;color:var(--amber)")}>{ICON.rule}</span>One file in this batch is not a supported input for this tool. Remove it, or pick a tool that accepts every file.</div>

                  </>)}

                  {Boolean(v.batchHasFiles) && (<>
              
      <div style={css("border-top:1px solid var(--line);overflow-x:auto")}>
      <table style={css("width:100%;min-width:640px;font-size:12.5px")}>
      <thead>
      <tr style={css("text-align:left;color:var(--ink3)")}>
      <th scope="col" style={css("font-weight:400;font-size:11px;letter-spacing:.06em;text-transform:uppercase;padding:9px 14px")}>File name</th>
      <th scope="col" style={css("font-weight:400;font-size:11px;letter-spacing:.06em;text-transform:uppercase;padding:9px 8px")}>Tool</th>
      <th scope="col" style={css("font-weight:400;font-size:11px;letter-spacing:.06em;text-transform:uppercase;padding:9px 8px")}>Status</th>
      <th scope="col" style={css("font-weight:400;font-size:11px;letter-spacing:.06em;text-transform:uppercase;padding:9px 8px")}>Current step</th>
      <th scope="col" style={css("font-weight:400;font-size:11px;letter-spacing:.06em;text-transform:uppercase;padding:9px 8px;width:150px")}>Progress</th>
      <th scope="col" style={css("font-weight:400;font-size:11px;letter-spacing:.06em;text-transform:uppercase;padding:9px 14px;width:88px")}>Actions</th>
      </tr>
      </thead>
      <tbody>

                          {(v.batchRows ?? []).map((b, bI) => (
                      
      <tr key={bI} style={css("border-top:1px solid var(--line);transition:background 200ms ease")}>
      <td style={css("padding:9px 14px")}><span style={css("display:block;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;max-width:180px")}>{b.name}</span><span style={css("display:block;font-size:10.5px;color:var(--ink3)")}>{b.size}</span></td>
      <td style={css("padding:9px 8px;color:var(--ink3)")}>{b.tool}</td>
      <td style={css("padding:9px 8px")}><span style={css(`display:inline-flex;align-items:center;gap:6px;color:${b.color}`)}><span className="ms" style={css("font-size:16px")}>{ICON[b.statusIcon]}</span>{b.status}</span></td>
      <td style={css("padding:9px 8px;color:var(--ink3)")}>{b.step}</td>
      <td style={css("padding:9px 8px")}>
      <span style={css("display:flex;align-items:center;gap:8px")}>
      <span style={css("flex:1;height:5px;border-radius:99px;background:var(--panel3);overflow:hidden")}><span style={css(`display:block;height:100%;width:${b.progW};background:${b.progColor};transition:width 220ms linear`)}></span></span>
      <span style={css("font-size:10.5px;color:var(--ink3);font-family:'Geist Mono',ui-monospace,monospace;min-width:32px;text-align:right")}>{b.prog}</span>
      </span>
      </td>
      <td style={css("padding:9px 14px")}>
      <span style={css("display:flex;gap:3px;justify-content:flex-end")}>
      <button onClick={b.retry} aria-label={b.retryLabel} style={css("display:flex;width:28px;height:28px;align-items:center;justify-content:center;border-radius:7px")}><span className="ms" style={css("font-size:16px;color:var(--ink3)")}>{ICON.refresh}</span></button>
      <button onClick={b.skip} aria-label={b.skipLabel} style={css("display:flex;width:28px;height:28px;align-items:center;justify-content:center;border-radius:7px")}><span className="ms" style={css("font-size:16px;color:var(--ink3)")}>{ICON.close}</span></button>
      </span>
      </td>
      </tr>

                          ))}
      </tbody>
      </table>
      </div>

                  </>)}

                  {Boolean(v.batchCancelled) && (<>
              
      <div style={css("display:flex;gap:9px;margin:14px;padding:11px;border:1px solid var(--line2);border-radius:10px;background:var(--panel2);font-size:12px;color:var(--ink2)")}><span className="ms" style={css("font-size:17px;color:var(--amber)")}>{ICON.cancel}</span>Batch cancelled. Finished results are still listed; nothing was stored.</div>

                  </>)}
      <div style={css("display:flex;flex-wrap:wrap;gap:9px;padding:14px;border-top:1px solid var(--line)")}>
      <button onClick={v.runBatchNow} style={css("height:36px;padding:0 15px;border-radius:9px;background:var(--em);color:var(--em-ink,#04231A);font-size:12.5px;font-weight:500")}>Run batch</button>

                    {Boolean(v.batchRunning) && (<>
                      <button onClick={v.stopBatch} style={css("height:36px;padding:0 13px;border:1px solid var(--coral);border-radius:9px;font-size:12.5px;color:var(--coral)")}>Stop batch</button>
                    </>)}
      <button onClick={v.downloadOk} style={css("height:36px;padding:0 13px;border:1px solid var(--line);border-radius:9px;font-size:12.5px;color:var(--ink2)")}>Download successful results</button>

                    {Boolean(v.batchHasFailures) && (<>
                
      <button onClick={v.retryFailed} style={css("height:36px;padding:0 13px;border:1px solid var(--line);border-radius:9px;font-size:12.5px;color:var(--ink2)")}>Retry failed</button>
      <button onClick={v.continueFailures} style={css("height:36px;padding:0 13px;border:1px solid var(--line);border-radius:9px;font-size:12.5px;color:var(--ink2)")}>Clear failures</button>

                    </>)}
      <button onClick={v.addBatchFiles} style={css("height:36px;padding:0 13px;border:1px solid var(--line);border-radius:9px;font-size:12.5px;color:var(--ink2)")}>Add files</button>
      </div>
      </section>

                {Boolean(v.batchHasFailures) && (<>
            
      <div style={css("display:flex;flex-wrap:wrap;gap:14px;margin-top:14px")}>
      <section style={css("flex:1 1 280px;border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:14px")}>
      <h2 style={css("font-size:14px;font-weight:500;display:flex;align-items:center;gap:7px")}><span className="ms" style={css("font-size:17px;color:var(--em)")}>{ICON.check_circle}</span>Successful — {v.batchOkCount}</h2>
      <ul style={css("margin-top:10px;display:flex;flex-direction:column;gap:5px")}>

                        {(v.batchOk ?? []).map((b, bI) => (
                    
      <li key={bI} style={css("display:flex;align-items:center;gap:8px;font-size:12px")}><span className="ms" style={css("font-size:15px;color:var(--em)")}>{ICON.draft}</span><span style={css("flex:1;min-width:0;white-space:nowrap;overflow:hidden;text-overflow:ellipsis")}>{b.name}</span><span style={css("font-size:10.5px;color:var(--ink3)")}>{b.size}</span></li>

                        ))}
      </ul>
      <button onClick={v.downloadOk} style={css("margin-top:11px;height:32px;padding:0 12px;border-radius:8px;background:var(--em);color:var(--em-ink,#04231A);font-size:12px;font-weight:500")}>Download as ZIP</button>
      </section>
      <section style={css("flex:1 1 280px;border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:14px")}>
      <h2 style={css("font-size:14px;font-weight:500;display:flex;align-items:center;gap:7px")}><span className="ms" style={css("font-size:17px;color:var(--coral)")}>{ICON.error}</span>Failed — {v.batchBadCount}</h2>
      <ul style={css("margin-top:10px;display:flex;flex-direction:column;gap:5px")}>

                        {(v.batchBad ?? []).map((b, bI) => (
                    
      <li key={bI} style={css("display:flex;align-items:center;gap:8px;font-size:12px")}><span className="ms" style={css("font-size:15px;color:var(--coral)")}>{ICON.draft}</span><span style={css("flex:1;min-width:0;white-space:nowrap;overflow:hidden;text-overflow:ellipsis")}>{b.name}</span><span style={css("font-size:10.5px;color:var(--ink3)")}>{b.step}</span></li>

                        ))}
      </ul>
      <button onClick={v.retryFailed} style={css("margin-top:11px;height:32px;padding:0 12px;border:1px solid var(--line2);border-radius:8px;font-size:12px")}>Retry failed</button>
      </section>
      </div>

                </>)}
      <p style={css("margin-top:14px;font-size:11.5px;color:var(--ink3)")}>Filenames appear in the active batch only. They are never written to Activity or any history.</p>
      </div>

            </>)}

            {Boolean(v.isStuff) && (<>
        
      <div style={css("max-width:1180px;margin:0 auto;padding:20px 24px 0;animation:slidein 200ms ease both")}>
      <div style={css("display:flex;flex-wrap:wrap;align-items:flex-end;gap:12px")}>
      <div style={css("flex:1;min-width:200px")}>
      <h1 style={css("font-size:clamp(21px,3.6vw,26px);font-weight:600;letter-spacing:-.02em")}>My Stuff</h1>
      <p style={css("margin-top:3px;font-size:12.5px;color:var(--ink3)")}>Stored on this device. No input or output documents are stored here.</p>
      </div>
      <span style={css("display:inline-flex;align-items:center;gap:6px;height:30px;padding:0 11px;border:1px solid var(--line);border-radius:99px;background:var(--panel);font-size:11.5px;color:var(--ink2)")}><span className="ms" style={css("font-size:16px;color:var(--em)")}>{ICON.smartphone}</span>Device only</span>
      </div>
      <ul style={css("display:grid;grid-template-columns:repeat(auto-fit,minmax(158px,1fr));gap:10px;margin-top:16px")}>

                  {(v.stuffTiles ?? []).map((t, tI) => (
              
      <li key={tI} style={css("display:flex;align-items:center;gap:10px;padding:13px;border:1px solid var(--line);border-radius:12px;background:var(--panel)")}>
      <span className="ms" style={css(`font-size:20px;color:${t.color}`)}>{ICON[t.icon]}</span>
      <span style={css("display:block;min-width:0")}><span style={css("display:block;font-size:12px;color:var(--ink3)")}>{t.label}</span><span style={css("display:block;font-size:14px;font-weight:500")}>{t.value}</span></span>
      </li>

                  ))}
      </ul>
      <div style={css("display:flex;flex-wrap:wrap;gap:14px;margin-top:14px;align-items:flex-start")}>
      <section style={css("flex:2 1 420px;min-width:0;border:1px solid var(--line);border-radius:14px;background:var(--panel);overflow:hidden")}>
      <div style={css("display:flex;flex-wrap:wrap;align-items:baseline;gap:10px;padding:14px 14px 11px")}>
      <h2 style={css("flex:1;min-width:120px;font-size:14px;font-weight:500")}>Activity</h2>
      <span style={css("font-size:11px;color:var(--ink3)")}>Tool, date and settings only — never filenames</span>
      <button onClick={v.eraseActivity} style={css("font-size:12px;color:var(--coral);white-space:nowrap")}>Erase activity</button>
      </div>

                    {Boolean(v.hasActivity) && (<>
                
      <div style={css("overflow-x:auto")}>
      <table style={css("width:100%;min-width:440px;font-size:12.5px")}>
      <thead><tr style={css("text-align:left;color:var(--ink3)")}>
      <th scope="col" style={css("font-weight:400;font-size:11px;letter-spacing:.06em;text-transform:uppercase;padding:8px 14px")}>Tool used</th>
      <th scope="col" style={css("font-weight:400;font-size:11px;letter-spacing:.06em;text-transform:uppercase;padding:8px")}>Date</th>
      <th scope="col" style={css("font-weight:400;font-size:11px;letter-spacing:.06em;text-transform:uppercase;padding:8px")}>Settings</th>
      <th scope="col" style={css("padding:8px 14px;width:44px")}></th>
      </tr></thead>
      <tbody>

                            {(v.activity ?? []).map((a, aI) => (
                        
      <tr key={aI} style={css("border-top:1px solid var(--line)")}>
      <td style={css("padding:9px 14px")}><span style={css("display:flex;align-items:center;gap:8px")}><span className="ms" style={css(`font-size:16px;color:${a.color}`)}>{ICON[a.icon]}</span>{a.name}</span></td>
      <td style={css("padding:9px 8px;color:var(--ink3);font-family:'Geist Mono',ui-monospace,monospace;font-size:11.5px")}>{a.date}</td>
      <td style={css("padding:9px 8px;color:var(--ink2)")}>{a.settings}</td>
      <td style={css("padding:9px 14px")}><button onClick={a.del} aria-label={a.delLabel} style={css("display:flex;width:28px;height:28px;align-items:center;justify-content:center;border-radius:7px")}><span className="ms" style={css("font-size:16px;color:var(--ink3)")}>{ICON.close}</span></button></td>
      </tr>

                            ))}
      </tbody>
      </table>
      </div>

                    </>)}
      <div style={css("padding:11px 14px;border-top:1px solid var(--line);font-size:11.5px;color:var(--ink3)")}>History is local. No documents and no filenames are stored.</div>
      </section>
      <section style={css("flex:1 1 280px;min-width:0;border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:14px")}>
      <div style={css("display:flex;align-items:center;gap:9px")}>
      <span className="ms" style={css("font-size:20px;color:var(--em)")}>{ICON.lock}</span>
      <span style={css("display:block;flex:1;min-width:0")}><span style={css("display:block;font-size:14px;font-weight:500")}>Vault</span><span style={css("display:block;font-size:11.5px;color:var(--ink3)")}>Passwords only. Secure and local.</span></span>
      </div>
      <div style={css("margin-top:12px;padding:16px;border:1px solid var(--line);border-radius:11px;background:var(--panel2);text-align:center")}>
      <span className="ms" style={css("font-size:26px;color:var(--em)")}>{ICON.lock}</span>
      <p style={css("margin-top:6px;font-size:12.5px")}>{v.vaultCount} entries · locked</p>
      <p style={css("margin-top:3px;font-size:11px;color:var(--ink3)")}>Your vault never leaves this device.</p>
      <button onClick={v.goVault} style={css("margin-top:11px;height:34px;padding:0 14px;border-radius:8px;background:var(--em);color:var(--em-ink,#04231A);font-size:12.5px;font-weight:500;white-space:nowrap")}>Open vault</button>
      </div>
      <button onClick={v.exportBackup} style={css("display:flex;align-items:center;gap:9px;width:100%;margin-top:11px;padding:11px;border:1px solid var(--line);border-radius:10px;background:var(--panel2)")}>
      <span className="ms" style={css("font-size:18px;color:var(--teal)")}>{ICON.enhanced_encryption}</span>
      <span style={css("display:block;min-width:0;text-align:left")}><span style={css("display:block;font-size:12.5px")}>Export encrypted backup</span><span style={css("display:block;font-size:10.5px;color:var(--ink3)")}>Passphrase encrypted. Includes passwords, excludes cloud tokens.</span></span>
      </button>
      </section>
      </div>
      <div style={css("display:flex;flex-wrap:wrap;gap:14px;margin-top:14px;align-items:flex-start")}>
      <section style={css("flex:1 1 260px;min-width:0;border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:14px")}>
      <h2 style={css("font-size:14px;font-weight:500")}>Assets</h2>
      <p style={css("margin-top:3px;font-size:11px;color:var(--ink3)")}>Signatures, logos and stamps you saved on purpose. Not documents.</p>
      <ul style={css("margin-top:11px;display:flex;flex-direction:column;gap:5px")}>

                      {(v.assets ?? []).map((a, aI) => (
                  
      <li key={aI} style={css("display:flex;align-items:center;gap:9px;padding:7px 8px;border-radius:8px;background:var(--panel2)")}>
      <span className="ms" style={css("font-size:17px;color:var(--em)")}>{ICON[a.icon]}</span>
      <span style={css("display:block;flex:1;min-width:0")}><span style={css("display:block;font-size:12.5px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis")}>{a.name}</span><span style={css("display:block;font-size:10.5px;color:var(--ink3)")}>{a.kind}</span></span>
      <button onClick={a.del} aria-label={a.delLabel} style={css("display:flex;width:26px;height:26px;align-items:center;justify-content:center;border-radius:6px")}><span className="ms" style={css("font-size:15px;color:var(--ink3)")}>{ICON.delete}</span></button>
      </li>

                      ))}
      </ul>
      </section>
      <section style={css("flex:1 1 260px;min-width:0;border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:14px")}>
      <h2 style={css("font-size:14px;font-weight:500")}>Bates counters</h2>
      <p style={css("margin-top:3px;font-size:11px;color:var(--ink3)")}>The next number only. No filenames, no documents.</p>
      <ul style={css("margin-top:11px;display:flex;flex-direction:column;gap:5px")}>

                      {(v.bates ?? []).map((b, bI) => (
                  
      <li key={bI} style={css("display:flex;align-items:center;gap:9px;padding:7px 8px;border-radius:8px;background:var(--panel2)")}>
      <span className="ms" style={css("font-size:17px;color:var(--violet)")}>{ICON.pin}</span>
      <span style={css("flex:1;min-width:0;font-size:12.5px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis")}>{b.name}</span>
      <span style={css("font-size:12px;font-family:'Geist Mono',ui-monospace,monospace;color:var(--ink2)")}>{b.next}</span>
      <button onClick={b.inc} aria-label={b.incLabel} style={css("display:flex;width:26px;height:26px;align-items:center;justify-content:center;border-radius:6px")}><span className="ms" style={css("font-size:15px;color:var(--ink3)")}>{ICON.add}</span></button>
      </li>

                      ))}
      </ul>
      </section>
      <section style={css("flex:1 1 260px;min-width:0;border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:14px")}>
      <h2 style={css("font-size:14px;font-weight:500")}>Tool defaults</h2>
      <p style={css("margin-top:3px;font-size:11px;color:var(--ink3)")}>Your preferred settings, remembered locally.</p>
      <ul style={css("margin-top:11px;display:flex;flex-direction:column;gap:5px")}>

                      {(v.defaults ?? []).map((d, dI) => (
                  
      <li key={dI} style={css("display:flex;align-items:center;gap:9px;padding:7px 8px;border-radius:8px;background:var(--panel2)")}>
      <span className="ms" style={css(`font-size:17px;color:${d.color}`)}>{ICON[d.icon]}</span>
      <span style={css("display:block;flex:1;min-width:0")}><span style={css("display:block;font-size:12.5px")}>{d.name}</span><span style={css("display:block;font-size:10.5px;color:var(--ink3);white-space:nowrap;overflow:hidden;text-overflow:ellipsis")}>{d.value}</span></span>
      </li>

                      ))}
      </ul>
      </section>
      </div>
      <section style={css("margin-top:14px;border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:14px")}>
      <div style={css("display:flex;flex-wrap:wrap;align-items:baseline;gap:10px")}>
      <h2 style={css("flex:1;min-width:140px;font-size:14px;font-weight:500")}>Connections</h2>
      <span style={css("font-size:11.5px;color:var(--ink3)")}>All connections are local until you disconnect.</span>
      </div>
      <ul style={css("display:grid;grid-template-columns:repeat(auto-fit,minmax(200px,1fr));gap:8px;margin-top:12px")}>

                    {(v.conns ?? []).map((c, cI) => (
                
      <li key={cI} style={css("display:flex;align-items:center;gap:9px;padding:10px;border:1px solid var(--line);border-radius:10px;background:var(--panel2)")}>
      <span className="ms" style={css("font-size:18px;color:var(--ink2)")}>{ICON[c.icon]}</span>
      <span style={css("display:block;flex:1;min-width:0")}><span style={css("display:block;font-size:12.5px")}>{c.name}</span><span style={css(`display:block;font-size:10.5px;color:${c.color}`)}>{c.state}</span></span>
      <button onClick={c.toggle} style={css("height:28px;padding:0 10px;border:1px solid var(--line2);border-radius:7px;font-size:11.5px;color:var(--ink2)")}>{c.action}</button>
      </li>

                    ))}
      </ul>
      <div style={css("display:flex;flex-wrap:wrap;gap:9px;margin-top:14px;padding-top:13px;border-top:1px solid var(--line)")}>
      <button onClick={v.exportBackup} style={css("height:34px;padding:0 13px;border:1px solid var(--line);border-radius:8px;font-size:12.5px;color:var(--ink2)")}>Export encrypted backup</button>
      <button onClick={v.eraseAll} style={css("height:34px;padding:0 13px;border:1px solid var(--coral);border-radius:8px;font-size:12.5px;color:var(--coral);white-space:nowrap")}>Erase everything local</button>
      </div>
      </section>
      </div>

            </>)}

            {Boolean(v.isVault) && (<>
        
      <div style={css("max-width:820px;margin:0 auto;padding:20px 24px 0;animation:slidein 200ms ease both")}>
      <div style={css("display:flex;flex-wrap:wrap;align-items:flex-end;gap:12px")}>
      <div style={css("flex:1;min-width:200px")}>
      <h1 style={css("font-size:clamp(21px,3.6vw,26px);font-weight:600;letter-spacing:-.02em")}>Vault</h1>
      <p style={css("margin-top:3px;font-size:12.5px;color:var(--ink3)")}>Passwords only. Never documents. Your vault never leaves this device.</p>
      </div>

                  {Boolean(v.vaultOpen) && (<>
                    <button onClick={v.lock} style={css("display:flex;align-items:center;gap:7px;height:32px;padding:0 12px;border:1px solid var(--line);border-radius:8px;background:var(--panel);font-size:12.5px;white-space:nowrap")}><span className="ms" style={css("font-size:17px;color:var(--em)")}>{ICON.lock}</span>Lock vault</button>
                  </>)}
      </div>

                {Boolean(v.vaultLocked) && (<>
            
      <section style={css("margin-top:18px;border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:28px 20px;text-align:center")}>
      <span className="ms" style={css("font-size:34px;color:var(--em)")}>{ICON.lock}</span>
      <h2 style={css("margin-top:10px;font-size:17px;font-weight:500")}>Vault is locked</h2>
      <p style={css("margin-top:4px;font-size:12.5px;color:var(--ink3)")}>Protection level: {v.vaultProtectLabel}. Nothing is sent anywhere.</p>
      <p style={css("max-width:52ch;margin:10px auto 0;padding:9px 11px;border:1px solid var(--amber);border-radius:9px;background:var(--amberSoft);font-size:11.5px;color:var(--ink2);line-height:1.5")}>{v.protoWarning}</p>

                    {Boolean(v.needsSetup) && (<>
                      <p style={css("margin-top:9px;font-size:12px;color:var(--ink3)")}>No {v.secretKind} has been created yet.</p>
                    </>)}
      <div style={css("display:flex;flex-wrap:wrap;gap:9px;justify-content:center;margin-top:16px")}>
      <button onClick={v.beginUnlock} style={css("display:flex;align-items:center;gap:7px;height:40px;padding:0 18px;border-radius:9px;background:var(--em);color:var(--em-ink,#04231A);font-size:13px;font-weight:500")}><span className="ms" style={css("font-size:18px")}>{ICON.lock_open}</span>Unlock vault</button>
      <button onClick={v.usePin} style={css("display:flex;align-items:center;gap:7px;height:40px;padding:0 15px;border:1px solid var(--line2);border-radius:9px;font-size:13px")}><span className="ms" style={css("font-size:18px")}>{ICON.pin}</span>Use PIN</button>
      <button onClick={v.usePassphrase} style={css("display:flex;align-items:center;gap:7px;height:40px;padding:0 15px;border:1px solid var(--line2);border-radius:9px;font-size:13px")}><span className="ms" style={css("font-size:18px")}>{ICON.key}</span>Use passphrase</button>
      </div>
      </section>

                </>)}

                {Boolean(v.vaultUnlocking) && (<>
            
      <section style={css("margin-top:18px;border:1px solid var(--emLine);border-radius:14px;background:var(--panel);padding:28px 20px;text-align:center")}>
      <span className="ms" style={css("font-size:34px;color:var(--em)")}>{ICON.lock_open}</span>
      <h2 style={css("margin-top:10px;font-size:17px;font-weight:500")}>Unlocking…</h2>
      <p style={css("margin-top:4px;font-size:12.5px;color:var(--ink3)")}>Decrypting on this device.</p>
      </section>

                </>)}

                {Boolean(v.vaultSetupStage) && (<>
            
      <section style={css("margin-top:18px;border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:24px 20px;max-width:460px;margin-left:auto;margin-right:auto")}>
      <div style={css("text-align:center")}>
      <span className="ms" style={css("font-size:28px;color:var(--em)")}>{ICON.key_vertical}</span>
      <h2 style={css("margin-top:8px;font-size:16px;font-weight:500")}>Create your {v.secretKind}</h2>
      <p style={css("margin-top:4px;font-size:12px;color:var(--ink3)")}>Step 1 of 2. You will be asked to confirm it.</p>
      </div>
      <p style={css("margin-top:11px;padding:9px 11px;border:1px solid var(--amber);border-radius:9px;background:var(--amberSoft);font-size:11.5px;color:var(--ink2);line-height:1.5")}>{v.protoWarning}</p>

                    {Boolean(v.vaultPinKind) && (<>
                
      <input type="password" inputMode="numeric" value={v.pin} onChange={v.onPin} aria-label="New PIN" aria-invalid={v.hasPinErr} style={css("display:block;width:160px;height:44px;margin:14px auto 0;padding:0 12px;border:1px solid var(--line2);border-radius:10px;background:var(--panel2);font-size:20px;letter-spacing:.4em;text-align:center;font-family:'Geist Mono',ui-monospace,monospace")} />

                      {Boolean(v.hasPinErr) && (<>
                        <p role="alert" style={css("margin-top:9px;font-size:12px;color:var(--coral);text-align:center")}>{v.pinErr}</p>
                      </>)}

                    </>)}

                    {Boolean(v.vaultPassKind) && (<>
                
      <span style={css("display:flex;align-items:center;gap:8px;margin-top:14px;padding:0 10px;height:42px;border:1px solid var(--line2);border-radius:10px;background:var(--panel2)")}>
      <input type={v.passType} value={v.pass} onChange={v.onPass} aria-label="New passphrase" aria-invalid={v.hasPassErr} style={css("flex:1;min-width:0;border:0;background:none;outline:none;font-size:13.5px;font-family:'Geist Mono',ui-monospace,monospace")} />
      <button onClick={v.togglePassShow} aria-pressed={v.passShow} aria-label="Show or hide passphrase" style={css("display:flex")}><span className="ms" style={css("font-size:18px;color:var(--ink3)")}>{ICON.visibility}</span></button>
      </span>

                      {Boolean(v.hasPassErr) && (<>
                        <p role="alert" style={css("margin-top:9px;font-size:12px;color:var(--coral)")}>{v.passErr}</p>
                      </>)}

                    </>)}
      <div style={css("display:flex;flex-wrap:wrap;gap:9px;justify-content:center;margin-top:14px")}>
      <button onClick={v.setupSecret} style={css("height:38px;padding:0 16px;border-radius:9px;background:var(--em);color:var(--em-ink,#04231A);font-size:12.5px;font-weight:500;white-space:nowrap")}>Continue</button>
      <button onClick={v.cancelUnlock} style={css("height:38px;padding:0 13px;border:1px solid var(--line);border-radius:9px;font-size:12.5px;color:var(--ink2)")}>Cancel</button>
      </div>
      </section>

                </>)}

                {Boolean(v.vaultConfirmStage) && (<>
            
      <section style={css("margin-top:18px;border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:24px 20px;max-width:460px;margin-left:auto;margin-right:auto")}>
      <div style={css("text-align:center")}>
      <span className="ms" style={css("font-size:28px;color:var(--em)")}>{ICON.done_all}</span>
      <h2 style={css("margin-top:8px;font-size:16px;font-weight:500")}>Confirm your {v.secretKind}</h2>
      <p style={css("margin-top:4px;font-size:12px;color:var(--ink3)")}>Step 2 of 2. Enter the same value again.</p>
      </div>

                    {Boolean(v.vaultPinKind) && (<>
                
      <input type="password" inputMode="numeric" value={v.pin} onChange={v.onPin} aria-label="Confirm PIN" aria-invalid={v.hasPinErr} style={css("display:block;width:160px;height:44px;margin:14px auto 0;padding:0 12px;border:1px solid var(--line2);border-radius:10px;background:var(--panel2);font-size:20px;letter-spacing:.4em;text-align:center;font-family:'Geist Mono',ui-monospace,monospace")} />

                      {Boolean(v.hasPinErr) && (<>
                        <p role="alert" style={css("margin-top:9px;font-size:12px;color:var(--coral);text-align:center")}>{v.pinErr}</p>
                      </>)}

                    </>)}

                    {Boolean(v.vaultPassKind) && (<>
                
      <span style={css("display:flex;align-items:center;gap:8px;margin-top:14px;padding:0 10px;height:42px;border:1px solid var(--line2);border-radius:10px;background:var(--panel2)")}>
      <input type={v.passType} value={v.pass} onChange={v.onPass} aria-label="Confirm passphrase" aria-invalid={v.hasPassErr} style={css("flex:1;min-width:0;border:0;background:none;outline:none;font-size:13.5px;font-family:'Geist Mono',ui-monospace,monospace")} />
      <button onClick={v.togglePassShow} aria-pressed={v.passShow} aria-label="Show or hide passphrase" style={css("display:flex")}><span className="ms" style={css("font-size:18px;color:var(--ink3)")}>{ICON.visibility}</span></button>
      </span>

                      {Boolean(v.hasPassErr) && (<>
                        <p role="alert" style={css("margin-top:9px;font-size:12px;color:var(--coral)")}>{v.passErr}</p>
                      </>)}

                    </>)}
      <div style={css("display:flex;flex-wrap:wrap;gap:9px;justify-content:center;margin-top:14px")}>
      <button onClick={v.confirmSecret} style={css("height:38px;padding:0 16px;border-radius:9px;background:var(--em);color:var(--em-ink,#04231A);font-size:12.5px;font-weight:500;white-space:nowrap")}>Create protection</button>
      <button onClick={v.restartSetup} style={css("height:38px;padding:0 13px;border:1px solid var(--line);border-radius:9px;font-size:12.5px;color:var(--ink2)")}>Start again</button>
      </div>
      </section>

                </>)}

                {Boolean(v.vaultPinStage) && (<>
            
      <section style={css("margin-top:18px;border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:24px 20px;max-width:420px;margin-left:auto;margin-right:auto;text-align:center")}>
      <span className="ms" style={css("font-size:28px;color:var(--em)")}>{ICON.pin}</span>
      <h2 style={css("margin-top:8px;font-size:16px;font-weight:500")}>Enter your PIN</h2>
      <p style={css("margin-top:4px;font-size:12px;color:var(--ink3)")}>Four to six digits, checked on this device only.</p>
      <input type="password" inputMode="numeric" value={v.pin} onChange={v.onPin} aria-label="PIN" aria-invalid={v.hasPinErr} style={css("width:160px;height:44px;margin-top:14px;padding:0 12px;border:1px solid var(--line2);border-radius:10px;background:var(--panel2);font-size:20px;letter-spacing:.4em;text-align:center;font-family:'Geist Mono',ui-monospace,monospace")} />

                    {Boolean(v.hasPinErr) && (<>
                      <p role="alert" style={css("margin-top:9px;font-size:12px;color:var(--coral)")}>{v.pinErr}</p>
                    </>)}
      <div style={css("display:flex;gap:9px;justify-content:center;margin-top:14px")}>
      <button onClick={v.submitPin} style={css("height:38px;padding:0 16px;border-radius:9px;background:var(--em);color:var(--em-ink,#04231A);font-size:12.5px;font-weight:500")}>Unlock</button>
      <button onClick={v.usePassphrase} style={css("height:38px;padding:0 13px;border:1px solid var(--line);border-radius:9px;font-size:12.5px;color:var(--ink2)")}>Use passphrase</button>
      <button onClick={v.cancelUnlock} style={css("height:38px;padding:0 13px;border:1px solid var(--line);border-radius:9px;font-size:12.5px;color:var(--ink2)")}>Cancel</button>
      </div>
      </section>

                </>)}

                {Boolean(v.vaultPassStage) && (<>
            
      <section style={css("margin-top:18px;border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:24px 20px;max-width:460px;margin-left:auto;margin-right:auto")}>
      <div style={css("text-align:center")}>
      <span className="ms" style={css("font-size:28px;color:var(--em)")}>{ICON.key}</span>
      <h2 style={css("margin-top:8px;font-size:16px;font-weight:500")}>Enter your passphrase</h2>
      <p style={css("margin-top:4px;font-size:12px;color:var(--ink3)")}>Checked on this device only.</p>
      </div>
      <div style={css("display:flex;align-items:center;gap:8px;margin-top:14px;padding:0 10px;height:42px;border:1px solid var(--line2);border-radius:10px;background:var(--panel2)")}>
      <input type={v.passType} value={v.pass} onChange={v.onPass} aria-label="Passphrase" aria-invalid={v.hasPassErr} style={css("flex:1;min-width:0;border:0;background:none;outline:none;font-size:13.5px;font-family:'Geist Mono',ui-monospace,monospace")} />
      <button onClick={v.togglePassShow} aria-pressed={v.passShow} aria-label="Show or hide passphrase" style={css("display:flex")}><span className="ms" style={css("font-size:18px;color:var(--ink3)")}>{ICON.visibility}</span></button>
      </div>

                    {Boolean(v.hasPassErr) && (<>
                      <p role="alert" style={css("margin-top:9px;font-size:12px;color:var(--coral)")}>{v.passErr}</p>
                    </>)}
      <div style={css("display:flex;flex-wrap:wrap;gap:9px;margin-top:14px")}>
      <button onClick={v.submitPass} style={css("height:38px;padding:0 16px;border-radius:9px;background:var(--em);color:var(--em-ink,#04231A);font-size:12.5px;font-weight:500")}>Unlock</button>
      <button onClick={v.usePin} style={css("height:38px;padding:0 13px;border:1px solid var(--line);border-radius:9px;font-size:12.5px;color:var(--ink2)")}>Use PIN</button>
      <button onClick={v.cancelUnlock} style={css("height:38px;padding:0 13px;border:1px solid var(--line);border-radius:9px;font-size:12.5px;color:var(--ink2)")}>Cancel</button>
      </div>
      </section>

                </>)}

                {Boolean(v.vaultOpen) && (<>
            
      <section style={css("margin-top:18px;border:1px solid var(--line);border-radius:14px;background:var(--panel);overflow:hidden;animation:rise 240ms ease both")}>
      <div style={css("display:flex;flex-wrap:wrap;align-items:center;gap:10px;padding:14px")}>
      <h2 style={css("flex:1;min-width:120px;font-size:14px;font-weight:500")}>{v.vaultCount} passwords</h2>
      <button onClick={v.addPassword} style={css("height:32px;padding:0 12px;border-radius:8px;background:var(--em);color:var(--em-ink,#04231A);font-size:12.5px;font-weight:500;white-space:nowrap")}>Add password</button>
      </div>
      <ul>

                      {(v.vaultEntries ?? []).map((v, vI) => (
                  
      <li key={vI} style={css(`display:flex;flex-wrap:wrap;align-items:center;gap:10px;padding:11px 14px;border-top:1px solid var(--line);animation:slidein 200ms ease both;animation-delay:${v.delay}`)}>
      <span className="ms" style={css("font-size:18px;color:var(--em)")}>{ICON[v.icon]}</span>
      <span style={css("display:block;flex:1;min-width:120px")}><span style={css("display:block;font-size:12.5px;font-weight:500")}>{v.name}</span><span style={css("display:block;font-size:10.5px;color:var(--ink3)")}>{v.user}</span></span>
      <span style={css("font-family:'Geist Mono',ui-monospace,monospace;font-size:12px;color:var(--ink2);min-width:130px")}>{v.secret}</span>
      <span style={css("display:flex;gap:3px")}>
      <button onClick={v.reveal} aria-label={v.revealLabel} style={css("display:flex;width:30px;height:30px;align-items:center;justify-content:center;border-radius:7px")}><span className="ms" style={css("font-size:17px;color:var(--ink3)")}>{ICON[v.revealIcon]}</span></button>
      <button onClick={v.copy} aria-label="Copy password" style={css("display:flex;width:30px;height:30px;align-items:center;justify-content:center;border-radius:7px")}><span className="ms" style={css("font-size:17px;color:var(--ink3)")}>{ICON.content_copy}</span></button>
      <button onClick={v.edit} aria-label={v.editLabel} style={css("display:flex;width:30px;height:30px;align-items:center;justify-content:center;border-radius:7px")}><span className="ms" style={css("font-size:17px;color:var(--ink3)")}>{ICON.edit}</span></button>
      <button onClick={v.del} aria-label={v.delLabel} style={css("display:flex;width:30px;height:30px;align-items:center;justify-content:center;border-radius:7px")}><span className="ms" style={css("font-size:17px;color:var(--coral)")}>{ICON.delete}</span></button>
      </span>
      </li>

                      ))}
      </ul>

                    {Boolean(v.vaultEmpty) && (<>
                
      <p style={css("padding:20px 14px;font-size:12.5px;color:var(--ink3);border-top:1px solid var(--line)")}>No passwords yet. Vault holds passwords only — never documents.</p>

                    </>)}
      </section>

                  {Boolean(v.entryEditing) && (<>
              
      <section style={css("margin-top:14px;border:1px solid var(--emLine);border-radius:14px;background:var(--panel);padding:14px;animation:rise 200ms ease both")}>
      <h2 style={css("font-size:14px;font-weight:500")}>Password details</h2>
      <div style={css("display:grid;grid-template-columns:repeat(auto-fit,minmax(190px,1fr));gap:11px;margin-top:12px")}>
      <label style={css("display:block")}><span style={css("display:block;font-size:11px;color:var(--ink3);margin-bottom:5px")}>Name</span><input value={v.dName} onChange={v.onDName} placeholder="Email (work)" aria-label="Entry name" style={css("width:100%;height:36px;padding:0 10px;border:1px solid var(--line);border-radius:9px;background:var(--panel2);font-size:12.5px")} /></label>
      <label style={css("display:block")}><span style={css("display:block;font-size:11px;color:var(--ink3);margin-bottom:5px")}>Username or note</span><input value={v.dUser} onChange={v.onDUser} placeholder="you@example.com" aria-label="Username" style={css("width:100%;height:36px;padding:0 10px;border:1px solid var(--line);border-radius:9px;background:var(--panel2);font-size:12.5px")} /></label>
      <label style={css("display:block")}><span style={css("display:block;font-size:11px;color:var(--ink3);margin-bottom:5px")}>Password</span><input value={v.dPw} onChange={v.onDPw} aria-label="Password" style={css("width:100%;height:36px;padding:0 10px;border:1px solid var(--line);border-radius:9px;background:var(--panel2);font-size:12.5px;font-family:'Geist Mono',ui-monospace,monospace")} /></label>
      </div>
      <div style={css("display:flex;gap:9px;margin-top:13px")}>
      <button onClick={v.saveEntry} style={css("height:36px;padding:0 15px;border-radius:9px;background:var(--em);color:var(--em-ink,#04231A);font-size:12.5px;font-weight:500")}>Save on this device</button>
      <button onClick={v.cancelEntry} style={css("height:36px;padding:0 13px;border:1px solid var(--line);border-radius:9px;font-size:12.5px;color:var(--ink2)")}>Cancel</button>
      </div>
      </section>

                  </>)}

                </>)}
      <section style={css("margin-top:14px;border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:14px")}>
      <h2 style={css("font-size:14px;font-weight:500")}>Protection level</h2>
      <ul role="radiogroup" style={css("display:grid;grid-template-columns:repeat(auto-fit,minmax(150px,1fr));gap:8px;margin-top:11px")}>

                    {(v.protections ?? []).map((p, pI) => (
                
      <li key={pI}><button onClick={p.go} role="radio" aria-checked={p.checked} style={css(`width:100%;min-height:44px;display:flex;align-items:center;gap:9px;padding:10px;border:1px solid ${p.bd};border-radius:10px;background:${p.bg}`)}>
      <span style={css(`display:flex;align-items:center;justify-content:center;width:15px;height:15px;flex:none;border-radius:50%;border:1px solid ${p.ring}`)}><span style={css(`width:7px;height:7px;border-radius:50%;background:${p.dot}`)}></span></span>
      <span style={css("display:block;min-width:0;text-align:left")}><span style={css("display:block;font-size:12.5px")}>{p.label}</span><span style={css("display:block;font-size:10.5px;color:var(--ink3)")}>{p.sub}</span></span>
      </button></li>

                    ))}
      </ul>
      <div style={css("display:flex;flex-wrap:wrap;align-items:center;gap:10px;margin-top:14px;padding-top:13px;border-top:1px solid var(--line)")}>
      <button onClick={v.exportBackup} style={css("height:34px;padding:0 13px;border:1px solid var(--line);border-radius:8px;font-size:12.5px;color:var(--ink2)")}>Export encrypted backup</button>

                    {Boolean(v.hasSecret) && (<>
                      <button onClick={v.clearSecret} style={css("height:34px;padding:0 13px;border:1px solid var(--coral);border-radius:8px;font-size:12.5px;color:var(--coral);white-space:nowrap")}>Remove protection</button>
                    </>)}
      <span style={css("font-size:11.5px;color:var(--ink3)")}>Passphrase encrypted. Includes passwords. Excludes cloud tokens.</span>
      </div>
      <p style={css("margin-top:11px;padding:9px 11px;border:1px solid var(--amber);border-radius:9px;background:var(--amberSoft);font-size:11.5px;color:var(--ink2);line-height:1.5")}>{v.protoWarning}</p>
      </section>
      </div>

            </>)}

            {Boolean(v.isCompare) && (<>
        
      <div style={css("max-width:1180px;margin:0 auto;padding:20px 24px 0;animation:slidein 200ms ease both")}>
      <h1 style={css("font-size:clamp(21px,3.6vw,26px);font-weight:600;letter-spacing:-.02em")}>Compare file tools</h1>
      <p style={css("margin-top:3px;font-size:12.5px;color:var(--ink3)")}>Choose what matters to you. No universal winner—results change with your priorities.</p>
      <div style={css("display:flex;flex-wrap:wrap;align-items:center;gap:8px;margin-top:14px")}>
      <span style={css("font-size:11.5px;color:var(--ink3)")}>Reorder by priority</span>

                  {(v.priorities ?? []).map((p, pI) => (
              
      <button key={pI} onClick={p.go} style={css(`height:28px;padding:0 11px;border:1px solid ${p.bd};border-radius:99px;background:${p.bg};color:${p.fg};font-size:12px;white-space:nowrap`)}>{p.label}</button>

                  ))}
      </div>
      <section style={css("margin-top:14px;border:1px solid var(--line);border-radius:14px;background:var(--panel);overflow:hidden")}>

                  {Boolean(v.notMobile) && (<>
              
      <div style={css("overflow-x:auto")}>
      <table style={css("width:100%;min-width:760px;font-size:12.5px")}>
      <thead>
      <tr style={css("text-align:left")}>
      <th scope="col" style={css("padding:12px 14px;font-weight:500;font-size:12px;width:26%")}>Feature / Priority</th>

                            {(v.cmpCols ?? []).map((c, cI) => (
                        
      <th key={cI} scope="col" style={css("padding:12px 10px;font-weight:500;font-size:12.5px")}>
      <button onClick={c.go} style={css("display:block;text-align:left;font-weight:500;font-size:12.5px")}>{c.label}</button>
      <span style={css("display:block;font-size:10px;color:var(--ink3);font-weight:400;margin-top:2px")}>{c.sub}</span>
      <span style={css(`display:block;font-size:10px;color:${c.badgeColor};font-weight:400`)}>{c.badge}</span>
      </th>

                            ))}
      </tr>
      </thead>
      <tbody>

                          {(v.cmpRows ?? []).map((r, rI) => (
                      
      <tr key={rI} style={css(`border-top:1px solid var(--line);background:${r.bg};transition:background 320ms ease`)}>
      <th scope="row" style={css("padding:10px 14px;text-align:left;font-weight:400;color:var(--ink2)")}>
      <span style={css("display:flex;align-items:center;gap:8px")}>{r.feature}<button onClick={r.openEvidence} aria-label="Show evidence" style={css("display:flex")}><span className="ms" style={css("font-size:15px;color:var(--ink3)")}>{ICON.info}</span></button></span>
      </th>

                              {(r.cells ?? []).map((c, cI) => (
                          
      <td key={cI} style={css("padding:10px")}><span style={css("display:inline-flex;align-items:center;gap:7px")}><span className="ms" style={css(`font-size:16px;color:${c.color}`)}>{ICON[c.icon]}</span>{c.v}</span></td>

                              ))}
      </tr>

                          ))}
      </tbody>
      </table>
      </div>

                  </>)}

                  {Boolean(v.isMobile) && (<>
              
      <ul style={css("padding:12px;display:flex;flex-direction:column;gap:10px")}>

                      {(v.cmpRows ?? []).map((r, rI) => (
                  
      <li key={rI} style={css(`border:1px solid var(--line);border-radius:11px;background:${r.bg};padding:11px`)}>
      <div style={css("display:flex;align-items:center;gap:8px;font-size:12.5px;font-weight:500")}>{r.feature}<button onClick={r.openEvidence} aria-label="Show evidence" style={css("display:flex;margin-left:auto")}><span className="ms" style={css("font-size:16px;color:var(--ink3)")}>{ICON.info}</span></button></div>
      <ul style={css("margin-top:8px;display:flex;flex-direction:column;gap:5px")}>

                            {(r.cells ?? []).map((c, cI) => (
                        
      <li key={cI} style={css("display:flex;align-items:center;gap:8px;font-size:12px;color:var(--ink2)")}><span className="ms" style={css(`font-size:15px;color:${c.color}`)}>{ICON[c.icon]}</span>{c.v}</li>

                            ))}
      </ul>
      </li>

                      ))}
      </ul>

                  </>)}
      <div style={css("display:flex;gap:9px;padding:12px 14px;border-top:1px solid var(--line);font-size:11.5px;color:var(--ink3)")}>
      <span className="ms" style={css("font-size:16px")}>{ICON.info}</span>
      <span>{v.cmpSlotCount}. Reference-profile columns are layout examples with no source, date or currency: no competitor fact is asserted. Only the PrivaTools column states our own product rules. No votes, ratings, testimonials or crowning.</span>
      </div>
      </section>
      <section style={css("margin-top:14px;border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:14px")}>
      <h2 style={css("font-size:14px;font-weight:500")}>No universal winner</h2>
      <p style={css("margin-top:3px;font-size:12px;color:var(--ink3)")}>Each tool has an honest advantage. Pick the one that matches your priorities.</p>
      <ul style={css("display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:9px;margin-top:12px")}>

                    {(v.advantages ?? []).map((a, aI) => (
                
      <li key={aI} style={css("padding:11px;border:1px solid var(--line);border-radius:10px;background:var(--panel2)")}>
      <span style={css("display:block;font-size:12.5px;font-weight:500")}>{a.name}</span>
      <span style={css("display:block;margin-top:4px;font-size:11.5px;color:var(--ink2);line-height:1.45")}>{a.text}</span>
      </li>

                    ))}
      </ul>
      </section>
      </div>

            </>)}

            {Boolean(v.isCompetitor) && (<>
        
      <div style={css("max-width:1000px;margin:0 auto;padding:20px 24px 0;animation:slidein 200ms ease both")}>
      <button onClick={v.goCompare} style={css("display:flex;align-items:center;gap:7px;font-size:12.5px;color:var(--em)")}><span className="ms" style={css("font-size:17px")}>{ICON.arrow_back}</span>All comparisons</button>
      <div style={css("display:flex;flex-wrap:wrap;align-items:flex-end;gap:12px;margin-top:10px")}>
      <div style={css("flex:1;min-width:220px")}>
      <h1 style={css("font-size:clamp(21px,3.6vw,26px);font-weight:600;letter-spacing:-.02em")}>{v.cName} vs PrivaTools</h1>
      <p style={css("margin-top:3px;font-size:12.5px;color:var(--ink3)")}>{v.cSub}</p>
      </div>

                  {Boolean(v.cIllustrative) && (<>
                    <span style={css("padding:4px 10px;border:1px solid var(--amber);border-radius:99px;background:var(--amberSoft);font-size:11px;color:var(--amber);white-space:nowrap")}>Illustrative — pending verification</span>
                  </>)}

                  {Boolean(v.cPending) && (<>
                    <span style={css("padding:4px 10px;border:1px solid var(--line2);border-radius:99px;background:var(--panel2);font-size:11px;color:var(--ink3);white-space:nowrap")}>Verified data pending</span>
                  </>)}
      </div>

                {Boolean(v.cPending) && (<>
            
      <section style={css("margin-top:16px;border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:26px 18px;text-align:center")}>
      <span className="ms" style={css("font-size:30px;color:var(--ink3)")}>{ICON.fact_check}</span>
      <h2 style={css("margin-top:9px;font-size:15px;font-weight:500")}>Verified comparison data pending</h2>
      <p style={css("margin-top:6px;font-size:12.5px;color:var(--ink2);line-height:1.6;max-width:60ch;margin-left:auto;margin-right:auto")}>{v.cPendingNote}</p>
      <div style={css("display:flex;flex-wrap:wrap;gap:9px;justify-content:center;margin-top:14px")}>
      <button onClick={v.goCompare} style={css("height:36px;padding:0 14px;border-radius:9px;background:var(--em);color:var(--em-ink,#04231A);font-size:12.5px;font-weight:500")}>Back to comparison hub</button>
      <button onClick={v.goSupport} style={css("height:36px;padding:0 13px;border:1px solid var(--line);border-radius:9px;font-size:12.5px;color:var(--ink2)")}>Submit evidence</button>
      </div>
      </section>

                </>)}

                {Boolean(v.cIllustrative) && (<>
            
      <section style={css("margin-top:16px;border:1px solid var(--line);border-radius:14px;background:var(--panel);overflow:hidden")}>
      <div style={css("overflow-x:auto")}>
      <table style={css("width:100%;min-width:560px;font-size:12.5px")}>
      <thead><tr style={css("text-align:left;background:var(--panel2);color:var(--ink3)")}>
      <th scope="col" style={css("padding:10px 14px;font-weight:400;font-size:11px;text-transform:uppercase;letter-spacing:.06em;width:34%")}>Feature / Priority</th>
      <th scope="col" style={css("padding:10px;font-weight:500;font-size:12.5px;color:var(--ink)")}>PrivaTools</th>
      <th scope="col" style={css("padding:10px;font-weight:500;font-size:12.5px;color:var(--ink)")}>{v.cName}</th>
      </tr></thead>
      <tbody>

                          {(v.cRows ?? []).map((r, rI) => (
                      
      <tr key={rI} style={css("border-top:1px solid var(--line)")}>
      <th scope="row" style={css("padding:10px 14px;text-align:left;font-weight:400;color:var(--ink2)")}>{r.feature}<span style={css("display:block;font-size:10.5px;color:var(--ink3)")}>{r.evidence}</span></th>
      <td style={css("padding:10px")}><span style={css("display:inline-flex;align-items:center;gap:7px")}><span className="ms" style={css(`font-size:16px;color:${r.mineColor}`)}>{ICON[r.mineIcon]}</span>{r.mine}</span></td>
      <td style={css("padding:10px")}><span style={css("display:inline-flex;align-items:center;gap:7px")}><span className="ms" style={css(`font-size:16px;color:${r.theirColor}`)}>{ICON[r.theirIcon]}</span>{r.theirs}</span></td>
      </tr>

                          ))}
      </tbody>
      </table>
      </div>
      <div style={css("display:flex;flex-wrap:wrap;gap:14px;padding:12px 14px;border-top:1px solid var(--line);font-size:11.5px;color:var(--ink3)")}>
      <span>{v.cChecked}</span><span>Currency: {v.cCurrency}</span><span>These rows are layout examples, not competitor facts.</span>
      </div>
      </section>
      <section style={css("margin-top:14px;border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:14px")}>
      <h2 style={css("font-size:14px;font-weight:500")}>Their honest advantage</h2>
      <p style={css("margin-top:5px;font-size:12.5px;color:var(--ink2);line-height:1.55;max-width:62ch")}>{v.cAdvantage}</p>
      <p style={css("margin-top:7px;font-size:11.5px;color:var(--ink3)")}>{v.cAdvantageNote} No universal winner—results change with your priorities.</p>
      </section>

                </>)}
      <section style={css("margin-top:14px;border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:14px")}>
      <h2 style={css("font-size:14px;font-weight:500")}>All 13 competitor slots</h2>
      <ul style={css("display:grid;grid-template-columns:repeat(auto-fit,minmax(200px,1fr));gap:8px;margin-top:11px")}>

                    {(v.cSlots ?? []).map((s, sI) => (
                
      <li key={sI}><button onClick={s.go} style={css(`width:100%;height:100%;display:flex;flex-direction:column;gap:4px;padding:10px;border:1px solid ${s.bd};border-radius:10px;background:${s.bg};text-align:left`)}>
      <span style={css("display:flex;align-items:center;gap:7px;width:100%")}><span style={css("flex:1;min-width:0;font-size:12.5px;font-weight:500;white-space:nowrap;overflow:hidden;text-overflow:ellipsis")}>{s.name}</span><span style={css(`font-size:10px;color:${s.badgeColor};white-space:nowrap`)}>{s.badge}</span></span>
      <span style={css("font-size:10.5px;color:var(--ink3);line-height:1.35")}>{s.sub}</span>
      </button></li>

                    ))}
      </ul>
      </section>
      </div>

            </>)}

            {Boolean(v.isBlog) && (<>
        
      <div style={css("max-width:900px;margin:0 auto;padding:20px 24px 0;animation:slidein 200ms ease both")}>
      <h1 style={css("font-size:clamp(21px,3.6vw,26px);font-weight:600;letter-spacing:-.02em")}>Blog</h1>
      <p style={css("margin-top:3px;font-size:12.5px;color:var(--ink3)")}>Notes on private file work, formats and how this product behaves.</p>
      <div style={css("display:flex;align-items:center;gap:8px;height:36px;max-width:320px;margin-top:14px;padding:0 10px;border:1px solid var(--line);border-radius:9px;background:var(--panel)")}>
      <span className="ms" style={css("font-size:18px;color:var(--ink3)")}>{ICON.search}</span>
      <input value={v.blogQ} onChange={v.onBlogQ} placeholder="Search articles" aria-label="Search articles" style={css("flex:1;min-width:0;border:0;background:none;outline:none;font-size:13px")} />
      </div>
      <div style={css("display:flex;flex-wrap:wrap;gap:7px;margin-top:12px")}>

                  {(v.blogCats ?? []).map((c, cI) => (
              
      <button key={cI} onClick={c.go} style={css(`height:28px;padding:0 11px;border:1px solid ${c.bd};border-radius:99px;background:${c.bg};color:${c.fg};font-size:12px;white-space:nowrap`)}>{c.label}</button>

                  ))}
      </div>

                {Boolean(v.blogEmpty) && (<>
            
      <div style={css("margin-top:16px;padding:34px 18px;text-align:center;border:1px solid var(--line);border-radius:14px;background:var(--panel)")}>
      <span className="ms" style={css("font-size:28px;color:var(--ink3)")}>{ICON.article}</span>
      <h2 style={css("margin-top:8px;font-size:14px;font-weight:500")}>Nothing matches this filter yet</h2>
      <p style={css("margin-top:4px;font-size:12.5px;color:var(--ink3)")}>Try another category, or clear the search.</p>
      <button onClick={v.clearBlog} style={css("margin-top:9px;font-size:12.5px;color:var(--em)")}>Show all articles</button>
      </div>

                </>)}
      <p style={css("margin-top:12px;padding:10px 12px;border:1px solid var(--line);border-radius:10px;background:var(--panel);font-size:11.5px;color:var(--ink3);line-height:1.5")}>{v.blogPending}</p>
      <ul style={css("margin-top:14px;display:flex;flex-direction:column;gap:10px")}>

                  {(v.posts ?? []).map((p, pI) => (
              
      <li key={pI}>
      <button onClick={p.go} style={css("width:100%;display:flex;gap:12px;padding:14px;border:1px solid var(--line);border-radius:12px;background:var(--panel);transition:border-color 160ms ease")} className="dc-i14">
      <span aria-hidden="true" style={css("width:64px;height:64px;flex:none;border-radius:9px;background:repeating-linear-gradient(135deg,var(--panel3) 0 6px,transparent 6px 12px);display:flex;align-items:center;justify-content:center")}><span className="ms" style={css("font-size:22px;color:var(--em)")}>{ICON.article}</span></span>
      <span style={css("display:block;flex:1;min-width:0;text-align:left")}>
      <span style={css("display:flex;flex-wrap:wrap;align-items:center;gap:8px;font-size:10.5px;color:var(--ink3)")}><span style={css("padding:2px 7px;border:1px solid var(--line);border-radius:99px")}>{p.cat}</span>{p.date}<span>·</span>{p.read}</span>
      <span style={css("display:block;margin-top:5px;font-size:14.5px;font-weight:500")}>{p.title}</span>
      <span style={css("display:block;margin-top:4px;font-size:12px;color:var(--ink2);line-height:1.5")}>{p.excerpt}</span>
      </span>
      </button>
      </li>

                  ))}
      </ul>
      </div>

            </>)}

            {Boolean(v.isPost) && (<>
        
      <div style={css("max-width:1000px;margin:0 auto;padding:20px 24px 0;animation:slidein 200ms ease both")}>

                {Boolean(v.postMissing) && (<>
            
      <div style={css("padding:34px 18px;text-align:center;border:1px solid var(--line);border-radius:14px;background:var(--panel)")}>
      <span className="ms" style={css("font-size:30px;color:var(--ink3)")}>{ICON.article_shortcut}</span>
      <h1 style={css("margin-top:9px;font-size:20px;font-weight:600")}>Article not found</h1>
      <p style={css("margin-top:5px;font-size:12.5px;color:var(--ink2)")}>No article is published at <span style={css("font-family:'Geist Mono',ui-monospace,monospace;color:var(--ink)")}>/blog/{v.postSlugRaw}</span>. Nine of the fifteen planned articles are still unpublished.</p>
      <div style={css("display:flex;flex-wrap:wrap;gap:9px;justify-content:center;margin-top:14px")}>
      <button onClick={v.goBlog} style={css("height:36px;padding:0 14px;border-radius:9px;background:var(--em);color:var(--em-ink,#04231A);font-size:12.5px;font-weight:500")}>All articles</button>
      <button onClick={v.openPalette} style={css("height:36px;padding:0 13px;border:1px solid var(--line);border-radius:9px;font-size:12.5px;color:var(--ink2)")}>Search</button>
      <button onClick={v.reportLink} style={css("height:36px;padding:0 13px;border:1px solid var(--line);border-radius:9px;font-size:12.5px;color:var(--ink2)")}>Report a broken link</button>
      </div>
      </div>

                </>)}

                {Boolean(v.postFound) && (<>
            
      <div style={css("display:flex;flex-wrap:wrap-reverse;gap:22px;align-items:flex-start")}>
      <article style={css("flex:2 1 460px;min-width:0")}>
      <div style={css("display:flex;flex-wrap:wrap;align-items:center;gap:8px;font-size:11px;color:var(--ink3)")}><span style={css("padding:2px 8px;border:1px solid var(--line);border-radius:99px")}>{v.postCat}</span>{v.postDate}<span>·</span>{v.postRead}</div>
      <h1 style={css("margin-top:9px;font-size:clamp(23px,4.2vw,30px);line-height:1.15;font-weight:600;letter-spacing:-.025em;text-wrap:pretty")}>{v.postTitle}</h1>
      <p style={css("margin-top:12px;font-size:15px;color:var(--ink2);line-height:1.6;max-width:62ch")}>{v.postLead}</p>

                      {Boolean(v.postSample) && (<>
                  
      <p style={css("display:flex;gap:10px;margin-top:14px;padding:11px 12px;border:1px solid var(--amber);border-radius:10px;background:var(--amberSoft);font-size:12px;color:var(--ink2);line-height:1.5;max-width:66ch")}><span className="ms" style={css("font-size:18px;color:var(--amber)")}>{ICON.draft_orders}</span>{v.postSampleNote}</p>

                      </>)}
      <h2 style={css("margin-top:26px;font-size:17px;font-weight:500")}>What local-first means</h2>
      <p style={css("margin-top:8px;font-size:13.5px;color:var(--ink2);line-height:1.65;max-width:66ch")}>A local-first tool does its work in the browser on your device. The file is read where it already sits, the result is written back to the same place, and no copy travels to a server. That is the default here, and every tool says which mode it uses before you start.</p>
      <div style={css("display:flex;gap:11px;margin-top:16px;padding:13px;border:1px solid var(--emLine);border-radius:11px;background:var(--emSoft)")}>
      <span className="ms" style={css("font-size:19px;color:var(--em)")}>{ICON.lightbulb}</span>
      <p style={css("font-size:12.5px;color:var(--ink2);line-height:1.55")}>If a tool cannot finish on your device, we say so and name the location: Mumbai, India. The file is processed in an isolated request and deleted immediately after the response.</p>
      </div>
      <h2 style={css("margin-top:26px;font-size:17px;font-weight:500")}>Limits and file sizes</h2>
      <div style={css("margin-top:10px;overflow-x:auto")}>
      <table style={css("width:100%;min-width:420px;font-size:12.5px;border:1px solid var(--line);border-radius:10px")}>
      <thead><tr style={css("text-align:left;color:var(--ink3);background:var(--panel2)")}><th scope="col" style={css("padding:9px 11px;font-weight:400;font-size:11px;text-transform:uppercase;letter-spacing:.06em")}>Mode</th><th scope="col" style={css("padding:9px 11px;font-weight:400;font-size:11px;text-transform:uppercase;letter-spacing:.06em")}>Where it runs</th><th scope="col" style={css("padding:9px 11px;font-weight:400;font-size:11px;text-transform:uppercase;letter-spacing:.06em")}>Limit</th></tr></thead>
      <tbody>
      <tr style={css("border-top:1px solid var(--line)")}><td style={css("padding:9px 11px")}>Local</td><td style={css("padding:9px 11px;color:var(--ink3)")}>Your device</td><td style={css("padding:9px 11px")}>500 MB per file</td></tr>
      <tr style={css("border-top:1px solid var(--line)")}><td style={css("padding:9px 11px")}>Local + fallback</td><td style={css("padding:9px 11px;color:var(--ink3)")}>Device, then Mumbai if needed</td><td style={css("padding:9px 11px")}>500 MB per file</td></tr>
      <tr style={css("border-top:1px solid var(--line)")}><td style={css("padding:9px 11px")}>Server required</td><td style={css("padding:9px 11px;color:var(--ink3)")}>Mumbai, India</td><td style={css("padding:9px 11px")}>500 MB per file</td></tr>
      </tbody>
      </table>
      </div>
      <h2 style={css("margin-top:26px;font-size:17px;font-weight:500")}>What we keep, and for how long</h2>
      <p style={css("margin-top:8px;font-size:13.5px;color:var(--ink2);line-height:1.65;max-width:66ch")}>Security logs are content-free. They never contain filenames or file contents.</p>
      <pre style={css("margin-top:12px;padding:13px;border:1px solid var(--line);border-radius:10px;background:var(--panel2);overflow-x:auto")}><code style={css("font-family:'Geist Mono',ui-monospace,monospace;font-size:11.5px;color:var(--ink2);line-height:1.7")}>routine      24 hours
      suspicious    7 days
      confirmed incident   30 days
      document retention   none</code></pre>
      </article>
      <aside style={css("flex:1 1 220px;min-width:0;position:sticky;top:72px;display:flex;flex-direction:column;gap:14px")}>
      <div style={css("border:1px solid var(--line);border-radius:12px;background:var(--panel);padding:13px")}>
      <h2 style={css("font-size:11px;letter-spacing:.08em;text-transform:uppercase;color:var(--ink3)")}>Contents</h2>
      <ul style={css("margin-top:9px;display:flex;flex-direction:column;gap:6px")}>

                          {(v.postToc ?? []).map((t, tI) => (
                      
      <li key={tI} style={css("font-size:12.5px;color:var(--ink2)")}>{t.label}</li>

                          ))}
      </ul>
      </div>
      <div style={css("border:1px solid var(--line);border-radius:12px;background:var(--panel);padding:13px")}>
      <h2 style={css("font-size:11px;letter-spacing:.08em;text-transform:uppercase;color:var(--ink3)")}>Related tools</h2>
      <ul style={css("margin-top:9px;display:flex;flex-direction:column;gap:5px")}>

                          {(v.relatedTools ?? []).map((r, rI) => (
                      
      <li key={rI}><button onClick={r.go} style={css("display:flex;align-items:center;gap:8px;width:100%;font-size:12.5px")}><span className="ms" style={css(`font-size:16px;color:${r.color}`)}>{ICON[r.icon]}</span><span style={css("flex:1;min-width:0;white-space:nowrap;overflow:hidden;text-overflow:ellipsis")}>{r.name}</span></button></li>

                          ))}
      </ul>
      </div>
      <div style={css("border:1px solid var(--line);border-radius:12px;background:var(--panel);padding:13px")}>
      <h2 style={css("font-size:11px;letter-spacing:.08em;text-transform:uppercase;color:var(--ink3)")}>More articles</h2>
      <ul style={css("margin-top:9px;display:flex;flex-direction:column;gap:7px")}>

                          {(v.relatedPosts ?? []).map((p, pI) => (
                      
      <li key={pI}><button onClick={p.go} style={css("width:100%;text-align:left;font-size:12.5px;color:var(--ink2)")}>{p.title}<span style={css("display:block;font-size:10.5px;color:var(--ink3)")}>{p.date}</span></button></li>

                          ))}
      </ul>
      </div>
      </aside>
      </div>

                </>)}
      </div>

            </>)}

            {Boolean(v.isAbout) && (<>
        
      <div style={css("max-width:900px;margin:0 auto;padding:20px 24px 0;animation:slidein 200ms ease both")}>
      <h1 style={css("font-size:clamp(21px,3.6vw,26px);font-weight:600;letter-spacing:-.02em")}>About PrivaTools</h1>
      <p style={css("margin-top:8px;font-size:14.5px;color:var(--ink2);line-height:1.6;max-width:64ch")}>PrivaTools is built for privacy-first file work. Local by default, with transparent server fallback when needed. No accounts. No tracking. No retention.</p>
      <ul style={css("display:grid;grid-template-columns:repeat(auto-fit,minmax(200px,1fr));gap:9px;margin-top:16px")}>
      <li style={css("display:flex;align-items:center;gap:9px;padding:12px;border:1px solid var(--line);border-radius:11px;background:var(--panel)")}><span className="ms" style={css("font-size:19px;color:var(--em)")}>{ICON.person}</span><span><span style={css("display:block;font-size:12.5px;font-weight:500")}>Built by an individual</span><span style={css("display:block;font-size:11px;color:var(--ink3)")}>Operated by one person, not a company</span></span></li>
      <li style={css("display:flex;align-items:center;gap:9px;padding:12px;border:1px solid var(--line);border-radius:11px;background:var(--panel)")}><span className="ms" style={css("font-size:19px;color:var(--em)")}>{ICON.savings}</span><span><span style={css("display:block;font-size:12.5px;font-weight:500")}>Owner-funded</span><span style={css("display:block;font-size:11px;color:var(--ink3)")}>Free forever. No paid tier, ads or analytics</span></span></li>
      <li style={css("display:flex;align-items:center;gap:9px;padding:12px;border:1px solid var(--line);border-radius:11px;background:var(--panel)")}><span className="ms" style={css("font-size:19px;color:var(--em)")}>{ICON.public}</span><span><span style={css("display:block;font-size:12.5px;font-weight:500")}>India, English-first</span><span style={css("display:block;font-size:11px;color:var(--ink3)")}>Global launch, operated from India</span></span></li>
      </ul>
      <section style={css("margin-top:18px;border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:16px")}>
      <h2 style={css("font-size:15px;font-weight:500")}>What happens to a file</h2>
      <p style={css("margin-top:3px;font-size:12px;color:var(--ink3)")}>The truthful journey, in order.</p>
      <ol style={css("margin-top:14px;display:flex;flex-direction:column;gap:2px")}>

                    {(v.journey ?? []).map((j, jI) => (
                
      <li key={jI} style={css("display:flex;gap:12px;padding:10px 0")}>
      <span style={css("display:flex;align-items:center;justify-content:center;width:30px;height:30px;flex:none;border-radius:8px;background:var(--emSoft)")}><span className="ms" style={css("font-size:17px;color:var(--em)")}>{ICON[j.icon]}</span></span>
      <span style={css("display:block;min-width:0")}><span style={css("display:block;font-size:13px;font-weight:500")}>{j.title}</span><span style={css("display:block;margin-top:2px;font-size:12px;color:var(--ink2)")}>{j.text}</span></span>
      </li>

                    ))}
      </ol>
      </section>
      </div>

            </>)}

            {Boolean(v.isPolicy) && (<>
        
      <div style={css("max-width:1000px;margin:0 auto;padding:20px 24px 0;animation:slidein 200ms ease both")}>
      <h1 style={css("font-size:clamp(21px,3.6vw,26px);font-weight:600;letter-spacing:-.02em")}>{v.pTitle}</h1>
      <p style={css("margin-top:3px;font-size:12.5px;color:var(--ink3)")}>{v.pSub} · {v.pUpdated}</p>
      <div style={css("display:flex;flex-wrap:wrap;gap:7px;margin-top:14px")}>

                  {(v.pTabs ?? []).map((t, tI) => (
              
      <button key={tI} onClick={t.go} style={css(`height:30px;padding:0 12px;border:1px solid ${t.bd};border-radius:99px;background:${t.bg};color:${t.fg};font-size:12px;white-space:nowrap`)}>{t.label}</button>

                  ))}
      </div>
      <div style={css("display:flex;flex-wrap:wrap-reverse;gap:22px;margin-top:18px;align-items:flex-start")}>
      <article style={css("flex:2 1 440px;min-width:0;display:flex;flex-direction:column;gap:22px")}>

                    {(v.pSecs ?? []).map((s, sI) => (
                
      <section key={sI} id={s.anchor}>
      <div style={css("display:flex;align-items:center;gap:9px")}>
      <h2 style={css("font-size:17px;font-weight:500")}>{s.h}</h2>
      <button onClick={s.copy} aria-label={s.copyLabel} style={css("display:flex;width:26px;height:26px;align-items:center;justify-content:center;border-radius:6px")}><span className="ms" style={css("font-size:15px;color:var(--ink3)")}>{ICON.link}</span></button>
      </div>

                        {(s.ps ?? []).map((p, pI) => (
                    
      <p key={pI} style={css("margin-top:8px;font-size:13.5px;color:var(--ink2);line-height:1.65;max-width:68ch;text-wrap:pretty")}>{p.t}</p>

                        ))}
      </section>

                    ))}
      </article>
      <aside style={css("flex:1 1 220px;min-width:0;position:sticky;top:72px;border:1px solid var(--line);border-radius:12px;background:var(--panel);padding:13px")}>
      <h2 style={css("font-size:11px;letter-spacing:.08em;text-transform:uppercase;color:var(--ink3)")}>Contents</h2>
      <ul style={css("margin-top:9px;display:flex;flex-direction:column;gap:6px")}>

                      {(v.pToc ?? []).map((t, tI) => (
                  
      <li key={tI}><button onClick={t.go} style={css("width:100%;text-align:left;font-size:12.5px;color:var(--ink2)")}>{t.label}</button></li>

                      ))}
      </ul>
      </aside>
      </div>
      </div>

            </>)}

            {Boolean(v.isSupport) && (<>
        
      <div style={css("max-width:1000px;margin:0 auto;padding:20px 24px 0;animation:slidein 200ms ease both")}>
      <h1 style={css("font-size:clamp(21px,3.6vw,26px);font-weight:600;letter-spacing:-.02em")}>Support</h1>
      <p style={css("margin-top:3px;font-size:12.5px;color:var(--ink3)")}>We are here to help. One person reads everything that arrives.</p>
      <div style={css("display:flex;flex-wrap:wrap;gap:14px;margin-top:16px;align-items:flex-start")}>
      <section style={css("flex:2 1 400px;min-width:0;border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:16px")}>
      <h2 style={css("font-size:15px;font-weight:500")}>Report an issue</h2>

                    {Boolean(v.formSent) && (<>
                
      <div role="status" style={css("display:flex;gap:11px;margin-top:12px;padding:13px;border:1px solid var(--emLine);border-radius:11px;background:var(--emSoft)")}>
      <span className="ms" style={css("font-size:20px;color:var(--em)")}>{ICON.check_circle}</span>
      <span style={css("flex:1")}><span style={css("display:block;font-size:13px;font-weight:500")}>Sent</span><span style={css("display:block;margin-top:2px;font-size:12px;color:var(--ink2)")}>Thank you. Nothing about your files was included — only what you typed.</span></span>
      <button onClick={v.resetForm} style={css("height:30px;padding:0 11px;border:1px solid var(--line2);border-radius:8px;font-size:12px")}>New report</button>
      </div>

                    </>)}

                    {Boolean(v.formError) && (<>
                
      <div role="alert" style={css("display:flex;gap:11px;margin-top:12px;padding:13px;border:1px solid var(--coral);border-radius:11px;background:var(--coralSoft)")}>
      <span className="ms" style={css("font-size:20px;color:var(--coral)")}>{ICON.error}</span>
      <span style={css("flex:1;font-size:12px;color:var(--ink2)")}>The report could not be sent. Nothing was lost — try again, or email support@priva.tools directly.</span>
      </div>

                    </>)}

                    {Boolean(v.formOffline) && (<>
                
      <div role="alert" style={css("display:flex;gap:11px;margin-top:12px;padding:13px;border:1px solid var(--amber);border-radius:11px;background:var(--amberSoft)")}>
      <span className="ms" style={css("font-size:20px;color:var(--amber)")}>{ICON.wifi_off}</span>
      <span style={css("flex:1;font-size:12px;color:var(--ink2)")}>You are offline. The form is kept on this device and can be sent when you reconnect.</span>
      </div>

                    </>)}
      <div style={css("display:flex;flex-direction:column;gap:13px;margin-top:14px")}>
      <label style={css("display:block")}>
      <span style={css("display:block;font-size:11.5px;color:var(--ink3);margin-bottom:5px")}>Subject</span>
      <input value={v.formSubject} onChange={v.onSubject} placeholder="Short summary" aria-label="Subject" style={css("width:100%;height:38px;padding:0 11px;border:1px solid var(--line);border-radius:9px;background:var(--panel2);font-size:13px")} />

                        {Boolean(v.errSubject) && (<>
                          <span role="alert" style={css("display:block;margin-top:5px;font-size:11.5px;color:var(--coral)")}>A subject is required.</span>
                        </>)}
      </label>
      <label style={css("display:block")}>
      <span style={css("display:block;font-size:11.5px;color:var(--ink3);margin-bottom:5px")}>Category</span>
      <select value={v.formCat} onChange={v.onCat} aria-label="Category" style={css("width:100%;max-width:280px;height:38px;padding:0 10px;border:1px solid var(--line);border-radius:9px;background:var(--panel2);font-size:13px")}>

                          {(v.formCats ?? []).map((c, cI) => (
                            <option key={cI} value={c.v}>{c.label}</option>
                          ))}
      </select>
      </label>
      <label style={css("display:block")}>
      <span style={css("display:block;font-size:11.5px;color:var(--ink3);margin-bottom:5px")}>What happened</span>
      <textarea value={v.formDesc} onChange={v.onDesc} rows="4" placeholder="Steps, the tool you used, and what you expected" aria-label="Description" style={css("width:100%;padding:10px 11px;border:1px solid var(--line);border-radius:9px;background:var(--panel2);font-size:13px;line-height:1.55;resize:vertical")}></textarea>

                        {Boolean(v.errDesc) && (<>
                          <span role="alert" style={css("display:block;margin-top:5px;font-size:11.5px;color:var(--coral)")}>Please add at least a sentence so the problem can be reproduced.</span>
                        </>)}
      </label>
      <div>
      <span style={css("display:block;font-size:11.5px;color:var(--ink3);margin-bottom:5px")}>Attachment (optional)</span>

                        {Boolean(v.formFile) && (<>
                    
      <span style={css("display:inline-flex;align-items:center;gap:8px;padding:7px 10px;border:1px solid var(--line);border-radius:9px;background:var(--panel2);font-size:12.5px")}>
      <span className="ms" style={css("font-size:16px;color:var(--em)")}>{ICON.attachment}</span>{v.formFile}
                          <button onClick={v.detach} aria-label="Remove attachment" style={css("display:flex")}><span className="ms" style={css("font-size:15px;color:var(--ink3)")}>{ICON.close}</span></button>
      </span>

                        </>)}
      <button onClick={v.attach} style={css("display:flex;align-items:center;gap:8px;width:100%;max-width:320px;padding:12px;border:1.5px dashed var(--line2);border-radius:10px;background:var(--panel2);font-size:12.5px;color:var(--ink2)")}>
      <span className="ms" style={css("font-size:18px;color:var(--em)")}>{ICON.upload_file}</span>Add a screenshot — never a document
                      </button>
      </div>
      <div style={css("display:flex;flex-wrap:wrap;gap:9px;padding-top:13px;border-top:1px solid var(--line)")}>
      <button onClick={v.submit} style={css("height:38px;padding:0 16px;border-radius:9px;background:var(--em);color:var(--em-ink,#04231A);font-size:13px;font-weight:500")}>Send report</button>
      <button onClick={v.simulateError} style={css("height:38px;padding:0 13px;border:1px solid var(--line);border-radius:9px;font-size:12.5px;color:var(--ink2)")}>Preview error state</button>
      <button onClick={v.simulateOffline} style={css("height:38px;padding:0 13px;border:1px solid var(--line);border-radius:9px;font-size:12.5px;color:var(--ink2)")}>Preview offline state</button>
      </div>
      </div>
      </section>
      <aside style={css("flex:1 1 260px;min-width:0;display:flex;flex-direction:column;gap:14px")}>
      <div style={css("border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:14px")}>
      <h2 style={css("font-size:14px;font-weight:500")}>Support channels</h2>
      <ul style={css("margin-top:11px;display:flex;flex-direction:column;gap:6px")}>

                        {(v.channels ?? []).map((c, cI) => (
                    
      <li key={cI}><button onClick={c.go} style={css("display:flex;align-items:center;gap:9px;width:100%;min-height:44px;padding:8px 9px;border-radius:9px;transition:background 140ms ease")} className="dc-i15">
      <span className="ms" style={css("font-size:18px;color:var(--em)")}>{ICON[c.icon]}</span>
      <span style={css("display:block;flex:1;min-width:0;text-align:left")}><span style={css("display:block;font-size:12.5px")}>{c.name}</span><span style={css("display:block;font-size:10.5px;color:var(--ink3);white-space:nowrap;overflow:hidden;text-overflow:ellipsis")}>{c.sub}</span></span>
      <span style={css("font-size:11px;color:var(--ink3)")}>{c.action}</span>
      </button></li>

                        ))}
      </ul>
      </div>
      <div style={css("border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:14px;font-size:11.5px;color:var(--ink3);line-height:1.55")}>
                    Never send us a document. Support requests carry only what you type and any screenshot you attach.
                  </div>
      </aside>
      </div>
      </div>

            </>)}

            {Boolean(v.isStatus) && (<>
        
      <div style={css("max-width:1000px;margin:0 auto;padding:20px 24px 0;animation:slidein 200ms ease both")}>
      <div style={css("display:flex;flex-wrap:wrap;align-items:flex-end;gap:12px")}>
      <div style={css("flex:1;min-width:200px")}>
      <h1 style={css("font-size:clamp(21px,3.6vw,26px);font-weight:600;letter-spacing:-.02em")}>Status</h1>
      <p style={css("margin-top:3px;font-size:12.5px;color:var(--ink3)")}>{v.statusUpdated}</p>
      </div>
      <button onClick={v.refresh} style={css("display:flex;align-items:center;gap:7px;height:32px;padding:0 12px;border:1px solid var(--line);border-radius:8px;background:var(--panel);font-size:12.5px")}><span className="ms" style={css("font-size:17px;color:var(--em)")}>{ICON.refresh}</span>Refresh</button>
      </div>
      <section style={css("margin-top:16px;border:1px solid var(--line);border-radius:14px;background:var(--panel);overflow:hidden")}>
      <ul>

                    {(v.components ?? []).map((c, cI) => (
                
      <li key={cI} style={css("display:flex;flex-wrap:wrap;align-items:center;gap:11px;padding:13px 14px;border-top:1px solid var(--line)")}>
      <span className="ms" style={css(`font-size:19px;color:${c.color};animation:onepulse 2.4s ease-out 1`)}>{ICON[c.icon]}</span>
      <span style={css("display:block;flex:1;min-width:160px")}><span style={css("display:block;font-size:13px;font-weight:500")}>{c.name}</span><span style={css("display:block;font-size:11px;color:var(--ink3)")}>{c.note}</span></span>
      <span style={css(`font-size:12px;color:${c.color}`)}>{c.label}</span>
      </li>

                    ))}
      </ul>
      <div style={css("display:flex;gap:9px;padding:13px 14px;border-top:1px solid var(--line);font-size:11.5px;color:var(--ink2)")}><span className="ms" style={css("font-size:17px;color:var(--em)")}>{ICON.info}</span>{v.serverOutageNote}</div>
      </section>
      <section style={css("margin-top:14px;border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:14px")}>
      <div style={css("display:flex;flex-wrap:wrap;align-items:center;gap:10px")}>
      <h2 style={css("flex:1;min-width:140px;font-size:14px;font-weight:500")}>Security logs</h2>
      <div role="group" aria-label="Log window" style={css("display:flex;gap:2px;padding:2px;border:1px solid var(--line);border-radius:9px;background:var(--panel2)")}>

                      {(v.logTabs ?? []).map((t, tI) => (
                  
      <button key={tI} onClick={t.go} style={css(`height:26px;padding:0 10px;border-radius:7px;background:${t.bg};color:${t.fg};font-size:11.5px`)}>{t.label}</button>

                      ))}
      </div>
      </div>
      <ul style={css("margin-top:12px")}>

                    {(v.logRows ?? []).map((r, rI) => (
                
      <li key={rI} style={css("display:flex;align-items:center;gap:10px;padding:9px 0;border-top:1px solid var(--line);font-size:12.5px")}>
      <span style={css("flex:1;color:var(--ink2)")}>{r.k}</span>
      <span style={css("font-family:'Geist Mono',ui-monospace,monospace")}>{r.v}</span>
      </li>

                    ))}
      </ul>
      <p style={css("margin-top:11px;font-size:11.5px;color:var(--ink3)")}>Counts are content-free: no filenames, no file contents, no analytics. Routine 24 hours · suspicious 7 days · confirmed incident 30 days.</p>
      </section>
      <section style={css("margin-top:14px;border:1px solid var(--line);border-radius:14px;background:var(--panel);overflow:hidden")}>
      <div style={css("display:flex;flex-wrap:wrap;align-items:center;gap:10px;padding:14px")}>
      <h2 style={css("flex:1;min-width:140px;font-size:14px;font-weight:500")}>Incident history</h2>
      <button onClick={v.toggleIncidents} style={css("height:30px;padding:0 11px;border:1px solid var(--line);border-radius:8px;background:var(--panel2);font-size:11.5px;color:var(--ink2)")}>{v.incidentsLabel}</button>
      </div>

                  {Boolean(v.noIncidents) && (<>
              
      <div style={css("padding:30px 18px;text-align:center;border-top:1px solid var(--line)")}>
      <span className="ms" style={css("font-size:28px;color:var(--em)")}>{ICON.check_circle}</span>
      <p style={css("margin-top:8px;font-size:13px;font-weight:500")}>No recent incidents</p>
      <p style={css("margin-top:3px;font-size:12px;color:var(--ink3)")}>Nothing has been reported in the last 90 days.</p>
      </div>

                  </>)}
      <ul>

                    {(v.incidents ?? []).map((i, iI) => (
                
      <li key={iI} style={css("padding:13px 14px;border-top:1px solid var(--line)")}>
      <div style={css("display:flex;flex-wrap:wrap;align-items:center;gap:9px")}>
      <span style={css("font-size:12.5px;font-weight:500")}>{i.service}</span>
      <span style={css(`padding:2px 8px;border:1px solid ${i.sevColor};border-radius:99px;font-size:10.5px;color:${i.sevColor}`)}>{i.severity}</span>
      <span style={css(`padding:2px 8px;border:1px solid var(--line);border-radius:99px;font-size:10.5px;color:${i.stateColor}`)}>{i.state}</span>
      <span style={css("margin-left:auto;font-size:11px;color:var(--ink3);font-family:'Geist Mono',ui-monospace,monospace")}>{i.when}</span>
      </div>
      <ul style={css("margin-top:9px;display:flex;flex-direction:column;gap:5px;padding-left:2px")}>

                          {(i.updates ?? []).map((u, uI) => (
                      
      <li key={uI} style={css("display:flex;gap:10px;font-size:12px;color:var(--ink2);line-height:1.5")}><span style={css("flex:0 0 44px;font-family:'Geist Mono',ui-monospace,monospace;font-size:11px;color:var(--ink3)")}>{u.t}</span>{u.text}</li>

                          ))}
      </ul>
      <p style={css("margin-top:7px;font-size:11px;color:var(--ink3)")}>Resolved {i.resolved}</p>
      </li>

                    ))}
      </ul>
      </section>
      </div>

            </>)}

            {Boolean(v.isAtlas) && (<>
        
      <div style={css("max-width:1180px;margin:0 auto;padding:20px 24px 0;animation:slidein 200ms ease both")}>
      <h1 style={css("font-size:clamp(21px,3.6vw,26px);font-weight:600;letter-spacing:-.02em")}>PWA, offline and error states</h1>
      <p style={css("margin-top:3px;font-size:12.5px;color:var(--ink3)")}>Every interruption has a recovery action. Nothing dead-ends.</p>
      <ul style={css("display:grid;grid-template-columns:repeat(auto-fit,minmax(268px,1fr));gap:10px;margin-top:16px")}>

                  {(v.atlas ?? []).map((a, aI) => (
              
      <li key={aI} style={css("display:flex;flex-direction:column;gap:8px;padding:14px;border:1px solid var(--line);border-radius:12px;background:var(--panel)")}>
      <span className="ms" style={css(`font-size:22px;color:${a.color}`)}>{ICON[a.icon]}</span>
      <span style={css("font-size:13.5px;font-weight:500")}>{a.title}</span>
      <span style={css("font-size:12px;color:var(--ink2);line-height:1.5;flex:1")}>{a.text}</span>
      <button onClick={a.go} style={css("align-self:flex-start;height:32px;padding:0 13px;border:1px solid var(--emLine);border-radius:8px;font-size:12.5px;color:var(--em)")}>{a.action}</button>
      </li>

                  ))}
      </ul>
      </div>

            </>)}

            {Boolean(v.is404) && (<>
        
      <div style={css("max-width:760px;margin:0 auto;padding:40px 24px 0;animation:slidein 200ms ease both;text-align:center")}>
      <span className="ms" style={css("font-size:40px;color:var(--ink3)")}>{ICON.explore_off}</span>
      <h1 style={css("margin-top:10px;font-size:clamp(21px,3.6vw,26px);font-weight:600;letter-spacing:-.02em")}>{v.notFoundTitle}</h1>

                {Boolean(v.is404Tool) && (<>
                  <p style={css("margin-top:6px;font-size:12.5px;color:var(--ink3);max-width:52ch;margin-left:auto;margin-right:auto")}>{v.notFoundLead}</p>
                </>)}
      <p style={css("margin-top:6px;font-size:13px;color:var(--ink2)")}>We could not find <span style={css("font-family:'Geist Mono',ui-monospace,monospace;color:var(--ink)")}>{v.failedUrlPath}</span></p>
      <button onClick={v.openPalette} style={css("display:flex;align-items:center;gap:8px;margin:16px auto 0;height:38px;padding:0 14px;border:1px solid var(--line);border-radius:9px;background:var(--panel);font-size:13px;color:var(--ink3);white-space:nowrap")}><span className="ms" style={css("font-size:18px")}>{ICON.search}</span>Search 200+ tools<kbd style={css("font-family:'Geist Mono',ui-monospace,monospace;font-size:10.5px;padding:2px 5px;border:1px solid var(--line);border-radius:5px")}>⌘K</kbd></button>
      <div style={css("margin-top:20px;border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:14px;text-align:left")}>
      <h2 style={css("font-size:13px;font-weight:500")}>Did you mean</h2>
      <ul style={css("margin-top:10px;display:flex;flex-direction:column;gap:5px")}>

                    {(v.fuzzy ?? []).map((f, fI) => (
                
      <li key={fI}><button onClick={f.go} style={css("display:flex;align-items:center;gap:9px;width:100%;min-height:40px;padding:6px 8px;border-radius:9px;transition:background 140ms ease")} className="dc-i16">
      <span className="ms" style={css(`font-size:17px;color:${f.color}`)}>{ICON[f.icon]}</span>
      <span style={css("display:block;flex:1;min-width:0;text-align:left")}><span style={css("display:block;font-size:12.5px")}>{f.name}</span><span style={css("display:block;font-size:10.5px;color:var(--ink3)")}>{f.purpose}</span></span>
      <span className="ms" style={css("font-size:16px;color:var(--ink3)")}>{ICON.chevron_right}</span>
      </button></li>

                    ))}
      </ul>
      </div>
      <div style={css("display:flex;flex-wrap:wrap;gap:9px;justify-content:center;margin-top:16px")}>
      <button onClick={v.goHome} style={css("height:36px;padding:0 14px;border-radius:9px;background:var(--em);color:var(--em-ink,#04231A);font-size:12.5px;font-weight:500;white-space:nowrap")}>Home</button>
      <button onClick={v.goTools} style={css("height:36px;padding:0 13px;border:1px solid var(--line);border-radius:9px;font-size:12.5px;color:var(--ink2);white-space:nowrap")}>All Tools</button>
      <button onClick={v.reportLink} style={css("height:36px;padding:0 13px;border:1px solid var(--line);border-radius:9px;font-size:12.5px;color:var(--ink2);white-space:nowrap")}>Report a broken link</button>
      </div>
      </div>

            </>)}

            {Boolean(v.isShowcase) && (<>
        
      <div style={css("max-width:1180px;margin:0 auto;padding:20px 24px 0;animation:slidein 200ms ease both")}>
      <div style={css("display:flex;flex-wrap:wrap;align-items:center;gap:10px")}>
      <div style={css("flex:1;min-width:220px")}>
      <h1 style={css("font-size:clamp(21px,3.6vw,26px);font-weight:600;letter-spacing:-.02em")}>Component & coverage showcase</h1>
      <p style={css("margin-top:3px;font-size:12.5px;color:var(--ink3)")}>Unlisted development route. Switch theme in the header to check both palettes.</p>
      </div>
      <span style={css("padding:4px 10px;border:1px solid var(--amber);border-radius:99px;background:var(--amberSoft);font-size:11px;color:var(--amber);white-space:nowrap")}>Not linked in navigation</span>
      </div>
      <div style={css("display:flex;flex-wrap:wrap;align-items:center;gap:8px;margin-top:14px")}>
      <span style={css("font-size:11.5px;color:var(--ink3)")}>Viewport</span>

                  {(v.viewports ?? []).map((v, vI) => (
              
      <button key={vI} onClick={v.go} style={css(`height:30px;padding:0 12px;border:1px solid ${v.bd};border-radius:99px;background:${v.bg};color:${v.fg};font-size:12px;white-space:nowrap`)}>{v.label}</button>

                  ))}
      <button onClick={v.probeBadTool} style={css("height:30px;padding:0 12px;border:1px solid var(--line);border-radius:99px;background:var(--panel2);color:var(--ink2);font-size:12px;white-space:nowrap")}>Probe unknown tool URL</button>
      </div>
      <section style={css("margin-top:16px;border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:16px")}>
      <h2 style={css("font-size:15px;font-weight:500")}>Controls and states</h2>
      <div style={css("display:grid;grid-template-columns:repeat(auto-fit,minmax(240px,1fr));gap:18px;margin-top:14px")}>
      <div>
      <h3 style={css("font-size:11px;letter-spacing:.08em;text-transform:uppercase;color:var(--ink3)")}>Buttons</h3>
      <div style={css("display:flex;flex-wrap:wrap;gap:7px;margin-top:9px")}>

                        {(v.btnStates ?? []).map((b, bI) => (
                    
      <button key={bI} style={css(`height:34px;padding:0 13px;border-radius:8px;background:${b.bg};color:${b.fg};opacity:${b.op};font-size:12.5px;font-weight:500`)}>{b.label}</button>

                        ))}
      <button style={css("display:flex;align-items:center;gap:7px;height:34px;padding:0 13px;border:1px solid var(--line2);border-radius:8px;font-size:12.5px")}><span className="ms" style={css("font-size:16px;color:var(--em);animation:shimmer 1.2s ease-in-out infinite")}>{ICON.progress_activity}</span>Loading</button>
      </div>
      </div>
      <div>
      <h3 style={css("font-size:11px;letter-spacing:.08em;text-transform:uppercase;color:var(--ink3)")}>Status badges</h3>
      <div style={css("display:flex;flex-wrap:wrap;gap:6px;margin-top:9px")}>

                        {(v.badges ?? []).map((b, bI) => (
                    
      <span key={bI} style={css(`display:inline-flex;align-items:center;gap:6px;padding:4px 10px;border:1px solid var(--line);border-radius:99px;background:var(--panel2);font-size:11.5px;color:${b.color};white-space:nowrap`)}><span style={css(`width:6px;height:6px;border-radius:50%;background:${b.color}`)}></span>{b.label}</span>

                        ))}
      </div>
      </div>
      <div>
      <h3 style={css("font-size:11px;letter-spacing:.08em;text-transform:uppercase;color:var(--ink3)")}>Text input</h3>
      <div style={css("display:flex;flex-direction:column;gap:7px;margin-top:9px")}>
      <input placeholder="Empty" aria-label="Empty input" style={css("height:36px;padding:0 10px;border:1px solid var(--line);border-radius:9px;background:var(--panel2);font-size:12.5px")} />
      <input defaultValue="Filled value" aria-label="Filled input" readOnly="readonly" style={css("height:36px;padding:0 10px;border:1px solid var(--line2);border-radius:9px;background:var(--panel2);font-size:12.5px")} />
      <input defaultValue="Invalid value" aria-label="Invalid input" aria-invalid="true" readOnly="readonly" style={css("height:36px;padding:0 10px;border:1px solid var(--coral);border-radius:9px;background:var(--coralSoft);font-size:12.5px")} />
      <input defaultValue="Disabled" aria-label="Disabled input" disabled="disabled" style={css("height:36px;padding:0 10px;border:1px solid var(--line);border-radius:9px;background:var(--panel3);font-size:12.5px;opacity:.6")} />
      </div>
      </div>
      <div>
      <h3 style={css("font-size:11px;letter-spacing:.08em;text-transform:uppercase;color:var(--ink3)")}>Alerts</h3>
      <div style={css("display:flex;flex-direction:column;gap:7px;margin-top:9px")}>

                        {(v.alerts ?? []).map((a, aI) => (
                    
      <span key={aI} style={css(`display:flex;align-items:center;gap:9px;padding:9px 11px;border:1px solid ${a.color};border-radius:9px;background:${a.bg};font-size:12px`)}><span className="ms" style={css(`font-size:17px;color:${a.color}`)}>{ICON[a.icon]}</span>{a.label}</span>

                        ))}
      </div>
      </div>
      <div>
      <h3 style={css("font-size:11px;letter-spacing:.08em;text-transform:uppercase;color:var(--ink3)")}>Progress</h3>
      <div style={css("display:flex;flex-direction:column;gap:11px;margin-top:12px")}>
      <span style={css("display:block;height:7px;border-radius:99px;background:var(--panel3);overflow:hidden")}><span style={css("display:block;height:100%;width:38%;background:linear-gradient(90deg,var(--teal),var(--em))")}></span></span>
      <span style={css("display:block;height:7px;border-radius:99px;background:var(--panel3);overflow:hidden")}><span style={css("display:block;height:100%;width:100%;background:var(--em)")}></span></span>
      <span style={css("display:block;height:7px;border-radius:99px;background:var(--panel3);overflow:hidden")}><span style={css("display:block;height:100%;width:62%;background:var(--coral)")}></span></span>
      </div>
      </div>
      <div>
      <h3 style={css("font-size:11px;letter-spacing:.08em;text-transform:uppercase;color:var(--ink3)")}>File row</h3>
      <div style={css("display:flex;align-items:center;gap:9px;margin-top:9px;padding:10px;border:1px solid var(--line);border-radius:10px;background:var(--panel2)")}>
      <span className="ms" style={css("font-size:18px;color:var(--coral)")}>{ICON.picture_as_pdf}</span>
      <span style={css("display:block;flex:1;min-width:0")}><span style={css("display:block;font-size:12.5px")}>report-q1.pdf</span><span style={css("display:block;font-size:10.5px;color:var(--ink3)")}>4.2 MB · inspected locally</span></span>
      <span className="ms" style={css("font-size:16px;color:var(--ink3)")}>{ICON.close}</span>
      </div>
      </div>
      </div>
      </section>
      <section style={css("margin-top:14px;border:1px solid var(--line);border-radius:14px;background:var(--panel);overflow:hidden")}>
      <h2 style={css("font-size:15px;font-weight:500;padding:16px 16px 0")}>Route coverage</h2>
      <p style={css("padding:3px 16px 0;font-size:11.5px;color:var(--ink3)")}>Every route renders at desktop, tablet and mobile in dark and light. Layout switches at 768 px; the rail is replaced by a drawer and bottom navigation.</p>
      <div style={css("margin-top:12px;overflow-x:auto")}>
      <table style={css("width:100%;min-width:620px;font-size:12.5px")}>
      <thead><tr style={css("text-align:left;color:var(--ink3);background:var(--panel2)")}>
      <th scope="col" style={css("padding:9px 14px;font-weight:400;font-size:11px;text-transform:uppercase;letter-spacing:.06em")}>Route</th>
      <th scope="col" style={css("padding:9px 10px;font-weight:400;font-size:11px;text-transform:uppercase;letter-spacing:.06em")}>Screen</th>
      <th scope="col" style={css("padding:9px 10px;font-weight:400;font-size:11px;text-transform:uppercase;letter-spacing:.06em")}>Desktop</th>
      <th scope="col" style={css("padding:9px 10px;font-weight:400;font-size:11px;text-transform:uppercase;letter-spacing:.06em")}>Tablet</th>
      <th scope="col" style={css("padding:9px 10px;font-weight:400;font-size:11px;text-transform:uppercase;letter-spacing:.06em")}>Mobile</th>
      <th scope="col" style={css("padding:9px 14px;font-weight:400;font-size:11px;text-transform:uppercase;letter-spacing:.06em")}>Dark / Light</th>
      </tr></thead>
      <tbody>

                        {(v.showRoutes ?? []).map((r, rI) => (
                    
      <tr key={rI} style={css("border-top:1px solid var(--line)")}>
      <td style={css("padding:8px 14px;font-family:'Geist Mono',ui-monospace,monospace;font-size:11.5px;color:var(--ink2)")}>{r.path}</td>
      <td style={css("padding:8px 10px")}>{r.name}</td>
      <td style={css("padding:8px 10px")}><span className="ms" style={css("font-size:16px;color:var(--em)")}>{ICON.check}</span></td>
      <td style={css("padding:8px 10px")}><span className="ms" style={css("font-size:16px;color:var(--em)")}>{ICON.check}</span></td>
      <td style={css("padding:8px 10px")}><span className="ms" style={css("font-size:16px;color:var(--em)")}>{ICON.check}</span></td>
      <td style={css("padding:8px 14px")}><span className="ms" style={css("font-size:16px;color:var(--em)")}>{ICON.check}</span></td>
      </tr>

                        ))}
      </tbody>
      </table>
      </div>
      </section>
      <section style={css("margin-top:14px;border:1px solid var(--line);border-radius:14px;background:var(--panel);overflow:hidden")}>
      <h2 style={css("font-size:15px;font-weight:500;padding:16px 16px 12px")}>State coverage</h2>
      <ul>

                    {(v.showStates ?? []).map((s, sI) => (
                
      <li key={sI} style={css("display:flex;flex-wrap:wrap;gap:10px;padding:11px 16px;border-top:1px solid var(--line)")}>
      <span style={css("flex:0 0 140px;font-size:12.5px;font-weight:500")}>{s.area}</span>
      <span style={css("flex:1;min-width:200px;font-size:12px;color:var(--ink2);line-height:1.5")}>{s.list}</span>
      </li>

                    ))}
      </ul>
      </section>
      <section style={css("margin-top:14px;border:1px solid var(--line);border-radius:14px;background:var(--panel);overflow:hidden")}>
      <h2 style={css("font-size:15px;font-weight:500;padding:16px 16px 12px")}>QA report</h2>
      <ul>

                    {(v.showQA ?? []).map((q, qI) => (
                
      <li key={qI} style={css("display:flex;flex-wrap:wrap;gap:10px;padding:11px 16px;border-top:1px solid var(--line)")}>
      <span style={css("flex:0 0 120px;font-size:12.5px;font-weight:500")}>{q.area}</span>
      <span style={css("flex:1;min-width:220px;font-size:12px;color:var(--ink2);line-height:1.5")}>{q.text}</span>
      </li>

                    ))}
      </ul>
      </section>
      <section style={css("margin-top:14px;border:1px solid var(--line);border-radius:14px;background:var(--panel);overflow:hidden")}>
      <h2 style={css("font-size:15px;font-weight:500;padding:16px 16px 0")}>Persistence matrix</h2>
      <p style={css("padding:3px 16px 0;font-size:11.5px;color:var(--ink3)")}>What survives a refresh, and what cannot by design.</p>
      <div style={css("margin-top:12px;overflow-x:auto")}>
      <table style={css("width:100%;min-width:560px;font-size:12.5px")}>
      <thead><tr style={css("text-align:left;color:var(--ink3);background:var(--panel2)")}>
      <th scope="col" style={css("padding:9px 14px;font-weight:400;font-size:11px;text-transform:uppercase;letter-spacing:.06em")}>Data</th>
      <th scope="col" style={css("padding:9px 10px;font-weight:400;font-size:11px;text-transform:uppercase;letter-spacing:.06em")}>Status</th>
      <th scope="col" style={css("padding:9px 14px;font-weight:400;font-size:11px;text-transform:uppercase;letter-spacing:.06em")}>Detail</th>
      </tr></thead>
      <tbody>

                        {(v.persistRows ?? []).map((r, rI) => (
                    
      <tr key={rI} style={css("border-top:1px solid var(--line)")}>
      <td style={css("padding:8px 14px")}>{r.k}</td>
      <td style={css(`padding:8px 10px;color:${r.color}`)}>{r.v}</td>
      <td style={css("padding:8px 14px;color:var(--ink3)")}>{r.note}</td>
      </tr>

                        ))}
      </tbody>
      </table>
      </div>
      </section>
      <section style={css("margin-top:14px;border:1px solid var(--line);border-radius:14px;background:var(--panel);overflow:hidden")}>
      <div style={css("display:flex;flex-wrap:wrap;align-items:baseline;gap:10px;padding:16px 16px 0")}>
      <h2 style={css("flex:1;min-width:180px;font-size:15px;font-weight:500")}>Catalogue import schema</h2>
      <span style={css("padding:3px 9px;border:1px solid var(--amber);border-radius:99px;background:var(--amberSoft);font-size:10.5px;color:var(--amber);white-space:nowrap")}>Representative data</span>
      </div>
      <p style={css("padding:4px 16px 0;font-size:11.5px;color:var(--ink3);line-height:1.5")}>{v.catMeta.loaded} records from the built-in representative fallback dataset. Assign <span style={css("font-family:'Geist Mono',ui-monospace,monospace")}>window.PRIVATOOLS_CATALOGUE</span> before the app loads to supply the real catalogue — it declares {v.catMeta.declared} entries ({v.catMeta.pdf} PDF, {v.catMeta.nonPdf} non-PDF) and is not present here. Records without <span style={css("font-family:'Geist Mono',ui-monospace,monospace")}>modeVerified: true</span> render as “Processing mode unverified”. Public copy stays “{v.catMeta.publicCopy}”.</p>
      <div style={css("margin-top:12px;overflow-x:auto")}>
      <table style={css("width:100%;min-width:560px;font-size:12.5px")}>
      <thead><tr style={css("text-align:left;color:var(--ink3);background:var(--panel2)")}>
      <th scope="col" style={css("padding:9px 14px;font-weight:400;font-size:11px;text-transform:uppercase;letter-spacing:.06em")}>Field</th>
      <th scope="col" style={css("padding:9px 10px;font-weight:400;font-size:11px;text-transform:uppercase;letter-spacing:.06em")}>Type</th>
      <th scope="col" style={css("padding:9px 14px;font-weight:400;font-size:11px;text-transform:uppercase;letter-spacing:.06em")}>Notes</th>
      </tr></thead>
      <tbody>

                        {(v.schemaRows ?? []).map((r, rI) => (
                    
      <tr key={rI} style={css("border-top:1px solid var(--line)")}>
      <td style={css("padding:8px 14px;font-family:'Geist Mono',ui-monospace,monospace;font-size:11.5px;color:var(--ink2)")}>{r.field}</td>
      <td style={css("padding:8px 10px;color:var(--ink3)")}>{r.type}</td>
      <td style={css("padding:8px 14px;color:var(--ink2)")}>{r.note}</td>
      </tr>

                        ))}
      </tbody>
      </table>
      </div>
      </section>
      <section style={css("margin-top:14px;border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:16px")}>
      <h2 style={css("font-size:15px;font-weight:500")}>Known limitations and data dependencies</h2>
      <ul style={css("margin-top:10px;display:flex;flex-direction:column;gap:7px")}>

                    {(v.showLimits ?? []).map((l, lI) => (
                
      <li key={lI} style={css("display:flex;gap:9px;font-size:12px;color:var(--ink2);line-height:1.5")}><span style={css("width:5px;height:5px;border-radius:50%;background:var(--amber);flex:none;margin-top:6px")}></span>{l.t}</li>

                    ))}
      </ul>
      </section>
      </div>

            </>)}


            {Boolean(v.isAccount) && (<>
        
      <div style={css("max-width:1000px;margin:0 auto;padding:20px 24px 0;animation:slidein 200ms ease both")}>
      <h1 style={css("font-size:clamp(21px,3.6vw,26px);font-weight:600;letter-spacing:-.02em")}>{v.acctTitle}</h1>
      <p style={css("margin-top:3px;font-size:12.5px;color:var(--ink3)")}>{v.acctLede}</p>

      <div style={css(`display:${v.acctRecoveryD};border:1px solid var(--emLine);border-radius:12px;background:var(--emSoft);padding:16px;margin-top:16px`)}>
      <div style={css("font-size:14.5px;font-weight:600")}>Save your recovery code</div>
      <div style={css("font-size:12.5px;color:var(--ink2);margin-top:6px;line-height:1.6")}>This is shown once and is the only way back into your account. We send no email, so there is no reset link to fall back on.</div>
      <div style={css("display:flex;flex-wrap:wrap;align-items:center;gap:10px;margin-top:12px")}>
      <code style={css("border:1px solid var(--line);border-radius:8px;background:var(--panel);padding:8px 11px;font-family:'Geist Mono',ui-monospace,monospace;font-size:13.5px;letter-spacing:.1em")}>{v.acctRecoveryCode}</code>
      <button type="button" onClick={v.acctCopyRecovery} style={css("height:34px;padding:0 13px;border-radius:8px;border:1px solid var(--line);background:transparent;color:var(--ink);cursor:pointer;font-size:12.5px;font-weight:500")}>{v.acctCopyLabel}</button>
      </div>
      <button type="button" onClick={v.acctAckRecovery} style={css("margin-top:13px;height:36px;padding:0 15px;border-radius:8px;border:none;cursor:pointer;font-size:12.5px;font-weight:500;background:var(--em);color:var(--em-ink,#04231A)")}>I have saved it</button>
      <div style={css(`display:${v.acctRecoveryNudgeD};font-size:11.5px;color:var(--ink3);margin-top:8px`)}>Write it down or put it in a password manager before continuing.</div>
      </div>

                {Boolean(v.acctSignedOut) && (<>
            
      <div style={css("display:flex;flex-wrap:wrap;gap:14px;margin-top:16px;align-items:flex-start")}>
      <form onSubmit={v.acctSubmit} style={css("flex:2 1 380px;min-width:0;border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:16px;display:grid;gap:11px")}>
      <div style={css("display:flex;gap:7px")}>
      <button type="button" onClick={v.acctShowSignIn} style={css(`flex:1;height:34px;border-radius:8px;cursor:pointer;font-size:12.5px;font-weight:500;border:1px solid ${v.acctSignInBd};background:${v.acctSignInBg};color:${v.acctSignInFg}`)}>Sign in</button>
      <button type="button" onClick={v.acctShowSignUp} style={css(`flex:1;height:34px;border-radius:8px;cursor:pointer;font-size:12.5px;font-weight:500;border:1px solid ${v.acctSignUpBd};background:${v.acctSignUpBg};color:${v.acctSignUpFg}`)}>Create account</button>
      </div>
      <label style={css("display:grid;gap:4px;font-size:11.5px;color:var(--ink3)")}>Email
              <input type="email" value={v.acctEmail} onChange={v.acctSetEmail} autoComplete="email" required={v.true} style={css("height:38px;padding:0 11px;border-radius:8px;border:1px solid var(--line);background:var(--bg2);color:var(--ink);font-size:13px")} />
      </label>
      <label style={css("display:grid;gap:4px;font-size:11.5px;color:var(--ink3)")}>{v.acctPasswordLabel}
              <input type="password" value={v.acctPassword} onChange={v.acctSetPassword} autoComplete={v.acctPwAutocomplete} required={v.true} style={css("height:38px;padding:0 11px;border-radius:8px;border:1px solid var(--line);background:var(--bg2);color:var(--ink);font-size:13px")} />
      </label>
      <div style={css(`display:${v.acctRecoverD}`)}>
      <label style={css("display:flex;flex-direction:column;gap:5px;font-size:12px;color:var(--ink2)")}>Recovery code
                <input type="text" value={v.acctRecoveryInput} onChange={v.acctSetRecoveryInput} autoComplete="one-time-code" style={css("padding:9px 11px;border-radius:8px;border:1px solid var(--line);background:var(--panel2);color:var(--ink);font-size:13px;min-height:44px;font-family:'Geist Mono',ui-monospace,monospace")} />
      </label>
      </div>
      <div style={css(`display:${v.acctHintD};font-size:11.5px;color:var(--ink3)`)}>At least 10 characters. Length is what makes a password strong.</div>
      <div role="alert" style={css(`display:${v.acctErrD};font-size:12px;color:var(--coral)`)}>{v.acctError}</div>
      <button type="button" onClick={v.acctShowRecover} style={css("align-self:flex-start;background:none;border:none;padding:0;cursor:pointer;font-size:11.5px;color:var(--ink2);text-decoration:underline")}>Forgotten your password? Use your recovery code</button>
      <button type="submit" disabled={v.acctBusy} style={css(`height:38px;border-radius:8px;border:none;cursor:pointer;font-size:13px;font-weight:500;background:var(--em);color:var(--bg);opacity:${v.acctBusyOpacity}`)}>{v.acctSubmitLabel}</button>
      </form>
      <section style={css("flex:1 1 260px;min-width:0;border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:16px")}>
      <h2 style={css("font-size:14px;font-weight:500")}>You do not need an account</h2>
      <p style={css("margin-top:7px;font-size:12.5px;line-height:1.6;color:var(--ink2)")}>Every tool works without signing in, and nothing on a tool page asks you to. An account exists for one thing: issuing API keys for the developer API.</p>
      <p style={css("margin-top:9px;font-size:12.5px;line-height:1.6;color:var(--ink2)")}>We store your email address and a scrypt hash of your password — never the password itself. Deleting your account removes both immediately, along with every key.</p>
      </section>
      </div>

                </>)}

                {Boolean(v.acctSignedIn) && (<>
            
      <div style={css("display:flex;flex-wrap:wrap;gap:14px;margin-top:16px;align-items:flex-start")}>
      <section style={css("flex:2 1 380px;min-width:0;border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:16px")}>
      <div style={css("display:flex;align-items:center;justify-content:space-between;gap:10px")}>
      <h2 style={css("font-size:14px;font-weight:500")}>API keys</h2>
      <button type="button" onClick={v.acctNewKey} style={css("height:30px;padding:0 11px;border-radius:8px;border:none;cursor:pointer;background:var(--em);color:var(--bg);font-size:12.5px;font-weight:500")}>New key</button>
      </div>
      <div style={css(`display:${v.acctNewKeyD};margin-top:11px;padding:11px;border:1px solid var(--emLine);border-radius:11px;background:var(--emSoft)`)}>
      <div style={css("font-size:11.5px;font-weight:600;color:var(--em)")}>Copy this now — it is not shown again</div>
      <code style={css("display:block;margin-top:6px;font-family:'Geist Mono',monospace;font-size:12px;word-break:break-all;color:var(--ink)")}>{v.acctNewKeyValue}</code>
      </div>
      <div style={css(`display:${v.acctNoKeysD};margin-top:11px;font-size:12.5px;color:var(--ink2)`)}>No keys yet. Create one to start using the API.</div>

                      {(v.acctKeys ?? []).map((k, kI) => (
                  
      <div key={kI} style={css("display:flex;align-items:center;gap:10px;height:44px;border-top:1px solid var(--line)")}>
      <div style={css("flex:1;min-width:0")}>
      <div style={css(`font-size:13px;color:${k.labelColor}`)}>{k.label}</div>
      <div style={css("font-size:11px;color:var(--ink3)")}>{k.meta}</div>
      </div>
      <button type="button" onClick={k.revoke} disabled={k.revoked} style={css(`display:${k.revokeD};height:28px;padding:0 10px;border-radius:7px;cursor:pointer;border:1px solid var(--line2);background:transparent;color:var(--coral);font-size:12px`)}>Revoke</button>
      </div>

                      ))}
      </section>
      <section style={css("flex:1 1 260px;min-width:0;border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:16px")}>
      <h2 style={css("font-size:14px;font-weight:500")}>Signed in</h2>
      <div style={css("margin-top:5px;font-size:12.5px;color:var(--ink2);word-break:break-all")}>{v.acctEmailShown}</div>
      <button type="button" onClick={v.acctSignOut} style={css("margin-top:12px;width:100%;height:34px;border-radius:8px;cursor:pointer;border:1px solid var(--line2);background:transparent;color:var(--ink);font-size:12.5px")}>Sign out</button>
      <button type="button" onClick={v.acctDelete} style={css("margin-top:7px;width:100%;height:34px;border-radius:8px;cursor:pointer;border:1px solid var(--coral);background:transparent;color:var(--coral);font-size:12.5px")}>{v.acctDeleteLabel}</button>
      <p style={css("margin-top:9px;font-size:11.5px;line-height:1.5;color:var(--ink3)")}>Deleting removes your email, your password hash and every key. It cannot be undone.</p>
      </section>
      </div>

                </>)}
      </div>

            </>)}


            {Boolean(v.isVaultReal) && (<>
        
      <div style={css("max-width:1000px;margin:0 auto;padding:20px 24px 0;animation:slidein 200ms ease both")}>
      <h1 style={css("font-size:clamp(21px,3.6vw,26px);font-weight:600;letter-spacing:-.02em")}>Vault</h1>
      <div style={css("margin-top:3px;font-size:12.5px;color:var(--ink3)")}>Passwords for locked documents, encrypted on this device. {v.vltCount} stored.</div>
      <div style={css("display:flex;flex-wrap:wrap;gap:14px;margin-top:16px;align-items:flex-start")}>
      <div style={css("flex:2 1 380px;min-width:0;border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:16px")}>
      <div style={css("font-size:14px;font-weight:500")}>Stored passwords</div>
      <div style={css(`display:${v.vltEmptyD};font-size:12.5px;color:var(--ink2);margin-top:10px`)}>Nothing stored yet. Add a password below and PrivaTools will try it automatically when you open a locked file.</div>
      <div role="status" style={css(`display:${v.vltUnreadableD};margin-top:10px;font-size:12px;color:var(--coral)`)}>{v.vltUnreadable}</div>

                    {(v.vltEntries ?? []).map((e, eI) => (
                
      <div key={eI} style={css("display:flex;align-items:center;gap:10px;padding:11px 0;border-top:1px solid var(--line)")}>
      <div style={css("flex:1;min-width:0")}>
      <div style={css(`font-size:13px;font-weight:600;color:${e.labelColor}`)}>{e.label}</div>
      <div style={css("font-family:'Geist Mono',monospace;font-size:12px")}>{e.secret}</div>
      <div style={css("font-size:11px;color:var(--ink3);margin-top:2px")}>{e.meta}</div>
      </div>
      <button type="button" onClick={e.reveal} aria-label={e.revealLabel} style={css("height:28px;padding:0 10px;border-radius:7px;cursor:pointer;border:1px solid var(--line2);background:transparent;color:var(--ink2);font-size:12px")}><span className="ms" style={css("font-size:18px;")}>{ICON[e.revealIcon]}</span></button>
      <button type="button" onClick={e.copy} aria-label="Copy password" style={css("height:28px;padding:0 10px;border-radius:7px;cursor:pointer;border:1px solid var(--line2);background:transparent;color:var(--ink2);font-size:12px")}><span className="ms" style={css("font-size:18px;")}>{ICON['content_copy']}</span></button>
      <button type="button" onClick={e.remove} aria-label="Delete password" style={css("height:28px;padding:0 10px;border-radius:7px;cursor:pointer;border:1px solid var(--line2);background:transparent;color:var(--coral);font-size:12px")}><span className="ms" style={css("font-size:18px;")}>{ICON['delete']}</span></button>
      </div>

                    ))}
      <button type="button" onClick={v.vltClear} style={css(`display:${v.vltClearD};margin-top:14px;height:28px;padding:0 10px;border-radius:7px;cursor:pointer;border:1px solid var(--line2);background:transparent;color:var(--coral);font-size:12px`)}>{v.vltClearLabel}</button>
      </div>
      <form onSubmit={v.vltAdd} style={css("flex:1 1 260px;min-width:0;border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:16px")}>
      <div style={css("font-size:14px;font-weight:500")}>Add a password</div>
      <label style={css("display:grid;gap:4px;font-size:11.5px;color:var(--ink3);margin-top:11px")}>Name
              <input type="text" value={v.vltLabel} onChange={v.vltSetLabel} placeholder="e.g. Bank statements" style={css("height:38px;padding:0 11px;border-radius:8px;border:1px solid var(--line);background:var(--bg2);color:var(--ink);font-size:13px")} />
      </label>
      <label style={css("display:grid;gap:4px;font-size:11.5px;color:var(--ink3);margin-top:9px")}>Password
              <input type="password" value={v.vltPassword} onChange={v.vltSetPassword} autoComplete="off" style={css("height:38px;padding:0 11px;border-radius:8px;border:1px solid var(--line);background:var(--bg2);color:var(--ink);font-size:13px")} />
      </label>
      <div role="alert" style={css(`display:${v.vltErrD};margin-top:8px;font-size:12px;color:var(--coral)`)}>{v.vltError}</div>
      <button type="submit" disabled={v.vltBusy} style={css(`margin-top:11px;width:100%;opacity:${v.vltBusyOpacity};height:38px;border-radius:8px;border:none;cursor:pointer;font-size:13px;font-weight:500;background:var(--em);color:var(--bg)`)}>{v.vltAddLabel}</button>
      <p style={css("margin-top:9px;font-size:11.5px;line-height:1.5;color:var(--ink3)")}>Encrypted with a key this browser generated and cannot export. That means it never leaves this device — and it cannot sync to another one. Clearing site data erases it.</p>
      </form>
      </div>
      </div>

            </>)}


            {Boolean(v.isRealTool) && (<>
        
      <div style={css("max-width:1000px;margin:0 auto;padding:20px 24px 0;animation:slidein 200ms ease both")}>
      <nav aria-label="Breadcrumb" style={css("display:flex;align-items:center;gap:6px;font-size:11.5px;color:var(--ink3);margin-bottom:12px")}>
      <a href="/" onClick={v.rtHome} style={css("color:var(--ink2)")}>Home</a>
      <span className="ms" style={css("font-size:13px")}>{ICON.chevron_right}</span>
      <a href="/tools" onClick={v.rtTools} style={css("color:var(--ink2)")}>All Tools</a>
      <span className="ms" style={css("font-size:13px")}>{ICON.chevron_right}</span>
      <span style={css("color:var(--ink)")}>{v.realToolName}</span>
      </nav>
      <h1 style={css("font-size:clamp(21px,3.6vw,26px);font-weight:600;letter-spacing:-.02em")}>{v.realToolName}</h1>
      <p style={css("margin-top:3px;font-size:12.5px;color:var(--ink3)")}>{v.realToolDesc}</p>
      <div style={css("display:flex;flex-wrap:wrap;gap:6px;margin-top:11px")}>

                  {(v.rtChips ?? []).map((c, cI) => (
              
      <span key={cI} style={css(`display:inline-flex;align-items:center;gap:5px;font-size:11.5px;color:${c.fg};border:1px solid var(--line);border-radius:7px;padding:4px 9px;background:${c.bg}`)}>
      <span className="ms" style={css("font-size:14px")}>{ICON[c.icon]}</span>{c.label}</span>

                  ))}
      </div>
      <section style={css("margin-top:16px;border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:16px")}>
          {v.realToolUI}
        </section>
      <div style={css(`display:${v.rtRelatedD};margin-top:16px`)}>
      <h2 style={css("font-size:14px;font-weight:500;margin-bottom:9px")}>Related tools</h2>
      <div style={css("display:grid;grid-template-columns:repeat(auto-fill,minmax(200px,1fr));gap:10px")}>

                    {(v.rtRelated ?? []).map((r, rI) => (
                
      <button key={rI} type="button" onClick={r.go} style={css("text-align:left;padding:12px;border:1px solid var(--line);border-radius:11px;background:var(--panel);cursor:pointer;color:var(--ink)")}>
      <span className="ms" style={css("font-size:18px;color:var(--em)")}>{ICON[r.icon]}</span>
      <div style={css("font-size:13px;margin-top:7px")}>{r.name}</div>
      <div style={css("font-size:11.5px;color:var(--ink2);margin-top:2px;line-height:1.45")}>{r.desc}</div>
      </button>

                    ))}
      </div>
      </div>
      </div>

            </>)}
      <footer style={css("max-width:1180px;margin:32px auto 0;padding:18px 24px 8px;border-top:1px solid var(--line)")}>
      <div style={css("display:flex;flex-wrap:wrap;gap:14px;align-items:flex-start")}>
      <div style={css("flex:1 1 220px")}>
      <span style={css("display:flex;align-items:center;gap:8px;font-size:13px;font-weight:600")}>
      <svg viewBox="0 0 40 40" aria-hidden="true" style={css("width:20px;height:20px")}><g fill="none" stroke="var(--em)" strokeWidth="1.8" strokeLinejoin="round"><path d="M20 5 L34 13 L34 27 L20 35 L6 27 L6 13 Z"></path><path d="M6 13 L20 21 L34 13"></path><path d="M20 21 L20 35"></path></g></svg>
                    PrivaTools
                  </span>
      <p style={css("margin-top:6px;font-size:11.5px;color:var(--ink3);max-width:34ch;line-height:1.5")}>Free forever, owner-funded, built by an individual in India. 200+ free file tools, local-first, 500 MB per file.</p>
      </div>
      <nav aria-label="Footer" style={css("flex:2 1 320px;display:flex;flex-wrap:wrap;gap:8px 16px;align-items:center")}>

                  {(v.footerLinks ?? []).map((l, lI) => (
              
      <button key={lI} onClick={l.go} style={css("font-size:12px;color:var(--ink2)")}>{l.label}</button>

                  ))}
      </nav>
      </div>
      </footer>
      </main>

          {Boolean(v.evidenceOpen) && (<>
      
      <div role="dialog" aria-modal="true" aria-label="Evidence" data-overlay="1" style={css("position:fixed;inset:0;z-index:65;display:flex;justify-content:flex-end;animation:fadein 150ms ease both")}>
      <div onClick={v.closeEvidence} style={css("position:absolute;inset:0;background:rgba(2,10,16,.55)")}></div>
      <aside style={css("position:relative;width:380px;max-width:92vw;height:100%;overflow-y:auto;background:var(--panel);border-left:1px solid var(--line2);animation:slidein 200ms ease both")}>
      <div style={css("display:flex;align-items:flex-start;gap:10px;padding:16px;border-bottom:1px solid var(--line)")}>
      <span style={css("display:block;flex:1;min-width:0")}><span style={css("display:block;font-size:11px;letter-spacing:.08em;text-transform:uppercase;color:var(--ink3)")}>Evidence</span><span style={css("display:block;margin-top:3px;font-size:14.5px;font-weight:500")}>{v.evidenceTitle}</span></span>
      <button onClick={v.closeEvidence} aria-label="Close evidence" style={css("display:flex;align-items:center;justify-content:center;width:32px;height:32px;border-radius:8px")}><span className="ms" style={css("font-size:19px;color:var(--ink3)")}>{ICON.close}</span></button>
      </div>
      <ul>

                  {(v.evidenceRows ?? []).map((r, rI) => (
              
      <li key={rI} style={css("display:flex;gap:12px;padding:12px 16px;border-bottom:1px solid var(--line)")}>
      <span style={css("flex:0 0 78px;font-size:11px;letter-spacing:.06em;text-transform:uppercase;color:var(--ink3)")}>{r.k}</span>
      <span style={css("flex:1;font-size:12.5px;color:var(--ink2);line-height:1.5")}>{r.v}</span>
      </li>

                  ))}
      </ul>
      <p style={css("padding:14px 16px;font-size:11.5px;color:var(--ink3);line-height:1.55")}>No universal winner—results change with your priorities. We publish evidence and dates instead of ratings, votes or testimonials.</p>
      </aside>
      </div>

          </>)}

          {Boolean(v.isMobile) && (<>
      
      <nav aria-label="Primary" style={css("position:fixed;left:0;right:0;bottom:0;z-index:40;display:flex;background:var(--bg2);border-top:1px solid var(--line);padding-bottom:env(safe-area-inset-bottom)")}>

              {(v.navMobile ?? []).map((m, mI) => (
          
      <button key={mI} onClick={m.go} aria-current={m.cur} style={css(`flex:1;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:3px;min-height:56px;padding:6px 2px;color:${m.fg};background:${m.bg}`)}>
      <span className="ms" style={css("font-size:21px")}>{ICON[m.icon]}</span>
      <span style={css("font-size:10.5px")}>{m.label}</span>
      </button>

              ))}
      </nav>

          </>)}
      </div>

        {Boolean(v.drawer) && (<>
    
      <div data-overlay="1" style={css("position:fixed;inset:0;z-index:60;display:flex;animation:fadein 160ms ease both")}>
      <div onClick={v.closeDrawer} style={css("position:absolute;inset:0;background:rgba(2,10,16,.6)")}></div>
      <aside role="dialog" aria-modal="true" aria-label="Menu" style={css("position:relative;width:264px;max-width:84vw;height:100%;overflow-y:auto;background:var(--bg2);border-right:1px solid var(--line);animation:slidein 200ms ease both")}>
      <div style={css("display:flex;align-items:center;justify-content:space-between;padding:14px 12px")}>
      <span style={css("display:flex;align-items:center;gap:9px;font-size:15px;font-weight:600")}><svg viewBox="0 0 40 40" aria-hidden="true" style={css("width:22px;height:22px")}><g fill="none" stroke="var(--em)" strokeWidth="1.8" strokeLinejoin="round"><path d="M20 5 L34 13 L34 27 L20 35 L6 27 L6 13 Z"></path><path d="M6 13 L20 21 L34 13"></path><path d="M20 21 L20 35"></path></g></svg>PrivaTools</span>
      <button onClick={v.closeDrawer} aria-label="Close menu" style={css("display:flex;align-items:center;justify-content:center;width:44px;height:44px;border-radius:10px")}><span className="ms" style={css("font-size:22px")}>{ICON.close}</span></button>
      </div>
      <nav style={css("display:flex;flex-direction:column;gap:2px;padding:4px 10px 16px")}>

                {(v.navMain ?? []).map((n, nI) => (
            
      <button key={nI} onClick={n.goClose} style={css(`display:flex;align-items:center;gap:11px;min-height:44px;padding:0 10px;border-radius:9px;font-size:14px;color:${n.fg};background:${n.bg}`)}>
      <span className="ms" style={css(`font-size:20px;color:${n.ic}`)}>{ICON[n.icon]}</span>{n.label}
                  </button>

                ))}
      <div style={css("padding:14px 10px 6px;font-size:10.5px;letter-spacing:.1em;text-transform:uppercase;color:var(--ink3)")}>Categories</div>

                {(v.navCats ?? []).map((c, cI) => (
            
      <button key={cI} onClick={c.goClose} style={css("display:flex;align-items:center;gap:11px;min-height:44px;padding:0 10px;border-radius:9px;font-size:14px;color:var(--ink2)")}>
      <span className="ms" style={css(`font-size:20px;color:${c.ic}`)}>{ICON[c.icon]}</span>{c.label}
                  </button>

                ))}
      <div style={css("margin-top:12px;padding:10px;border-top:1px solid var(--line)")}>
      <div role="group" aria-label="Theme" style={css("display:flex;gap:4px")}>

                    {(v.themes ?? []).map((t, tI) => (
                
      <button key={tI} onClick={t.go} aria-pressed={t.on} style={css(`flex:1;display:flex;align-items:center;justify-content:center;gap:6px;min-height:44px;border-radius:9px;border:1px solid var(--line);background:${t.bg};color:${t.fg};font-size:12px`)}><span className="ms" style={css("font-size:18px")}>{ICON[t.icon]}</span>{t.title}</button>

                    ))}
      </div>
      </div>
      </nav>
      </aside>
      </div>

        </>)}

        {Boolean(v.palette) && (<>
    
      <div role="dialog" aria-modal="true" aria-label="Search and commands" data-overlay="1" style={css("position:fixed;inset:0;z-index:70;display:flex;align-items:flex-start;justify-content:center;padding:10vh 16px 16px;animation:fadein 140ms ease both")}>
      <div onClick={v.closePalette} style={css("position:absolute;inset:0;background:rgba(2,10,16,.62);backdrop-filter:blur(3px)")}></div>
      <div style={css("position:relative;width:100%;max-width:620px;border:1px solid var(--line2);border-radius:14px;background:var(--panel);box-shadow:0 24px 60px rgba(0,0,0,.45);overflow:hidden;animation:slidein 180ms ease both")}>
      <div style={css("display:flex;align-items:center;gap:10px;padding:12px 14px;border-bottom:1px solid var(--line)")}>
      <span className="ms" style={css("font-size:20px;color:var(--ink3)")}>{ICON.search}</span>
      <input id="paletteInput" value={v.pq} onChange={v.onPq} onKeyDown={v.onPaletteKey} placeholder="Search tools, commands or plain language" aria-label="Search tools and commands" style={css("flex:1;min-width:0;height:28px;border:0;background:none;font-size:15px;outline:none")} />
      <kbd style={css("font-family:'Geist Mono',ui-monospace,monospace;font-size:10.5px;padding:2px 5px;border:1px solid var(--line);border-radius:5px;color:var(--ink3)")}>Esc</kbd>
      </div>
      <div style={css("max-height:52vh;overflow-y:auto;padding:6px")}>

                {Boolean(v.pEmpty) && (<>
            
      <div style={css("padding:26px 16px;text-align:center")}>
      <p style={css("font-size:13px;color:var(--ink2)")}>No tool matches “{v.pq}”.</p>
      <p style={css("margin-top:5px;font-size:12px;color:var(--ink3)")}>Try a plain-language task, or browse the full catalogue.</p>
      <button onClick={v.goTools} style={css("margin-top:10px;font-size:12.5px;color:var(--em)")}>Open All Tools →</button>
      </div>

                </>)}

                {(v.pResults ?? []).map((r, rI) => (
            
      <button key={rI} onClick={r.go} onMouseEnter={r.hover} aria-selected={r.sel} style={css(`width:100%;display:flex;align-items:center;gap:10px;padding:9px 10px;border-radius:9px;background:${r.bg};animation:slidein ${r.delay} ease both`)}>
      <span className="ms" style={css(`font-size:19px;color:${r.color}`)}>{ICON[r.icon]}</span>
      <span style={css("display:block;flex:1;min-width:0")}>
      <span style={css("display:block;font-size:13px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis")}>{r.name}</span>
      <span style={css("display:block;font-size:11px;color:var(--ink3);white-space:nowrap;overflow:hidden;text-overflow:ellipsis")}>{r.sub}</span>
      </span>
      <span style={css("font-size:10.5px;color:var(--ink3);border:1px solid var(--line);border-radius:5px;padding:2px 6px")}>{r.kind}</span>
      </button>

                ))}
      </div>
      <div style={css("display:flex;gap:14px;padding:9px 14px;border-top:1px solid var(--line);font-size:11px;color:var(--ink3)")}>
      <span>↑↓ move</span><span>↵ open</span><span>Esc close</span><span style={css("margin-left:auto")}>Synonyms and tasks included</span>
      </div>
      </div>
      </div>

        </>)}

        {Boolean(v.dialogOpen) && (<>
    
      <div data-overlay="1" role="dialog" aria-modal="true" aria-label="Confirm" style={css("position:fixed;inset:0;z-index:75;display:flex;align-items:center;justify-content:center;padding:20px;animation:fadein 140ms ease both")}>
      <div onClick={v.closeDialog} style={css("position:absolute;inset:0;background:rgba(2,10,16,.62)")}></div>
      <div style={css("position:relative;width:100%;max-width:460px;border:1px solid var(--line2);border-radius:14px;background:var(--panel);box-shadow:0 24px 60px rgba(0,0,0,.45);overflow:hidden;animation:slidein 180ms ease both")}>

              {Boolean(v.dialogIsBackup) && (<>
          
      <div style={css("padding:16px")}>
      <h2 style={css("font-size:16px;font-weight:500")}>Export encrypted backup</h2>
      <p style={css("margin-top:5px;font-size:12px;color:var(--ink2);line-height:1.55")}>The export is encrypted with a passphrase you choose. <strong style={css("font-weight:500;color:var(--ink)")}>Passwords are included.</strong><strong style={css("font-weight:500;color:var(--ink)")}>Cloud access tokens are excluded</strong> and stay on this device only.</p>

                  {Boolean(v.bkIdle) && (<>
              
      <div style={css("display:flex;flex-direction:column;gap:11px;margin-top:14px")}>
      <label style={css("display:block")}>
      <span style={css("display:block;font-size:11px;color:var(--ink3);margin-bottom:5px")}>Passphrase</span>
      <span style={css("display:flex;align-items:center;gap:8px;height:38px;padding:0 10px;border:1px solid var(--line);border-radius:9px;background:var(--panel2)")}>
      <input type={v.bkType} value={v.bkPass} onChange={v.onBkPass} aria-label="Backup passphrase" style={css("flex:1;min-width:0;border:0;background:none;outline:none;font-size:13px;font-family:'Geist Mono',ui-monospace,monospace")} />
      <button onClick={v.toggleBkShow} aria-pressed={v.bkShow} aria-label="Show or hide passphrase" style={css("display:flex")}><span className="ms" style={css("font-size:17px;color:var(--ink3)")}>{ICON.visibility}</span></button>
      </span>
      </label>
      <span style={css("display:block;height:4px;border-radius:99px;background:var(--panel3);overflow:hidden")}><span style={css(`display:block;height:100%;width:${v.bkBarW};background:${v.bkStrengthColor};transition:width 200ms ease`)}></span></span>
      <span style={css(`display:block;font-size:11.5px;color:${v.bkStrengthColor}`)}>{v.bkStrength}</span>
      <label style={css("display:block")}>
      <span style={css("display:block;font-size:11px;color:var(--ink3);margin-bottom:5px")}>Confirm passphrase</span>
      <input type={v.bkType} value={v.bkConfirm} onChange={v.onBkConfirm} aria-label="Confirm passphrase" style={css("width:100%;height:38px;padding:0 10px;border:1px solid var(--line);border-radius:9px;background:var(--panel2);font-size:13px;font-family:'Geist Mono',ui-monospace,monospace")} />
      </label>

                      {Boolean(v.bkMismatch) && (<>
                        <span role="alert" style={css("display:block;font-size:11.5px;color:var(--coral)")}>The two passphrases do not match.</span>
                      </>)}
      </div>
      <div style={css("display:flex;gap:9px;margin-top:15px")}>
      <button onClick={v.runBackup} style={css("height:38px;padding:0 15px;border-radius:9px;background:var(--em);color:var(--em-ink,#04231A);font-size:12.5px;font-weight:500;white-space:nowrap")}>Prepare export</button>
      <button onClick={v.closeDialog} style={css("height:38px;padding:0 13px;border:1px solid var(--line);border-radius:9px;font-size:12.5px;color:var(--ink2)")}>Cancel</button>
      </div>

                  </>)}

                  {Boolean(v.bkBusy) && (<>
              
      <div style={css("display:flex;align-items:center;gap:10px;margin-top:16px;padding:13px;border:1px solid var(--line);border-radius:11px;background:var(--panel2)")}>
      <span className="ms" style={css("font-size:20px;color:var(--em);animation:shimmer 1.2s ease-in-out infinite")}>{ICON.progress_activity}</span>
      <span style={css("font-size:12.5px")}>Preparing encrypted export…</span>
      </div>

                  </>)}

                  {Boolean(v.bkDone) && (<>
              
      <div style={css("display:flex;gap:10px;margin-top:16px;padding:13px;border:1px solid var(--emLine);border-radius:11px;background:var(--emSoft)")}>
      <span className="ms" style={css("font-size:20px;color:var(--em)")}>{ICON.task_alt}</span>
      <span style={css("display:block;font-size:12.5px;line-height:1.5")}>Export ready. Passwords included, cloud tokens excluded. The file exists only in this tab until you save it.</span>
      </div>
      <div style={css("display:flex;gap:9px;margin-top:13px")}>
      <button onClick={v.closeDialog} style={css("height:38px;padding:0 15px;border-radius:9px;background:var(--em);color:var(--em-ink,#04231A);font-size:12.5px;font-weight:500")}>Done</button>
      </div>

                  </>)}

                  {Boolean(v.bkFailed) && (<>
              
      <div role="alert" style={css("display:flex;gap:10px;margin-top:16px;padding:13px;border:1px solid var(--coral);border-radius:11px;background:var(--coralSoft)")}>
      <span className="ms" style={css("font-size:20px;color:var(--coral)")}>{ICON.error}</span>
      <span style={css("display:block;font-size:12.5px;line-height:1.5")}>Export failed. Use at least ten characters and make both fields match. Nothing was written.</span>
      </div>
      <div style={css("display:flex;gap:9px;margin-top:13px")}>
      <button onClick={v.retryBackup} style={css("height:38px;padding:0 15px;border-radius:9px;background:var(--em);color:var(--em-ink,#04231A);font-size:12.5px;font-weight:500")}>Try again</button>
      <button onClick={v.closeDialog} style={css("height:38px;padding:0 13px;border:1px solid var(--line);border-radius:9px;font-size:12.5px;color:var(--ink2)")}>Cancel</button>
      </div>

                  </>)}
      </div>

              </>)}

              {Boolean(v.dialogIsErase) && (<>
          
      <div style={css("padding:16px")}>
      <h2 style={css("font-size:16px;font-weight:500;display:flex;align-items:center;gap:9px")}><span className="ms" style={css("font-size:20px;color:var(--coral)")}>{ICON.warning}</span>Erase everything local</h2>
      <p style={css("margin-top:6px;font-size:12px;color:var(--ink2);line-height:1.55")}>This removes, from this device only:</p>
      <ul style={css("margin-top:9px;display:flex;flex-direction:column;gap:5px")}>
      <li style={css("display:flex;gap:8px;font-size:12px;color:var(--ink2)")}><span style={css("width:4px;height:4px;border-radius:50%;background:var(--coral);flex:none;margin-top:6px")}></span>Activity — tool, date and settings entries</li>
      <li style={css("display:flex;gap:8px;font-size:12px;color:var(--ink2)")}><span style={css("width:4px;height:4px;border-radius:50%;background:var(--coral);flex:none;margin-top:6px")}></span>Saved pipelines and tool defaults</li>
      <li style={css("display:flex;gap:8px;font-size:12px;color:var(--ink2)")}><span style={css("width:4px;height:4px;border-radius:50%;background:var(--coral);flex:none;margin-top:6px")}></span>Reusable assets and Bates counters</li>
      <li style={css("display:flex;gap:8px;font-size:12px;color:var(--ink2)")}><span style={css("width:4px;height:4px;border-radius:50%;background:var(--coral);flex:none;margin-top:6px")}></span>Vault entries and remembered cloud connections</li>
      </ul>
      <p style={css("margin-top:11px;padding:10px;border:1px solid var(--line);border-radius:9px;background:var(--panel2);font-size:11.5px;color:var(--ink2);line-height:1.5")}>No documents are involved: none are stored here in the first place. This cannot be undone.</p>
      <div style={css("display:flex;gap:9px;margin-top:14px")}>
      <button onClick={v.confirmErase} style={css("height:38px;padding:0 15px;border-radius:9px;background:var(--coral);color:#fff;font-size:12.5px;font-weight:500;white-space:nowrap")}>Erase everything</button>
      <button onClick={v.closeDialog} style={css("height:38px;padding:0 13px;border:1px solid var(--line);border-radius:9px;font-size:12.5px;color:var(--ink2)")}>Cancel</button>
      </div>
      </div>

              </>)}

              {Boolean(v.dialogIsDelEntry) && (<>
          
      <div style={css("padding:16px")}>
      <h2 style={css("font-size:16px;font-weight:500;display:flex;align-items:center;gap:9px")}><span className="ms" style={css("font-size:20px;color:var(--coral)")}>{ICON.delete}</span>Delete this password</h2>
      <p style={css("margin-top:6px;font-size:12.5px;color:var(--ink2);line-height:1.55")}>“{v.pendingDelete}” will be removed from the vault on this device. This cannot be undone.</p>
      <div style={css("display:flex;gap:9px;margin-top:14px")}>
      <button onClick={v.confirmDeleteEntry} style={css("height:38px;padding:0 15px;border-radius:9px;background:var(--coral);color:#fff;font-size:12.5px;font-weight:500")}>Delete</button>
      <button onClick={v.closeDialog} style={css("height:38px;padding:0 13px;border:1px solid var(--line);border-radius:9px;font-size:12.5px;color:var(--ink2)")}>Cancel</button>
      </div>
      </div>

              </>)}
      </div>
      </div>

        </>)}

        {Boolean(v.toast) && (<>
    
      <div role="status" aria-live="polite" style={css("position:fixed;left:50%;transform:translateX(-50%);bottom:88px;z-index:80;display:flex;align-items:center;gap:9px;padding:10px 14px;border:1px solid var(--line2);border-radius:10px;background:var(--panel);box-shadow:0 12px 30px rgba(0,0,0,.4);font-size:12.5px;animation:slidein 180ms ease both")}>
      <span className="ms" style={css("font-size:18px;color:var(--em)")}>{ICON.check_circle}</span>{v.toast}
          </div>

        </>)}
      </div>

    </>
  );
};

/** The design's own prop defaults, from its data-props schema. */
Component.defaultProps = {
  "initialTheme": "dark",
  "accent": "",
  "density": "comfortable",
  "serverState": "best-effort",
  "reducedMotion": false
};

export default Component;
